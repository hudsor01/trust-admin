---
milestone: v4.0
name: Production Hardening & Completeness
audited: 2026-05-20
status: tech_debt
scope: phases 15-23 (9 phases)
scores:
  requirements: 40/40
  phases: 9/9
  integration: clean (2 minor non-blocking gaps)
  flows: 4/4 traced complete
gaps:
  requirements: []
  integration:
    - id: "INT-G1"
      description: "/api/inventory re-added to proxy publicPaths post-phase-16; REQUIREMENTS.md SEC-08 still claims it removed"
      from: "Phase 16 (proxy publicPaths hardening)"
      to: "Phase 07 (public /forms inventory submission)"
      affected_requirements: ["SEC-08"]
      severity: low
      evidence: "src/proxy.ts:15 has /api/inventory; commit 0a62754 reverted phase-16's 5e1bdc1; proxy-paths test updated to expect it. Route-level auth (SEC-09: hasInventoryAccess timing-safe + 10MB cap + requireTrustAdmin) intact — no security impact. Documentation drift only."
    - id: "INT-G2"
      description: "trustee/beneficiary reorder persists order but no server-side list query applies ORDER BY"
      from: "Phase 23 (reorder mutations + migration 0012 composite indexes)"
      to: "beneficiary.list / getBeneficiariesWithDistributions / trustee.list"
      affected_requirements: ["FEAT-11", "phase-23 sortable lists (no REQ-ID)"]
      severity: low
      evidence: "reorder writes sortIndex/order; migration 0012 adds idx_beneficiary_entity_sort + idx_trustee_entity_order; but beneficiary.ts:20, db/queries.ts:73, trustee.ts:10 return heap order. SortableList cards mask it via client-side .sort() (BeneficiariesClient.tsx:246, TrusteesClient.tsx:147); the main beneficiary DataTable and other *.list consumers get unsorted rows. Composite indexes currently unused by any query plan."
  flows: []
tech_debt:
  - phase: 16-api-infrastructure-security
    items:
      - "verifyAccess.ts ('use server') exported non-async helpers — logged as a build blocker across phases 16/18/19. Phase 23 builds clean, so likely resolved during 17-23, but never explicitly closed in an artifact."
  - phase: 17-dashboard-accounting-performance
    items:
      - "~40 pre-existing inventory-analysis test failures block the lefthook pre-commit hook (worked around in prior sessions)."
      - "INT-01 (accounting report missing 3 asset types) — RESOLVED post-phase-23 (AccountingClient.tsx:296-337 now fetches all 7 asset types)."
  - phase: 18-data-integrity-correctness
    items:
      - "Pre-existing biome formatting issues in proxy-paths/validation/business-logic test files trip the project-wide lint hook."
  - phase: 22-code-quality-cleanup
    items:
      - "No VALIDATION.md authored (Nyquist MISSING)."
      - "46 pre-existing inventory-analysis test failures + 6 useExhaustiveDependencies warnings; pre-commit hook skipped (LEFTHOOK=0) for 2 commits after confirming failures predate the phase."
  - phase: 23-shadcn-registry-adoption-and-dashboard-ux-revamp
    items:
      - "INT-G2 — reorder ORDER BY missing from list queries (see gaps.integration)."
      - "@next/bundle-analyzer never wired into next.config.js — precise cumulative bundle delta unmeasured."
      - "/accounts row-detail shows account metadata, not linked liabilities — accepted via VERIFICATION override; needs a liability.bankAccountId/investmentAccountId FK (architectural)."
      - "DataTable bulkActions/exportable rolled onto /accounts only; 16 other admin tables are opt-in follow-up."
      - "trpc.dashboard.activityCounts query not built — /accounts 30d-activity sparkline suppressed until it lands."
      - "Bequest 'Total value' ships as em-dash (no value column on specific_bequest); Artwork 'Insured count' ships 0 (no insured column); Assets 'Transfer-status progress' approximated (asset.listAll omits transferStatus)."
      - "Purple/violet informational alerts mapped to the accent token (no dedicated milestone hue exists) — flagged for design review."
      - "23-VERIFICATION.md frontmatter still lists the 6 WR code-review warnings as open — stale; all 26 findings were resolved in PR #91 (commit d646876). Documentation-hygiene fix."
nyquist:
  compliant_phases: []
  partial_phases: [15, 16, 17, 18, 19, 20, 21, 23]
  missing_phases: [22]
  overall: partial
---

# Milestone v4.0 Audit — Production Hardening & Completeness

**Audited:** 2026-05-20 (post-PR #91 / phase 23 merge)
**Scope:** Phases 15-23 (9 phases)
**Status:** `tech_debt` — all requirements met, no critical blockers, accumulated non-blocking debt warrants triage before archival.

## Phase Verification

| Phase | Status | Score | Critical Gaps |
|-------|--------|-------|---------------|
| 15 auth-session-security | passed | 5/5 | None |
| 16 api-infrastructure-security | passed | 4/4 | None |
| 17 dashboard-accounting-performance | passed | 5/5 | None |
| 18 data-integrity-correctness | passed | 7/7 | None |
| 19 missing-asset-uis | passed | 7/7 | None |
| 20 beneficiary-distribution-features | passed | 10/10 | None |
| 21 admin-feature-completeness | passed | 10/10 | None |
| 22 code-quality-cleanup | passed | 7/7 | None |
| 23 shadcn-registry-adoption + dashboard UX | passed | 48/48 | None (1 fixed inline, 1 accepted via override) |

All 9 phases verified `passed`. No phase reports `gaps_found` or `human_needed`.

## Requirements Coverage — 40/40 satisfied

3-source cross-reference (REQUIREMENTS.md traceability ↔ phase VERIFICATION.md tables ↔ SUMMARY.md frontmatter) agrees for every REQ-ID:

- **SEC-01..09** (9) — Phases 15-16 — all satisfied
- **PERF-01..05** (5) — Phases 17-18 — all satisfied
- **CORR-01..05** (5) — Phase 18 — all satisfied
- **FEAT-01..11** (11) — Phases 19-21 — all satisfied
- **CLEAN-01..10** (10) — Phases 17, 22 — all satisfied

Zero partial, zero unsatisfied, zero orphaned. Phase 23 carries no REQ-IDs by design — it is a UX-revamp phase tracked via its 48 must-have observable truths. The `DOC-*`/`TXN-*`/`FEE-*`/`ENT-*`/`NOTIF-*`/`ASSOC-*` items in REQUIREMENTS.md are explicitly future-scoped and correctly absent from all v4.0 artifacts.

## Cross-Phase Integration — CLEAN

- **Security chain (15→16→23):** phase 23's 3 new mutations (`hemsRequest.markDistributed`, `trustee.reorder`, `beneficiary.reorder`) all inherit `adminProcedure` + ADMIN_EMAIL override + JWT-bound RLS. Reorder mutations ship a `getClient().begin()` transaction (better than the SUMMARY's described `Promise.all` — silently closes review warning WR-06).
- **Data-integrity (18→23):** migration 0012 follows phase-18 discipline (camelCase identifiers, `IF NOT EXISTS`, `ROW_NUMBER` backfill, journal lockstep).
- **Feature completeness (19→20→21→23):** asset UIs → `asset.listAll` aggregator + KPI strips; portal HEMS → kanban approve → distribution; asset wizard → unchanged `*.create` → lists + dashboard totals. All connected.
- **INT-01 from the prior (2026-03-11) audit is RESOLVED** — `AccountingClient.buildReportHtml` now fetches all 7 asset types; the printable accounting report no longer understates trust property.
- **E2E flows:** 4/4 traced flows complete (admin sign-in→dashboard, beneficiary HEMS→kanban→distribution, wizard asset creation, trustee/beneficiary reorder).

Two minor non-blocking gaps surfaced — see `gaps.integration` (INT-G1 SEC-08 doc drift, INT-G2 reorder ORDER BY missing). Neither breaks a flow.

## Nyquist Validation — partial

Every phase except 22 has a `VALIDATION.md`, but all are `nyquist_compliant: false` / `wave_0_complete: false` (drafts). Phase 22 has none.

| Classification | Phases |
|----------------|--------|
| COMPLIANT | — |
| PARTIAL | 15, 16, 17, 18, 19, 20, 21, 23 |
| MISSING | 22 |

Optional remediation: `/gsd-validate-phase {N}` per phase to close Wave-0 validation gaps.

## Overall Assessment

The milestone's functional bar is fully cleared: 9/9 phases `passed`, 40/40 requirements satisfied with three-source agreement, phase 23's 48 must-haves verified, no orphaned/unsatisfied requirements, no critical or security defects, and cross-phase integration is clean with all traced E2E flows complete. The prior audit's INT-01 gap is closed.

It is classified `tech_debt` (not a clean `passed`) because a real backlog persists: INT-G2 (reorder persists but list queries lack `ORDER BY` — the migration-0012 indexes are currently unused and the "ordered throughout the app" promise is partially unmet), INT-G1 (SEC-08 documentation drift), Nyquist validation `partial`/`missing` across all 9 phases, a pre-existing 40-46-failure inventory-analysis test suite that forced hook bypasses, and a cluster of phase-23 deferrals (bundle analyzer unwired, `activityCounts` query absent, bulk-action rollout incomplete, linked-liabilities FK missing, a few placeholder KPI columns).

None of this blocks archival — the milestone is shippable and complete in capability — but the backlog should be triaged. The cheapest high-value fix is INT-G2 (add `.orderBy()` to 3 queries), which makes migration 0012's indexes earn their keep.

---

*Audit method: aggregated 9 phase VERIFICATION.md + SUMMARY.md files, 3-source requirements cross-reference, parallel cross-phase integration check. Supersedes the 2026-03-11 audit (phases 15-22).*
