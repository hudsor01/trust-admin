# Plan 02-01: Verify and Fix Liability Payment Workflow Tests

**Phase:** 2 - Test Completion
**Created:** 2026-01-09
**Status:** Ready to execute (depends on Phase 1)

---

## Objective

Fix the liability payment workflow integration test to use correct HTTP status codes and verify the complete workflow: Create liability → Record payment → Verify balance updated → Verify expense entry created.

**Success Criteria:**
- Test expects 201 (not 200) for POST /api/liabilities
- Test expects 200 for POST /api/liabilities/{id}/record-payment (action endpoint)
- Balance update verification works correctly
- Expense entry creation verified
- Test passes in full test suite

---

## Context

**Prerequisite:** Phase 1 complete (validation bug fixed, API endpoints functional)

**Current State:**
- Test fails at line 1017: `expect(createLiabilityResponse.status).toBe(200)`
- Receives `201` (correct for resource creation)
- Need to fix status code assertion

**Test Location:** `tests/api.test.ts` lines 995-1088

**Pattern:** This is a multi-step workflow test that verifies:
1. Liability creation via POST
2. Payment recording via special endpoint
3. Balance calculation accuracy
4. Expense entry auto-creation

---

## Tasks

### Task 1: Fix HTTP status code assertion

**File:** `tests/api.test.ts`

**Change:** Line 1017

**Find:**
```typescript
expect(createLiabilityResponse.status).toBe(200);
```

**Replace with:**
```typescript
expect(createLiabilityResponse.status).toBe(201);
```

**Rationale:** POST requests that create resources return `201 Created`, not `200 OK`. The API is correct; the test assertion is wrong.

**Acceptance:**
- Line 1017 updated to expect 201
- No other changes to liability payment test block

---

### Task 2: Run test to verify fix

**Command:**
```bash
bun test tests/api.test.ts --test-name-pattern="liability payment"
```

**Expected Results:**
- Test "Complete workflow: Create liability → Record payment → Verify balance updated → Verify expense entry created" passes
- Balance update assertion passes (line 1053)
- Expense entry assertion passes (lines 1058-1076)
- No validation errors

**Failure Cases:**
- If balance not updated: Check CRUD update logic in `db/queries.ts`
- If expense not created: Check `record-payment` endpoint in `index.ts`
- If other assertions fail: Review test data requirements

**Acceptance:**
- Liability payment workflow test passes completely
- No errors in test output
- All multi-step assertions succeed

---

### Task 3: Verify full test suite still passes

**Command:**
```bash
bun test tests/api.test.ts
```

**Expected Results:**
- 46 tests pass (45 from Phase 1 + 1 fixed liability test)
- 2 HEMS tests still fail (will be fixed in Plan 02-02)
- No regressions in previously passing tests

**Acceptance:**
- Total passing tests increases from 45 to 46
- No new test failures introduced
- Liability payment test appears in pass list

---

## Dependencies

**Required:**
- Phase 1 complete (validation schemas fixed)
- PostgreSQL running with seed data
- API server running (`bun run dev:api`)

**Blocks:**
- Nothing (parallel with Plan 02-02)

---

## Validation Checklist

Before marking complete, verify:

- [ ] Line 1017 expects 201 (not 200)
- [ ] Liability payment test passes in isolation
- [ ] Liability payment test passes in full suite
- [ ] Total passing tests = 46 (up from 45)
- [ ] No new test failures
- [ ] Balance update logic works correctly
- [ ] Expense entry creation works correctly

---

## Rollback Plan

If issues discovered:
1. Git revert: `git checkout tests/api.test.ts`
2. Review actual API behavior with manual curl tests
3. Update test assertions to match API behavior

---

## Success Metrics

**Impact of This Fix:**
- ✅ Critical workflow test coverage restored
- ✅ Validates liability payment business logic
- ✅ Verifies principal/income allocation
- ✅ Confirms accounting entry auto-creation
- ✅ One step closer to full Phase 2 completion

---

## Notes

- This is a simple status code fix - should take <5 minutes
- The test logic itself is correct, only the assertion is wrong
- Recording payment is a special endpoint (not standard CRUD POST)
- Balance update uses Drizzle's `.set()` with SQL calculation
- Expense entry uses standard CRUD create operation

---

*Plan 02-01 ready for execution. Simple fix to restore critical workflow test.*
