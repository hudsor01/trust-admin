# Plan 02-02: Verify and Fix HEMS Approval Workflow Tests

**Phase:** 2 - Test Completion
**Created:** 2026-01-09
**Status:** Ready to execute (depends on Plan 02-01)

---

## Objective

Fix the HEMS (Health, Education, Maintenance, Support) request approval/denial workflow integration tests. Fix HTTP status codes and response field assertions to match actual API behavior.

**Success Criteria:**
- HEMS request creation expects 201 (not 200)
- Approval/denial responses validated correctly
- denialReason field handling fixed
- Both HEMS tests pass in full suite
- Total passing tests increases from 46 to 48

---

## Context

**Prerequisite:** Plan 02-01 complete (liability tests fixed)

**Current State:**
- 2 HEMS tests fail:
  1. Line 1165: `expect(createResponse.status).toBe(200)` receives `201`
  2. Line 1242: `expect(denied.denialReason).toContain(...)` type error

**Test Location:** `tests/api.test.ts` lines 1144-1251

**Pattern:** These tests verify:
1. HEMS request creation
2. Approval workflow → status change to APPROVED
3. Denial workflow → status change to DENIED + reason recorded

---

## Tasks

### Task 1: Fix HEMS request creation status code

**File:** `tests/api.test.ts`

**Change:** Line 1165

**Find:**
```typescript
expect(createResponse.status).toBe(200);
```

**Replace with:**
```typescript
expect(createResponse.status).toBe(201);
```

**Rationale:** POST /api/hems-requests creates a resource, returns 201 Created.

**Acceptance:**
- Line 1165 updated to expect 201
- No other changes to HEMS approval test block

---

### Task 2: Fix denialReason assertion

**File:** `tests/api.test.ts`

**Issue:** Line 1242 has type error: `.toContain()` expects string or array.

**Investigation needed:**
1. Check what `/api/hems-requests/{id}/deny` endpoint actually returns
2. Verify `denialReason` field exists in response
3. Confirm field type (string expected)

**Possible fixes:**
- If `denialReason` not in response: Check API endpoint returns it
- If field is undefined: Add null check before assertion
- If field is wrong type: Fix API or test expectation

**Find current code** (lines 1239-1242):
```typescript
expect(denyResponse.status).toBe(200);
const denied = await denyResponse.json();
expect(denied.status).toBe("DENIED");
expect(denied.denialReason).toContain("does not meet HEMS standard");
```

**Decision tree:**
1. Run test to see actual error message
2. Check API endpoint `/api/hems-requests/{id}/deny` implementation
3. Apply appropriate fix:
   - If `denialReason` missing from response → fix API
   - If `denialReason` is null/undefined → add conditional check
   - If assertion is wrong → update to correct matcher

**Acceptance:**
- denialReason assertion works correctly
- Test validates denial reason is recorded
- No type errors in test output

---

### Task 3: Run tests to verify fixes

**Command:**
```bash
bun test tests/api.test.ts --test-name-pattern="HEMS"
```

**Expected Results:**
- Both HEMS tests pass:
  - "Complete workflow: Create HEMS request → Approve → Verify status change"
  - "Complete workflow: Create HEMS request → Deny → Verify status and reason"
- No validation errors
- Status changes verified correctly

**Acceptance:**
- HEMS approval test passes
- HEMS denial test passes
- Both workflows execute completely

---

### Task 4: Verify full test suite

**Command:**
```bash
bun test tests/api.test.ts
```

**Expected Results:**
- 48 tests pass (ALL integration tests now passing!)
- 0 tests fail
- Phase 2 test completion goals achieved

**Acceptance:**
- Total passing tests = 48 (100% pass rate)
- No test failures
- All critical workflows validated

---

## Dependencies

**Required:**
- Plan 02-01 complete (liability tests fixed)
- PostgreSQL running with seed data
- API server running

**Blocks:**
- Plan 02-03 (can proceed in parallel or after)

---

## Validation Checklist

Before marking complete, verify:

- [ ] Line 1165 expects 201 (not 200)
- [ ] denialReason assertion works correctly
- [ ] Both HEMS tests pass in isolation
- [ ] Both HEMS tests pass in full suite
- [ ] Total passing tests = 48
- [ ] No test failures
- [ ] HEMS approval workflow validated
- [ ] HEMS denial workflow validated

---

## Rollback Plan

If issues discovered:
1. Git revert: `git checkout tests/api.test.ts`
2. Review actual API responses with manual curl tests
3. Update test assertions to match actual behavior

---

## Success Metrics

**Impact of This Fix:**
- ✅ HEMS workflow test coverage complete
- ✅ Validates HEMS approval business logic
- ✅ Validates HEMS denial business logic
- ✅ Verifies beneficiary request workflow
- ✅ 100% integration test pass rate achieved

---

## Notes

- HEMS = Health, Education, Maintenance, Support (trust distribution standard)
- Approval/denial are special action endpoints (not standard CRUD)
- Status field tracks PENDING → APPROVED/DENIED state machine
- Denial reason required for audit trail and beneficiary communication

---

*Plan 02-02 ready for execution. Fixes remaining HEMS test failures.*
