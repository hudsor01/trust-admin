---
phase: 25-loan-terms-calculation-engine
plan: 01
type: execute
---

<objective>
Add loan term fields to liability schema and update validation schemas.

Purpose: Enable storage of loan terms needed for amortization calculations.
Output: Updated schema with 4 new fields, updated Zod validation, database synced.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@db/schema.ts
@db/validation.ts

**Fields to add to liability table:**
| Field | Type | Purpose |
|-------|------|---------|
| `loanTermMonths` | integer (nullable) | Loan duration (360 for 30yr, 60 for 5yr) |
| `loanStartDate` | timestamp (nullable) | When loan originated |
| `escrowMonthly` | numeric(12,2) (nullable) | Monthly escrow for taxes/insurance |
| `isRevolvingCredit` | boolean (default false) | True for credit cards (no fixed term) |

**Existing fields already in schema:**
- `interestRate` (numeric 5,3) - Annual rate
- `monthlyPayment` (numeric 12,2) - Payment amount
- `dueDate` - Final maturity date
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loan term fields to liability schema</name>
  <files>db/schema.ts</files>
  <action>
Add 4 new fields to the `liability` table definition around line 1415 (after monthlyPayment):
- `loanTermMonths`: `t.integer()` - nullable, for loan duration in months
- `loanStartDate`: `t.timestamp({ precision: 3, mode: 'string' })` - nullable, when loan originated
- `escrowMonthly`: `t.numeric({ precision: 12, scale: 2 })` - nullable, monthly escrow amount
- `isRevolvingCredit`: `t.boolean().default(false).notNull()` - for credit cards

Place them logically after `monthlyPayment` and before the property link fields (rentalPropertyId, homesteadId).
  </action>
  <verify>bun run typecheck passes, no errors in schema.ts</verify>
  <done>4 new fields added to liability table with correct types</done>
</task>

<task type="auto">
  <name>Task 2: Update Zod validation schemas</name>
  <files>db/validation.ts</files>
  <action>
The Zod schemas are auto-generated from Drizzle via `createInsertSchema` and `createSelectSchema`.
After adding fields to schema.ts, the types will automatically include the new fields.

Verify that `insertLiabilitySchema` and `updateLiabilitySchema` properly handle the new nullable fields.
If needed, add explicit refinements for:
- `loanTermMonths` should be positive if provided
- `escrowMonthly` should be non-negative if provided

Run `bun run db:push` to sync schema to database.
  </action>
  <verify>bun run db:push succeeds, bun run typecheck passes</verify>
  <done>Schema synced to DB, validation schemas handle new fields correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck` passes
- [ ] `bun run db:push` succeeds
- [ ] New fields visible in Drizzle Studio (`bun run db:studio`)
- [ ] `bun test` passes (existing tests still work)
</verification>

<success_criteria>
- All 4 new fields added to liability schema
- Database schema synced
- TypeScript types updated
- Existing functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/25-loan-terms-calculation-engine/25-01-SUMMARY.md`
</output>
