---
phase: 43-table-consolidation
plan: 01
type: execute
---

<objective>
Delete unused table component files to achieve single table implementation.

Purpose: Eliminate dead code and establish single source of truth for table components.
Output: Only `data-table.tsx` and `virtualized-table.tsx` remain.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-table-consolidation/43-RESEARCH.md
@.planning/phases/43-table-consolidation/43-CONTEXT.md

**From 43-RESEARCH.md:**
- `data-table.tsx` is already TanStack-based (uses `@tanstack/react-table`)
- `tanstack-table.tsx` has 0 imports - dead code
- `ui/data-table.tsx` has 0 imports - dead code
- Goal is achieved by deleting unused files, not migrating

**Tech stack available:**
- TanStack Table (already used in data-table.tsx)
- VirtualizedTable (extends data-table for large lists)

**Key files (keeping):**
- `src/components/data-table.tsx` - Primary table (354 lines, used by 9 pages)
- `src/components/virtualized-table.tsx` - Extension for large datasets

**Files to delete:**
- `src/components/tanstack-table.tsx` - 255 lines, 0 imports
- `src/components/ui/data-table.tsx` - 331 lines, 0 imports
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused tanstack-table.tsx</name>
  <files>src/components/tanstack-table.tsx</files>
  <action>Delete the file src/components/tanstack-table.tsx. This file has 0 imports anywhere in the codebase (verified in 43-RESEARCH.md). It was created as an alternative implementation but never adopted.</action>
  <verify>File no longer exists: `ls src/components/tanstack-table.tsx` returns error</verify>
  <done>File deleted</done>
</task>

<task type="auto">
  <name>Task 2: Delete unused ui/data-table.tsx</name>
  <files>src/components/ui/data-table.tsx</files>
  <action>Delete the file src/components/ui/data-table.tsx. This file has 0 imports anywhere in the codebase (verified in 43-RESEARCH.md). It was scaffolded by shadcn but never used.</action>
  <verify>File no longer exists: `ls src/components/ui/data-table.tsx` returns error</verify>
  <done>File deleted</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and typecheck</name>
  <files>-</files>
  <action>Run typecheck and build to confirm no broken imports or missing dependencies. Both should pass since the deleted files had no imports.</action>
  <verify>`bun run typecheck` and `bun run build` both pass</verify>
  <done>Build passes, typecheck passes, no errors</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `src/components/tanstack-table.tsx` does not exist
- [ ] `src/components/ui/data-table.tsx` does not exist
- [ ] `bun run typecheck` passes
- [ ] `bun run build` passes
- [ ] Tables render correctly (spot check dashboard page)
</verification>

<success_criteria>

- Both unused files deleted
- Build and typecheck pass
- No runtime errors on pages using DataTable
- Single table implementation remains (data-table.tsx + virtualized-table.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/43-table-consolidation/43-01-SUMMARY.md`:

# Phase 43 Plan 01: Table Consolidation Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments

- Deleted unused tanstack-table.tsx (255 lines)
- Deleted unused ui/data-table.tsx (331 lines)
- Single table implementation confirmed

## Files Deleted

- `src/components/tanstack-table.tsx` - 255 lines
- `src/components/ui/data-table.tsx` - 331 lines

## Impact

~586 lines of dead code removed

## Decisions Made

None - straightforward cleanup based on research findings

## Issues Encountered

[Any issues or "None"]

## Next Phase Readiness

Phase complete. Ready for Phase 44 (Query Optimization).
</output>
