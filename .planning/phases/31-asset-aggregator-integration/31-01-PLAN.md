---
phase: 31-asset-aggregator-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/trpc/routers/asset.ts
  - src/server/trpc/routers/dashboard.ts
  - src/app/(admin)/dashboard/_components/DashboardClient.tsx
autonomous: false
requirements: [ASSET-01, ASSET-02]

must_haves:
  truths:
    - "SC-1: After adding a firearm with non-zero dodValue, the dashboard 'Total Assets' KPI increases by that amount"
    - "SC-2: The dashboard allocation pie chart includes a 'Firearms' slice when at least one firearm record exists"
    - "SC-3: Firearm rows appear in the /assets unified view with href linking to /firearms and category 'Firearm'"
    - "SC-4: Removing all firearm records causes the Firearms slice to disappear AND the 'Total Assets' KPI to decrease accordingly"
  artifacts:
    - path: "src/server/trpc/routers/asset.ts"
      provides: "Firearm rows mapped into AssetRow envelope via listAll fan-out"
      contains: "kind: 'firearm'"
    - path: "src/server/trpc/routers/dashboard.ts"
      provides: "firearms array on dashboard summary return shape"
      contains: "from(firearm)"
    - path: "src/app/(admin)/dashboard/_components/DashboardClient.tsx"
      provides: "firearmTotal contribution to Total Assets KPI and Firearms slice in allocation pie"
      contains: "firearmTotal"
  key_links:
    - from: "src/server/trpc/routers/asset.ts"
      to: "db/schema.ts firearm table"
      via: "import + Promise.all query"
      pattern: "from\\(firearm\\)\\.where\\(eq\\(firearm\\.entityId"
    - from: "src/server/trpc/routers/dashboard.ts"
      to: "db/schema.ts firearm table"
      via: "import + Promise.all query"
      pattern: "from\\(firearm\\)\\.where\\(eq\\(firearm\\.entityId"
    - from: "src/app/(admin)/dashboard/_components/DashboardClient.tsx"
      to: "dashboard.summary firearms field"
      via: "summary?.firearms ?? [] destructure + firearmTotal in assetTotal + Firearms entry in allocationData"
      pattern: "summary\\?\\.firearms"
---

<objective>
Wire the `firearm` table into the two hardcoded asset-aggregator fan-outs (`asset.ts:listAll` and `dashboard.ts:summary`) and the dashboard client useMemo that consumes them, so firearms count toward Total Assets, appear as a slice in the allocation pie chart, and show up in the unified `/assets` view.

Purpose: Closes the documented "looks done but isn't" trap (Critical Pitfall #1, research SUMMARY.md / PITFALLS.md §7). The `/firearms` admin page (Phase 30) is fully functional but firearms silently vanish from cross-asset surfaces until both aggregator queries are extended. This phase satisfies ASSET-01 and ASSET-02.

Output: 3 modified files (no new files, no new procedures, no tests). All 4 ROADMAP success criteria verifiable via single 2-minute admin UAT flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-asset-aggregator-integration/31-CONTEXT.md
@.planning/phases/31-asset-aggregator-integration/31-PATTERNS.md
@src/server/trpc/routers/asset.ts
@src/server/trpc/routers/dashboard.ts
@src/app/(admin)/dashboard/_components/DashboardClient.tsx

<interfaces>
<!-- Pre-extracted contracts. Executor consumes these directly — no codebase exploration needed. -->

From `src/server/trpc/routers/asset.ts` (lines 22-61):

```typescript
export type AssetKind =
    | 'vehicle'
    | 'homestead'
    | 'rentalProperty'
    | 'bankAccount'
    | 'investmentAccount'
    | 'personalProperty'
    | 'insurancePolicy'
    // Phase 31 adds: | 'firearm'

export interface AssetRow {
    id: number
    kind: AssetKind
    name: string
    description: string | null
    category: string
    value: string | null
    status: string
    href: string
    transferStatus: string | null   // null only for insurancePolicy
    updatedAt: string
}
```

From `db/schema.ts` (firearm table — confirmed shipped Phase 28): columns used by this plan are `id`, `entityId`, `name`, `description`, `dodValue`, `status`, `transferStatus`, `updatedAt`. Do NOT use `nfaTransferStatus` (per D-06 — that's the ATF Form 5 lifecycle, distinct from physical transfer to a beneficiary).

From `src/lib/money.ts`: `sumStrings(strings: string[]): string`, `toCents(s: string): number` — already imported by DashboardClient.tsx.

Vehicle mapper (the verbatim template for the firearm mapper) — `src/server/trpc/routers/asset.ts` lines 127-140:

```typescript
for (const v of vehicles) {
    rows.push({
        id: v.id,
        kind: 'vehicle',
        name: v.name,
        description: v.description,
        category: 'Vehicle',
        value: v.dodValue,
        status: v.status,
        href: '/vehicles',
        transferStatus: v.transferStatus,
        updatedAt: v.updatedAt,
    })
}
```

Vehicle total + allocation entry templates — `DashboardClient.tsx` lines 211-213 and 249-253:

```typescript
const vehicleTotal = sumStrings(vehicles.map((v) => v.dodValue ?? '0'))
// ...
{ name: 'Vehicles', value: toCents(vehicleTotal) / 100, fill: 'var(--chart-4)' },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire firearm into asset.ts listAll (AssetKind union + Promise.all + mapper loop)</name>
  <files>src/server/trpc/routers/asset.ts</files>
  <read_first>
    - src/server/trpc/routers/asset.ts (entire file — only 244 lines)
    - .planning/phases/31-asset-aggregator-integration/31-PATTERNS.md (§1 — exact insertion lines for asset.ts)
    - .planning/phases/31-asset-aggregator-integration/31-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <action>
    Make four edits to `src/server/trpc/routers/asset.ts`, in this order, following PATTERNS.md §1 verbatim:

    1. Add `firearm` to the named import list from `@/db/schema` (alphabetical placement after `bankAccount`, before `homestead`; the existing block spans lines 4-12).
    2. Append `| 'firearm'` to the `AssetKind` union (line 29, after `'insurancePolicy'`). Per D-01, union order has no semantic meaning — append at the end for minimum diff.
    3. Add `firearms,` to the `Promise.all` destructure array after `insurance,` (line 93 area), and append the firearm query `db.select().from(firearm).where(eq(firearm.entityId, entityId)),` to the `Promise.all` argument array after the `insurancePolicy` query (after line 119).
    4. Add a new firearm mapper loop AFTER the insurance loop (after line 236) and BEFORE the `rows.sort(...)` call on line 239. Copy the vehicle mapper shape verbatim (lines 127-140) with identifier swaps: loop variable `f`, source `firearms`, `kind: 'firearm'`, `category: 'Firearm'` (singular per D-05), `value: f.dodValue`, `href: '/firearms'`, `transferStatus: f.transferStatus` (the generic enum — NOT `f.nfaTransferStatus`, per D-06).

    Do NOT modify the AssetRow interface — all fields the firearm mapper writes already exist on it. Do NOT change `bankAccount`/`investmentAccount`/`insurancePolicy` mappers — they have different `value` sourcing rules (`currentBalance ?? dodValue` and `coverageAmount` respectively); firearm follows the vehicle/homestead/personalProperty `dodValue` rule per D-06.
  </action>
  <verify>
    <automated>
bun run typecheck 2>&1 | tail -20 \
  && grep -q "| 'firearm'" src/server/trpc/routers/asset.ts \
  && grep -q "kind: 'firearm'" src/server/trpc/routers/asset.ts \
  && grep -q "category: 'Firearm'" src/server/trpc/routers/asset.ts \
  && grep -q "href: '/firearms'" src/server/trpc/routers/asset.ts \
  && grep -q "from(firearm).where(eq(firearm.entityId" src/server/trpc/routers/asset.ts \
  && ! grep -q "f\.nfaTransferStatus" src/server/trpc/routers/asset.ts \
  && ! grep -q "category: 'Firearms'" src/server/trpc/routers/asset.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `grep -q "| 'firearm'" src/server/trpc/routers/asset.ts` passes (union extension)
    - `grep -q "kind: 'firearm'" src/server/trpc/routers/asset.ts` passes (mapper kind discriminator)
    - `grep -q "category: 'Firearm'" src/server/trpc/routers/asset.ts` passes (singular per D-05)
    - `grep -q "href: '/firearms'" src/server/trpc/routers/asset.ts` passes (link target per D-06)
    - `grep -q "from(firearm).where(eq(firearm.entityId" src/server/trpc/routers/asset.ts` passes (entity-scoped query)
    - `grep -q "f\.nfaTransferStatus" src/server/trpc/routers/asset.ts` returns nothing (must NOT use nfaTransferStatus per D-06)
    - `grep -q "category: 'Firearms'" src/server/trpc/routers/asset.ts` returns nothing (must be singular per D-05)
  </acceptance_criteria>
  <done>
    asset.ts has firearm imported, `'firearm'` in AssetKind, firearm query in Promise.all, firearms mapper loop pushing rows with the D-06 field choices, and typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire firearm into dashboard.ts summary (Promise.all + return object)</name>
  <files>src/server/trpc/routers/dashboard.ts</files>
  <read_first>
    - src/server/trpc/routers/dashboard.ts (lines 1-165 — the `summary` procedure; other procedures untouched)
    - .planning/phases/31-asset-aggregator-integration/31-PATTERNS.md (§2 — exact insertion lines for dashboard.ts)
    - .planning/phases/31-asset-aggregator-integration/31-CONTEXT.md (D-02)
  </read_first>
  <action>
    Make three edits to `src/server/trpc/routers/dashboard.ts`, all confined to the `summary` procedure (lines 59-164):

    1. Add `firearm,` to the named import list from `@/db/schema` (alphabetical placement between `distribution,` and `hemsRequest,` in the block spanning lines 4-21).
    2. Add `firearms,` to the destructure array after `insurancePolicies,` and before `liabilities,` (after line 75). Append the firearm query block `db.select().from(firearm).where(eq(firearm.entityId, entityId)),` to the `Promise.all` argument array after the `insurancePolicy` query (after line 137) and before the `liability` query.
    3. Add `firearms,` to the `return { ... }` object after `insurancePolicies,` and before `liabilities,` (after line 160).

    Do NOT touch `summaryTotals` or `activityCounts` procedures — they don't need firearm wiring (summaryTotals is income/expense from trustAccounting, activityCounts uses an explicit allowlist and firearm is not in scope for activity counting in this phase). Do NOT touch the existing `ACTIVITY_COUNTS_TABLES` allowlist (lines 34-43).
  </action>
  <verify>
    <automated>
bun run typecheck 2>&1 | tail -20 \
  && grep -q "from(firearm)" src/server/trpc/routers/dashboard.ts \
  && grep -q "where(eq(firearm.entityId, entityId))" src/server/trpc/routers/dashboard.ts \
  && [ "$(grep -cE '^\s*firearms,$' src/server/trpc/routers/dashboard.ts)" = "2" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `grep -q "from(firearm)" src/server/trpc/routers/dashboard.ts` passes (Promise.all query added)
    - `grep -q "where(eq(firearm.entityId, entityId))" src/server/trpc/routers/dashboard.ts` passes (entity scope enforced)
    - Count of lines matching `^\s*firearms,$` equals exactly 2 — once in the `const [ ... ] = await Promise.all([...])` destructure (after `insurancePolicies,`) and once in the `return { ... }` object (after `insurancePolicies,`)
    - `summaryTotals` and `activityCounts` procedures are byte-identical to before (visual diff confirms only the `summary` procedure and the schema import block changed)
  </acceptance_criteria>
  <done>
    dashboard.summary now returns a `firearms` field alongside `vehicles`/`insurancePolicies`/etc; typecheck passes; only the `summary` procedure was modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire firearm into DashboardClient (destructure + firearmTotal + assetTotal + Firearms slice + deps)</name>
  <files>src/app/(admin)/dashboard/_components/DashboardClient.tsx</files>
  <read_first>
    - src/app/(admin)/dashboard/_components/DashboardClient.tsx (lines 60-285 — destructure block, useMemo, dependency array)
    - .planning/phases/31-asset-aggregator-integration/31-PATTERNS.md (§3 — exact insertion lines for DashboardClient.tsx)
    - .planning/phases/31-asset-aggregator-integration/31-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
    Make five edits to `src/app/(admin)/dashboard/_components/DashboardClient.tsx`, all inside or immediately around the existing `{ totalLiabilities, totalAssets, assetAllocationData }` `useMemo` block (lines 198-280):

    1. Add `const firearms = summary?.firearms ?? []` to the summary destructure block, immediately after `const insurancePolicies = summary?.insurancePolicies ?? []` (after line 77).
    2. Inside the `useMemo` body, add `firearmTotal` computation after the existing `insuranceTotal` block (after line 219), copying the vehicleTotal shape with identifier swaps: `const firearmTotal = sumStrings(firearms.map((f) => f.dodValue ?? '0'))`.
    3. Include `firearmTotal` as the last entry in the `assetTotal = sumStrings([...])` array (after `insuranceTotal,` on line 229), so the array is exactly `[bankTotal, investTotal, realEstateTotal, vehicleTotal, personalPropertyTotal, insuranceTotal, firearmTotal]`.
    4. Add a `Firearms` entry to `allocationData` as the last entry BEFORE the `.filter((item) => item.value > 0)` call. The entry is exactly `{ name: 'Firearms', value: toCents(firearmTotal) / 100, fill: 'var(--chart-2)' }` — fill is `var(--chart-2)` per D-04 (NOT `--chart-6`; this is the documented wrap pattern, the same color Investments uses, which is acceptable because both slices rarely coexist with non-zero values and the `.filter` already removes zero-value slices).
    5. Add `firearms,` to the `useMemo` dependency array. Looking at the existing array (lines 271-280), insert `firearms,` between `insurancePolicies,` and `liabilities,`.

    Do NOT change the `bankTotal`/`investTotal`/`realEstateTotal`/`vehicleTotal`/`personalPropertyTotal`/`insuranceTotal`/`liabilityTotal` calculations. Do NOT change any existing entry in `allocationData`. Do NOT change the `.filter((item) => item.value > 0)` clause — it already suppresses the Firearms slice when no firearms exist (closes SC-4).
  </action>
  <verify>
    <automated>
bun run typecheck 2>&1 | tail -20 \
  && bun run lint 2>&1 | tail -20 \
  && grep -q "const firearms = summary?.firearms" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" \
  && grep -q "const firearmTotal = sumStrings" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" \
  && grep -q "name: 'Firearms'" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" \
  && grep -q "fill: 'var(--chart-2)'" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" \
  && ! grep -q "fill: 'var(--chart-6)'" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" \
  && grep -A 9 "const assetTotal = sumStrings" "src/app/(admin)/dashboard/_components/DashboardClient.tsx" | grep -q "firearmTotal,"
    </automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 (no new biome warnings introduced — per MEMORY.md "lint warnings are never pre-existing")
    - `grep -q "const firearms = summary?.firearms" DashboardClient.tsx` passes (destructure added)
    - `grep -q "const firearmTotal = sumStrings" DashboardClient.tsx` passes (total computed inside useMemo)
    - `grep -q "name: 'Firearms'" DashboardClient.tsx` passes (allocation slice present)
    - `grep -q "fill: 'var(--chart-2)'" DashboardClient.tsx` passes — but this is also true for Investments, so additionally verify the Firearms entry is paired with chart-2 (the grep -A 9 check on assetTotal confirms firearmTotal is in the assetTotal array)
    - `grep -q "fill: 'var(--chart-6)'" DashboardClient.tsx` returns nothing (D-04 mandates chart-2, NOT chart-6)
    - The `firearmTotal,` token appears within 9 lines of `const assetTotal = sumStrings`, confirming it is included in the asset sum
  </acceptance_criteria>
  <done>
    DashboardClient destructures `firearms` from summary, computes `firearmTotal`, includes it in `assetTotal`, adds a `Firearms` entry to `allocationData` with `var(--chart-2)`, and lists `firearms` in the useMemo dep array. Typecheck + lint clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Admin UAT — verify all 4 ROADMAP success criteria</name>
  <files>(none — UAT against running dev server)</files>
  <action>
    Block until the admin runs the 4-step UAT below and confirms all 4 ROADMAP success criteria. This is the verification spine for ASSET-01 and ASSET-02 — no automated test substitutes for it (per D-08).
  </action>
  <what-built>
    Firearm rows now flow through the two aggregator fan-outs and contribute to dashboard totals. After Task 3 ships, dashboard "Total Assets" KPI includes firearms, the allocation pie chart shows a "Firearms" slice when any firearm exists, the unified `/assets` table lists firearm rows with category "Firearm" and links to `/firearms`, and removing the last firearm reverses all three surfaces.
  </what-built>
  <how-to-verify>
    Start the dev server (`bun run dev`) and sign in as admin. Run the following 4-step UAT — all 4 ROADMAP success criteria must hold:

    1. **SC-1 (Total Assets KPI increases):**
       - Navigate to `/dashboard`. Note the current "Total Assets" KPI value (call it `BEFORE`).
       - Navigate to `/firearms`. Click the create button and add a firearm with `dodValue = 1000.00` (any other required fields can be filler — Make/Model/Serial/etc).
       - Navigate back to `/dashboard`. Verify "Total Assets" KPI now reads `BEFORE + $1,000.00`.

    2. **SC-2 (Firearms slice in pie chart):**
       - On `/dashboard`, find the asset allocation pie chart.
       - Verify a slice labeled `Firearms` is present (it should appear because the newly-added firearm has `value > 0` and the `.filter((item) => item.value > 0)` clause now lets it through).
       - Verify the slice color is `var(--chart-2)` (same as Investments). It is acceptable that they share a color — they rarely coexist at non-zero values.

    3. **SC-3 (Firearm in /assets unified view):**
       - Navigate to `/assets`.
       - Verify a row exists for the firearm you just added.
       - Verify its Category column shows `Firearm` (singular).
       - Click the row (or its link affordance) — verify it navigates to `/firearms`.

    4. **SC-4 (Removing all firearms reverses SC-1 + SC-2):**
       - Navigate to `/firearms`. Delete the firearm you added.
       - Navigate back to `/dashboard`. Verify "Total Assets" KPI returns to `BEFORE` (or wherever it was prior to this UAT).
       - Verify the `Firearms` slice is no longer rendered in the allocation pie chart.
       - Navigate to `/assets` and verify the firearm row is gone.

    If all 4 SCs hold, type `approved`. If any SC fails, describe which one and what you observed.
  </how-to-verify>
  <verify>
    <human-check>Admin runs the 4-step UAT against the dev server and confirms all 4 ROADMAP SCs (SC-1, SC-2, SC-3, SC-4) hold. The checkpoint blocks until "approved" is entered or a failure is reported.</human-check>
  </verify>
  <acceptance_criteria>
    - SC-1 observed: Adding a firearm with dodValue=1000.00 increased the "Total Assets" KPI by exactly $1,000.00 on the dashboard
    - SC-2 observed: A "Firearms" slice appeared in the dashboard allocation pie chart while the firearm existed
    - SC-3 observed: The firearm row appeared in `/assets` with category "Firearm" (singular) and clicking the row navigated to `/firearms`
    - SC-4 observed: Deleting the firearm caused the "Total Assets" KPI to return to its pre-UAT value AND the "Firearms" slice to disappear AND the row to vanish from `/assets`
    - The admin entered "approved" as the resume signal
  </acceptance_criteria>
  <done>
    All 4 ROADMAP success criteria for Phase 31 confirmed by admin UAT against the running app. ASSET-01 and ASSET-02 are satisfied.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC (admin) | Authenticated admin requests `trpc.asset.listAll` and `trpc.dashboard.summary` with an `entityId` input |
| tRPC → Postgres | Drizzle queries against `firearm` table run as `authenticated` role with RLS `app.is_admin()` policies (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-XEN | Information Disclosure | new `db.select().from(firearm).where(...)` calls in asset.ts and dashboard.ts | mitigate | Both new queries enforce `eq(firearm.entityId, entityId)` in the WHERE clause, matching the verbatim pattern of the other 7 asset tables. Defense-in-depth: `adminProcedure` gates the entire `asset.listAll` and `dashboard.summary` procedures (no beneficiary access), and the firearm table has `.enableRLS()` per Phase 28 with admin-only SELECT. Cross-entity firearm rows cannot leak. Task 1 + Task 2 acceptance criteria grep for the `entityId` predicate. |
| T-31-INV | Tampering (stale state) | DashboardClient useMemo + react-query cache for `dashboard.summary` and `asset.listAll` | accept | The existing FirearmsClient invalidation pattern (firearm.list.invalidate on create/update/delete) propagates to dashboard.summary and asset.listAll through the same shared QueryClient. No code change required; this is the same pattern already proven for vehicles/insurance/etc. If stale-state regressions emerge in UAT, the fix is to add `utils.dashboard.summary.invalidate()` and `utils.asset.listAll.invalidate()` to firearm mutation `onSuccess` — but that's a Phase 30 surface, not this phase. |
| T-31-SC | Tampering | npm/pip/cargo installs | n/a | This phase performs ZERO package installs (no new dependencies, no `bun add`, no schema changes). Package legitimacy gate is not applicable. |
</threat_model>

<verification>
- `bun run typecheck` exits 0 after each task
- `bun run lint` exits 0 after Task 3 (per MEMORY.md "lint warnings are never pre-existing")
- All 4 ROADMAP success criteria verified by admin UAT in Task 4
- Both new `from(firearm).where(eq(firearm.entityId, entityId))` queries enforce entity scope (T-31-XEN mitigation)
- `f.nfaTransferStatus` appears NOWHERE in asset.ts (D-06: generic `transferStatus` only)
- `var(--chart-6)` appears NOWHERE in DashboardClient.tsx for the Firearms slice (D-04: must be `--chart-2`)
- `category: 'Firearm'` (singular) — `category: 'Firearms'` (plural) is forbidden per D-05
</verification>

<success_criteria>
1. ROADMAP SC-1: Adding a firearm with `dodValue = 1000.00` increases dashboard "Total Assets" by $1,000.00 (verified in Task 4 UAT step 1)
2. ROADMAP SC-2: Allocation pie chart shows a "Firearms" slice when at least one firearm exists (verified in Task 4 UAT step 2)
3. ROADMAP SC-3: Firearm row appears in `/assets` with category "Firearm" and links to `/firearms` (verified in Task 4 UAT step 3)
4. ROADMAP SC-4: Deleting the firearm reverses SC-1, SC-2, and removes the row from `/assets` (verified in Task 4 UAT step 4)
5. `bun run typecheck` clean
6. `bun run lint` clean
7. Zero new files created; exactly 3 files modified
8. Zero new tRPC procedures (per D-09)
9. Zero automated tests added (per D-08 — follows Phase 19 precedent for aggregator additions)
</success_criteria>

<output>
Create `.planning/phases/31-asset-aggregator-integration/31-01-SUMMARY.md` when done. Include:
- Which files were edited and line ranges of insertions
- Confirmation of all 4 ROADMAP SCs (with UAT observations from Task 4)
- Confirmation that `f.nfaTransferStatus` was NOT used (D-06)
- Confirmation that the Firearms slice uses `var(--chart-2)` (D-04)
- Confirmation that the category label is `'Firearm'` singular (D-05)
- Confirmation that the AssetKind union extension is the only change to the type surface
</output>
