# Plan 40-01: Quick Fixes for Type and Enum Duplication

## Objective

Remove code duplication: eliminate duplicate type definitions, replace hardcoded enum literals with constants, and ensure `getAllArray()` helper is used consistently across all tRPC routers.

## Execution Context

- Phase 40 of v7.0 Codebase Consolidation
- Discovery level: 0 (pure internal work, no external research needed)
- Scope: Small (3 mechanical tasks)

## Context

### Prior Decisions
- `getAllArray()` was added to crud-factory.ts to eliminate 20+ type guards
- Centralized enum constants exist in `src/lib/type-utils.ts`

### Files Involved
- `src/lib/classification-rules.ts` - may have duplicate AllocationClass type
- `src/server/trpc/routers/liability.ts` - may have hardcoded enum arrays
- `db/crud-factory.ts` - getAllArray() helper
- 23 tRPC routers - migration to getAllArray()

## Tasks

### Task 1: Remove duplicate AllocationClass type
**Status:** Already complete in working tree
- `classification-rules.ts` imports `AllocationClass` from `@/lib/type-utils` (line 10)
- Re-exports it for convenience (line 12)
- No duplicate definition exists

### Task 2: Replace hardcoded enum literals
**Status:** Already complete in working tree
- `liability.ts` imports from type-utils (lines 13-16):
  - `ALLOCATION_CLASS_VALUES`
  - `LIABILITY_TYPE_VALUES`
  - `PAYMENT_METHOD_VALUES`
- No inline `z.enum(["..."])` patterns remain in routers

### Task 3: Migrate routers to getAllArray()
**Status:** Already complete in working tree
- `crud-factory.ts` has `getAllArray()` method (lines 176-179)
- 23 tRPC routers updated to use `getAllArray()` instead of manual type guards
- Changes eliminate pattern: `Array.isArray(result) ? result : result.data`

## Verification

- [ ] TypeScript compiles: `bun run typecheck`
- [ ] Tests pass: `bun test`
- [ ] Dev server starts: `bun run dev`

## Success Criteria

- No duplicate type definitions
- All enum literals use centralized constants
- All tRPC routers use `getAllArray()` for list queries
- TypeScript compiles clean
- Tests pass

## Output

Commit: `refactor(40-01): use getAllArray() across tRPC routers`
Files: 23 tRPC routers + planning artifacts
