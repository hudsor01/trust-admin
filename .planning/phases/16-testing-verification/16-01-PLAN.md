---
phase: 16-testing-verification
plan: 01
type: execute
---

<objective>
Verify all functionality works after Next.js + tRPC migration.

Purpose: Validate that the migration from React/Vite + Bun.serve() to Next.js 16.1 + tRPC v11 preserved all application functionality and meets quality standards.
Output: Verified application with passing tests, working UI, and functional workflows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

**Tech stack (v2.0):**
- Next.js 16.1 (App Router with src/ directory)
- tRPC v11 with TanStack Query
- Better Auth with nextCookies plugin
- 24 tRPC routers merged in router.ts
- 22 routes confirmed in build output

**Current status:**
- ✅ TypeScript compiles without errors (`tsc --noEmit` clean)
- ✅ Build passes (22 routes)
- ❓ Existing tests may need adaptation (written for Bun.serve API)
- ❓ Bundle size target: < 500KB initial load
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and test adaptation</name>
  <files>tests/api.test.ts, tests/auth.test.ts, tests/formatters.test.ts</files>
  <action>
1. Verify TypeScript compiles: `bun run typecheck` or `bunx tsc --noEmit`
2. Check bundle size - aim for < 500KB initial load
3. Run existing tests: `bun test`
4. If tests fail due to API changes (Bun.serve → Next.js/tRPC):
   - Update BASE_URL from localhost:5050 to localhost:3000
   - Update API paths if needed (routes may have changed)
   - Skip auth.test.ts if Better Auth routes changed
   - formatters.test.ts should pass unchanged (pure functions)
5. Document which tests pass/fail and why
  </action>
  <verify>
- `bunx tsc --noEmit` exits with code 0
- `bun test` runs (document results - some may need adaptation)
- Bundle size documented
  </verify>
  <done>
- TypeScript compiles clean
- Test results documented
- Bundle size < 500KB or documented why larger
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migrated Next.js application with 22 routes</what-built>
  <how-to-verify>
**Start dev server:**
```bash
bun run dev  # or: bunx next dev --turbopack
```

**Verify all 14 admin pages render without errors:**
1. Visit http://localhost:3000/ (Dashboard)
2. Visit http://localhost:3000/trustees
3. Visit http://localhost:3000/beneficiaries
4. Visit http://localhost:3000/contacts
5. Visit http://localhost:3000/hems (distributions)
6. Visit http://localhost:3000/hems-queue
7. Visit http://localhost:3000/distribution-wizard
8. Visit http://localhost:3000/bequests
9. Visit http://localhost:3000/accounting
10. Visit http://localhost:3000/properties
11. Visit http://localhost:3000/accounts
12. Visit http://localhost:3000/vehicles
13. Visit http://localhost:3000/liabilities
14. Visit http://localhost:3000/activity-log
15. Visit http://localhost:3000/settings

**Verify all 4 portal pages render:**
16. Visit http://localhost:3000/portal
17. Visit http://localhost:3000/portal/login

**Check for issues:**
- No React errors in browser console (F12 → Console)
- No network errors (F12 → Network, filter by red)
- Data loads from tRPC (check for entity lists, beneficiaries, etc.)
- Toast notifications appear (try an action that should show toast)

**Test inline editing:**
- Click an editable cell (e.g., beneficiary share percentage)
- Change value and blur/press Enter
- Confirm value saves (toast notification, data persists on refresh)
  </how-to-verify>
  <resume-signal>Type "approved" if all pages render correctly, or describe any issues found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>tRPC CRUD operations and workflows</what-built>
  <how-to-verify>
**Test CRUD operations:**

1. **Create** - Add a new contact:
   - Go to /contacts
   - Click "Add Contact" button
   - Fill form and submit
   - Confirm toast success and contact appears in list

2. **Read** - Verify data loads:
   - Go to /beneficiaries
   - Confirm beneficiary list loads with shares, names
   - Go to /liabilities
   - Confirm liabilities load with balances

3. **Update** - Edit inline:
   - On /beneficiaries, click a share percentage
   - Change value, press Enter
   - Confirm save (toast + persists on refresh)

4. **Delete** - Remove a test record:
   - Create a test contact first
   - Delete it using the delete button
   - Confirm removal (toast + gone from list)

**Test liability payment workflow:**
1. Go to /liabilities
2. Click "Record Payment" on a liability
3. Fill payment form (amount, date, method)
4. Submit
5. Confirm:
   - Toast shows success
   - Liability balance decreased
   - Payment appears in payment history

**Test HEMS request workflow:**
1. Go to /hems-queue (pending requests)
2. If no pending requests, may need to create one via portal
3. Approve or deny a request
4. Confirm status updates

**Test auth flow (if portal enabled):**
1. Go to /portal/login
2. Enter beneficiary email
3. Confirm magic link email sent (check console or actual email)
4. Click link → session created → redirected to portal dashboard
  </how-to-verify>
  <resume-signal>Type "approved" if CRUD and workflows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bunx tsc --noEmit` passes (TypeScript clean)
- [ ] `bun run build` succeeds (22 routes)
- [ ] All 14 admin pages render without errors
- [ ] All 4 portal pages render without errors
- [ ] CRUD operations work (create, read, update, delete)
- [ ] Inline editing saves changes
- [ ] Toast notifications appear on success/error
- [ ] Liability payment recording works
- [ ] No console errors in browser
- [ ] Test results documented
</verification>

<success_criteria>
- TypeScript compiles without errors
- All verification checks pass
- All pages render and load data
- CRUD operations work via tRPC
- Key workflows functional (payment recording, HEMS)
- No blocking errors or regressions from migration
</success_criteria>

<output>
After completion, create `.planning/phases/16-testing-verification/16-01-SUMMARY.md`:

# Phase 16 Plan 01: Testing & Verification Summary

**[Migration verified - all pages render, CRUD works, workflows functional]**

## Accomplishments
- [Verification results]

## Test Results
- TypeScript: [pass/fail]
- Build: [pass/fail]
- Unit tests: [X/Y passing]
- Integration tests: [adapted/skipped - reason]

## Pages Verified
- Admin pages: [14/14]
- Portal pages: [4/4]

## Workflows Tested
- CRUD operations: [status]
- Liability payments: [status]
- HEMS requests: [status]
- Auth flow: [status]

## Issues Encountered
[Any issues found during verification]

## Next Step
Ready for Phase 17 (Cleanup)
</output>
