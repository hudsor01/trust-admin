---
phase: 29-firearm-trpc-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/validation.ts
  - src/server/trpc/routers/firearm.ts
  - src/server/trpc/router.ts
  - tests/trpc/firearm.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
tags: [trpc, firearm, asset-router, cqs, nfa]

must_haves:
  truths:
    - "trpc.firearm.list returns entity-scoped firearm rows for admin caller (SC-1)"
    - "trpc.firearm.create throws TRPCError CONFLICT on duplicate serialNumber (SC-2)"
    - "trpc.firearm.byId throws TRPCError NOT_FOUND when id does not belong to the requested entityId (SC-3)"
    - "All 6 procedures (list/byId/create/update/delete/setNfaTransferStatus) reject a beneficiary JWT (SC-4)"
    - "bun run typecheck exits 0 after firearmRouter is registered (SC-5)"
    - "trpc.firearm.setNfaTransferStatus updates nfaTransferStatus + optional taxStampDate/atfControlNumber atomically; throws BAD_REQUEST when target firearm has isNfa=false"
    - "trpc.firearm.update input schema omits nfaTransferStatus — the field cannot flow through generic update"
  artifacts:
    - path: "src/server/trpc/routers/firearm.ts"
      provides: "firearmRouter with 6 procedures + isFirearmSerialConflict predicate"
      min_lines: 130
      contains: "export const firearmRouter = createTRPCRouter({"
    - path: "src/server/trpc/router.ts"
      provides: "firearmRouter registered in appRouter Assets section"
      contains: "firearm: firearmRouter"
    - path: "db/validation.ts"
      provides: "insertFirearmSchemaBase exported"
      contains: "export const insertFirearmSchemaBase"
    - path: "tests/trpc/firearm.test.ts"
      provides: "Integration tests covering SC-1..SC-4 + setNfaTransferStatus + D-03 regression"
      contains: "describe.skipIf(isProductionDb)"
  key_links:
    - from: "src/server/trpc/routers/firearm.ts"
      to: "db/validation.ts"
      via: "import { insertFirearmSchema, insertFirearmSchemaBase }"
      pattern: "from '@/db/validation'"
    - from: "src/server/trpc/router.ts"
      to: "src/server/trpc/routers/firearm.ts"
      via: "import + appRouter registration"
      pattern: "firearm: firearmRouter"
    - from: "tests/trpc/firearm.test.ts"
      to: "src/server/trpc/router.ts"
      via: "createCallerFactory(appRouter)"
      pattern: "createCaller"
---

<objective>
Ship a complete `firearmRouter` (6 procedures: `list`, `byId`, `create`, `update`, `delete`, `setNfaTransferStatus`) — register it in `appRouter` — and lock the contract with an integration test that covers all 5 ROADMAP success criteria plus the D-03 update-omit regression and the NFA guard for `setNfaTransferStatus`.

Purpose: Unblock Phase 30's `/firearms` admin page. Without this router, `trpc.firearm.*` does not exist on the client and Phase 30 cannot start. This phase has no new REQ-IDs — it is a pure dependency phase whose binding contract is the 5 ROADMAP success criteria (SC-1..SC-5).

Output:
- `db/validation.ts` (modified, 1 word): `insertFirearmSchemaBase` exported
- `src/server/trpc/routers/firearm.ts` (NEW, ~150 lines): 6 procedures + local `isFirearmSerialConflict` predicate
- `src/server/trpc/router.ts` (modified): import + registration in Assets section
- `tests/trpc/firearm.test.ts` (NEW): integration coverage of SC-1..SC-4 + setNfaTransferStatus happy path + NFA guard + D-03 regression
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-firearm-trpc-router/29-CONTEXT.md
@.planning/phases/29-firearm-trpc-router/29-RESEARCH.md
@.planning/phases/29-firearm-trpc-router/29-PATTERNS.md
@.planning/phases/29-firearm-trpc-router/29-VALIDATION.md
@.planning/phases/28-firearm-schema-and-migration/28-01-SUMMARY.md
@.planning/phases/28-firearm-schema-and-migration/28-02-SUMMARY.md

<!-- Source-of-truth analogs (named in PATTERNS.md) -->
@src/server/trpc/routers/vehicle.ts
@src/server/trpc/routers/hemsRequest.ts
@src/server/trpc/routers/userManagement.ts
@src/server/trpc/router.ts
@src/server/trpc/init.ts
@db/validation.ts
@db/schema.ts
@db/relations.ts
@tests/trpc/trustee.test.ts
@tests/trpc/hemsRequest.test.ts
@tests/helpers/mock-context.ts
@tests/helpers/db-guard.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase (verified live). -->
<!-- No codebase exploration needed beyond <read_first> in each task. -->

From db/validation.ts (lines 254-275, after step 1 modification):

  export const insertFirearmSchemaBase = createInsertSchema(firearm, {
      createdAt: (schema) => schema.optional(),
      updatedAt: (schema) => schema.optional(),
      name: (schema) => schema.min(1, 'Name is required'),
      make: (schema) => schema.min(1, 'Make is required'),
      model: (schema) => schema.min(1, 'Model is required'),
      serialNumber: () => serialNumberValidation,
      caliber: (schema) => schema.trim().optional(),
      acquisitionCost: () => positiveNumberValidation,
      dodValue: () => positiveNumberValidation,
  })
  export const insertFirearmSchema = insertFirearmSchemaBase.refine(...)
  export const selectFirearmSchema = createSelectSchema(firearm)
  export const updateFirearmSchema = requireAtLeastOneField(
      insertFirearmSchemaBase.partial().refine(...)
  )

From db/schema.ts firearm table — relevant identifiers:
  - firearm.id (bigint identity PK)
  - firearm.entityId (FK)
  - firearm.serialNumber (text, unique index "firearm_serial_number_key")
  - firearm.isNfa (boolean)
  - firearm.nfaTransferStatus (enum NfaTransferStatus: NOT_FILED | FILED | APPROVED)
  - firearm.taxStampDate (text/date)
  - firearm.atfControlNumber (text)
  - firearm.updatedAt (text)

From db/relations.ts firearmRelations:
  - relations: { entity, valuations, documents } (all already wired in Phase 28-01)

From src/server/trpc/init.ts:
  - createTRPCRouter, createCallerFactory
  - adminProcedure (admits admin/trustee/arbiter, FORBIDDEN for beneficiary/user)

From tests/helpers/mock-context.ts:
  - createAdminContext({ id, name, email })
  - createBeneficiaryContext(beneficiaryId: number | null, overrides?)

From tests/helpers/db-guard.ts:
  - isProductionDb (boolean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export insertFirearmSchemaBase + create firearmRouter source file</name>
  <files>db/validation.ts, src/server/trpc/routers/firearm.ts</files>
  <read_first>
    - src/server/trpc/routers/vehicle.ts (the canonical 4-procedure template — copy list/create/update/delete shape verbatim)
    - src/server/trpc/routers/hemsRequest.ts (lines 211-266 markDistributed — preflight-then-update CQS template for setNfaTransferStatus; lines 71-80 byId base shape)
    - src/server/trpc/routers/userManagement.ts (lines 29-39 isBeneficiaryLinkUniqueViolation predicate — duplicate verbatim, substitute constraint name)
    - db/validation.ts lines 254-275 (current state — insertFirearmSchemaBase is const without export at line 257)
    - .planning/phases/29-firearm-trpc-router/29-PATTERNS.md (Source 1/2/3 sections with literal code excerpts and the D-03 critical block)
    - .planning/phases/29-firearm-trpc-router/29-RESEARCH.md Finding 2 (D-03 expression — use insertFirearmSchemaBase.omit({nfaTransferStatus:true}).partial().refine(...) inline; NOT updateFirearmSchema.omit())
    - .planning/phases/29-firearm-trpc-router/29-RESEARCH.md Finding 3 (byId NOT_FOUND guard requirement, deviates from liability.byId)
    - .planning/phases/29-firearm-trpc-router/29-RESEARCH.md Finding 6 (NFA guard recommendation — INCLUDE it; one findFirst + BAD_REQUEST throw when !existing.isNfa)
  </read_first>
  <action>
    Step 1 — Modify `db/validation.ts` line 257: prepend `export ` to the `const insertFirearmSchemaBase` declaration. No other change to that file. Verify with `grep -n "^export const insertFirearmSchemaBase" db/validation.ts` (must return exactly one line).

    Step 2 — Create `src/server/trpc/routers/firearm.ts` (new file) with the exact import block named in `<interfaces>`: `TRPCError` from `@trpc/server`, `and`/`eq` from `drizzle-orm`, `z` from `zod`, `db` from `@/db`, `firearm` from `@/db/schema`, `{ insertFirearmSchema, insertFirearmSchemaBase }` from `@/db/validation`, `{ adminProcedure, createTRPCRouter }` from `../init`.

    Step 3 — Above the router export, declare a local `function isFirearmSerialConflict(err: unknown): boolean` that returns `true` when `err` is an object with `code === '23505'` and `constraint === 'firearm_serial_number_key'`. Copy the userManagement.ts `isBeneficiaryLinkUniqueViolation` shape verbatim per PATTERNS.md Source 2, substituting the constraint string.

    Step 4 — Export `firearmRouter = createTRPCRouter({ ... })` with exactly 6 procedures, in this order:

    (a) `list` — adminProcedure, input `z.object({ entityId: z.coerce.number() })`, returns `db.select().from(firearm).where(eq(firearm.entityId, input.entityId))`. Direct copy of vehicle.list, substitute identifiers.

    (b) `byId` — adminProcedure, input `z.object({ id: z.coerce.number(), entityId: z.coerce.number() })`. Body: `db.query.firearm.findFirst({ where: and(eq(firearm.id, input.id), eq(firearm.entityId, input.entityId)), with: { entity: true, valuations: true, documents: true } })`. If falsy, throw `TRPCError({ code: 'NOT_FOUND', message: 'Firearm not found in this entity' })` per RESEARCH Finding 3 (binding ROADMAP SC-3 — deliberate deviation from liability.byId which returns undefined).

    (c) `create` — adminProcedure, input `insertFirearmSchema`. Wrap the vehicle.create body in `try { ... } catch (err) { if (isFirearmSerialConflict(err)) throw new TRPCError({ code: 'CONFLICT', message: 'A firearm with this serial number already exists.' }); throw err }`. Inside try: `db.insert(firearm).values({ ...input, updatedAt: new Date().toISOString() }).returning()`; if empty array throw `INTERNAL_SERVER_ERROR` with message 'Failed to create firearm'.

    (d) `update` — adminProcedure, input `z.object({ id: z.coerce.number(), entityId: z.coerce.number(), data: insertFirearmSchemaBase.omit({ nfaTransferStatus: true }).partial().refine((data) => Object.values(data).some((v) => v !== undefined), { message: 'Update requires at least one field to be provided' }) })`. Body wrapped in try/catch identical to create's, with one additional first check inside catch: `if (err instanceof TRPCError) throw err` BEFORE the `isFirearmSerialConflict` check (per RESEARCH Pitfall 2). Inside try: `db.update(firearm).set({ ...input.data, updatedAt: new Date().toISOString() }).where(and(eq(firearm.id, input.id), eq(firearm.entityId, input.entityId))).returning()`; if empty throw NOT_FOUND with message 'Firearm not found in this entity'.

    (e) `delete` — adminProcedure, input `z.object({ id: z.coerce.number(), entityId: z.coerce.number() })`. Direct copy of vehicle.delete, substitute `firearm` and message 'Firearm not found in this entity'.

    (f) `setNfaTransferStatus` — adminProcedure, input `z.object({ id: z.coerce.number(), entityId: z.coerce.number(), status: z.enum(['NOT_FILED', 'FILED', 'APPROVED']), taxStampDate: z.string().datetime().optional(), atfControlNumber: z.string().trim().min(1).optional() })`. Body follows hemsRequest.markDistributed preflight pattern (PATTERNS.md Source 3):
      1. Preflight `findFirst` by `and(eq(id), eq(entityId))`
      2. If falsy → throw NOT_FOUND 'Firearm not found in this entity'
      3. If `!existing.isNfa` → throw `BAD_REQUEST` with message 'Cannot set NFA transfer status on a non-NFA firearm' (RESEARCH Finding 6 — Claude's discretion locked IN per planning context scope_notes)
      4. Build `updates: Record<string, unknown> = { nfaTransferStatus: input.status, updatedAt: new Date().toISOString() }`; conditionally spread `taxStampDate` and `atfControlNumber` only when they are not `undefined`.
      5. `db.update(firearm).set(updates).where(and(eq(id), eq(entityId))).returning()`; if empty throw `INTERNAL_SERVER_ERROR` 'Failed to update NFA transfer status'.
      6. Return updated row.

    Do NOT emit any `activityLog.insert(...)` calls (D-09 confirmed in RESEARCH Finding 1 — zero asset routers do this).

    Do NOT add ORDER BY in `list` (D-06 — copy vehicle.list verbatim; table sort is Phase 30 UI concern).

    Do NOT add any forward-API helpers like `byNfaClass` / `byDocumentId` (D-01 — YAGNI; the [Phase 26] STATE.md "tested forward API NOT consumed" lesson applies).

    Step 5 — Run `bun run typecheck`. Must exit 0 (the new file is not yet registered, but it must compile in isolation).
  </action>
  <verify>
    <automated>
      grep -n "^export const insertFirearmSchemaBase = createInsertSchema(firearm" db/validation.ts \
      && grep -c "export const firearmRouter = createTRPCRouter" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "function isFirearmSerialConflict" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "firearm_serial_number_key" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "with: { entity: true, valuations: true, documents: true }" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "setNfaTransferStatus" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "Cannot set NFA transfer status on a non-NFA firearm" src/server/trpc/routers/firearm.ts | grep -q "^1$" \
      && grep -c "createActivityLog\|activityLog\.insert" src/server/trpc/routers/firearm.ts | grep -q "^0$" \
      && bun run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `db/validation.ts` contains exactly one line matching `^export const insertFirearmSchemaBase = createInsertSchema(firearm`
    - `src/server/trpc/routers/firearm.ts` exists and contains the string `export const firearmRouter = createTRPCRouter({`
    - `src/server/trpc/routers/firearm.ts` contains exactly one `function isFirearmSerialConflict` declaration with constraint string `firearm_serial_number_key`
    - `src/server/trpc/routers/firearm.ts` contains the byId eager-load string `with: { entity: true, valuations: true, documents: true }` (covers D-05)
    - `src/server/trpc/routers/firearm.ts` contains `setNfaTransferStatus` and the BAD_REQUEST message `Cannot set NFA transfer status on a non-NFA firearm` (covers NFA guard)
    - `src/server/trpc/routers/firearm.ts` contains zero `createActivityLog` or `activityLog.insert` references (covers D-09)
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>
    insertFirearmSchemaBase is publicly exported; firearm.ts source compiles in isolation with all 6 procedures, the local 23505 predicate, the NOT_FOUND byId guard, the D-03 inline omit/partial/refine update.data schema, and the NFA guard in setNfaTransferStatus.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register firearmRouter in appRouter</name>
  <files>src/server/trpc/router.ts</files>
  <read_first>
    - src/server/trpc/router.ts (current state — imports lines 1-27, appRouter lines 29-70, Assets section lines 35-43)
    - .planning/phases/29-firearm-trpc-router/29-PATTERNS.md "src/server/trpc/router.ts" section (PATTERNS.md lines 329-374 — exact insertion points)
    - .planning/phases/29-firearm-trpc-router/29-RESEARCH.md Finding 4 (insertion position confirmed)
  </read_first>
  <action>
    Step 1 — In `src/server/trpc/router.ts`, add the import for `firearmRouter` alphabetically. Insert between the existing `entityRouter` import (currently line 10) and `hemsRequestRouter` import (currently line 11):
        `import { firearmRouter } from './routers/firearm'`

    Step 2 — In the `appRouter` `Assets (pure CRUD)` section (currently lines 35-43), add `firearm: firearmRouter,` between the `investmentAccount: investmentAccountRouter,` line (currently line 37) and the `homestead: homesteadRouter,` line (currently line 38). The resulting section block must read in order: `bankAccount`, `investmentAccount`, `firearm`, `homestead`, `rentalProperty`, `vehicle`, `personalProperty`, `insurancePolicy`, `trustee`, `specificBequest`, `task` (D-07 — alphabetical placement within the Assets section per RESEARCH Finding 4).

    Step 3 — Run `bun run typecheck`. Must exit 0 (this is SC-5 — the binding type-check gate). If any error surfaces, it must be fixed before the task closes — the firearm router and its registration are now visible to TypeScript and any procedure-signature mismatch will appear here.

    Do NOT reorder any other rows in the Assets section beyond the one insertion (D-06: list/create/update/delete copy vehicleRouter pattern verbatim; the routing block is not the site of further refactor).

    Do NOT add new section comments or grouping changes.
  </action>
  <verify>
    <automated>
      grep -n "import { firearmRouter } from './routers/firearm'" src/server/trpc/router.ts | grep -q "^11:" \
      && grep -c "firearm: firearmRouter," src/server/trpc/router.ts | grep -q "^1$" \
      && bun run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/server/trpc/router.ts` line 11 is exactly `import { firearmRouter } from './routers/firearm'`
    - `src/server/trpc/router.ts` contains exactly one `firearm: firearmRouter,` line
    - The `firearm: firearmRouter,` line appears between `investmentAccount: investmentAccountRouter,` and `homestead: homesteadRouter,` (the Assets section ordering matches the action)
    - `bun run typecheck` exits 0 (ROADMAP SC-5 — binding contract)
  </acceptance_criteria>
  <done>
    The Phase 30 frontend can `import { trpc }` and see `trpc.firearm.*` autocompleting with the 6 procedures end-to-end-typed. The appRouter compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration test covering SC-1..SC-4 + NFA guard + D-03 regression</name>
  <files>tests/trpc/firearm.test.ts</files>
  <read_first>
    - tests/trpc/trustee.test.ts (compact CRUD + beneficiary rejection template — PATTERNS.md identifies this as primary analog; especially lines 1-12 imports, 15-36 caller factory, 38-46 ids object + describe.skipIf, 47-58 beforeAll insert entity, 170-182 .rejects.toThrow() rejection pattern)
    - tests/trpc/hemsRequest.test.ts (TRPCError try/catch assertion style at lines 228-241; afterAll child-row delete-by-entityId pattern at lines 184-207)
    - tests/helpers/mock-context.ts (createAdminContext + createBeneficiaryContext signatures — beneficiary helper accepts `null` for beneficiaryId first arg)
    - tests/helpers/db-guard.ts (isProductionDb gate)
    - src/server/trpc/routers/firearm.ts (the procedures being tested — must exist from Task 1)
    - src/server/trpc/router.ts (must show firearm registered — confirms appRouter has the new procedures)
    - db/schema.ts firearm table (column names + isNfa boolean field — needed for both NFA-true and non-NFA test fixtures)
    - .planning/phases/29-firearm-trpc-router/29-PATTERNS.md tests/trpc/firearm.test.ts section (lines 378-561 — every test shape is named there)
    - .planning/phases/29-firearm-trpc-router/29-VALIDATION.md (the 5 SCs + the recommended additional coverage list)
  </read_first>
  <action>
    Create new file `tests/trpc/firearm.test.ts` modeled on `tests/trpc/trustee.test.ts` for structure and `tests/trpc/hemsRequest.test.ts` for TRPCError assertion shape.

    Step 1 — Imports per PATTERNS.md: `afterAll, beforeAll, describe, expect, test` from `bun:test`; `TRPCError` from `@trpc/server`; `eq` from `drizzle-orm`; `db` from `@/db`; `entity, firearm` from `@/db/schema`; `createCallerFactory` from `@/server/trpc/init`; `appRouter` from `@/server/trpc/router`; `isProductionDb` from `../helpers/db-guard`; `createAdminContext, createBeneficiaryContext` from `../helpers/mock-context`.

    Step 2 — Module-level constants: `TEST_TIMEOUT = 30000`, `const createCaller = createCallerFactory(appRouter)`, `const TS = Date.now().toString().slice(-8)`, and `adminCaller()` / `beneficiaryCaller()` factory functions using IDs `'981'` / `'982'` and emails `firearm-admin@test.com` / `firearm-ben@test.com`. `beneficiaryCaller` must pass `null` as the first arg to `createBeneficiaryContext` (signature confirmed in PATTERNS.md line 520).

    Step 3 — `ids` object tracking `entityId: number | null` plus a small number of fixture row ids as needed (e.g. `nonNfaFirearmId`, `nfaFirearmId` — only what the tests reference). All other firearm rows can be created ad-hoc without tracking, since cleanup deletes by entityId.

    Step 4 — Wrap the suite in `describe.skipIf(isProductionDb)('firearm router', () => { ... })`.

    Step 5 — `beforeAll`: insert a test `entity` (use `name: 'Firearm Router Test Entity ' + TS`, all other required fields copied from the trustee.test.ts beforeAll). Insert a non-NFA fixture firearm and an NFA fixture firearm for tests that need stable IDs. Record the inserted ids into the `ids` object. Use `TEST_TIMEOUT`.

    Step 6 — Tests covering the binding contract (one test per requirement; group as you like but every behavior below MUST be exercised):

      SC-1: `firearm.list` returns rows scoped to `entityId`. Insert 1+ firearm rows, call `adminCaller().firearm.list({ entityId })`, expect array length ≥ inserted count and every returned row to have `entityId === ids.entityId`.

      SC-2: `firearm.create` rejects duplicate serial. Create firearm with `serialNumber: 'DUPE' + TS`. Then attempt a second create with the same serial. Use the try/catch pattern (PATTERNS.md lines 528-560): force-fail if no throw; on catch, assert `err instanceof TRPCError` and `(err as TRPCError).code === 'CONFLICT'` and message matches `/serial number/i`.

      SC-3: `firearm.byId` throws NOT_FOUND when id does not belong to entity. Create a firearm in `ids.entityId`. Call `firearm.byId({ id: <that-id>, entityId: 999999 })` AND `firearm.byId({ id: 999999999, entityId: ids.entityId! })`. Both must throw TRPCError code `NOT_FOUND`. Use try/catch pattern.

      SC-4: All 6 procedures reject beneficiary JWT. One test per procedure (recommended), or one describe block with a loop — planner discretion. Use `.rejects.toThrow()` pattern from trustee.test.ts lines 170-182. The 6 procedures: `list`, `byId`, `create`, `update`, `delete`, `setNfaTransferStatus`. For mutations that need an id, use a small dummy `id: 1` — they will fail before any DB work because adminProcedure rejects the beneficiary context first.

      SC-5: covered by Task 2's typecheck — no test code needed here.

    Step 7 — Additional coverage per VALIDATION.md "Additional coverage beyond the 5 SCs" list:

      (a) `setNfaTransferStatus` happy path: create an NFA firearm (`isNfa: true, nfaClass: <valid>`), call `setNfaTransferStatus({ id, entityId, status: 'FILED', atfControlNumber: 'CTRL-' + TS })`. Expect returned row to have `nfaTransferStatus === 'FILED'` and `atfControlNumber === 'CTRL-' + TS`. Then call again with `status: 'APPROVED', taxStampDate: '2026-01-15T00:00:00.000Z'` — expect `nfaTransferStatus === 'APPROVED'`.

      (b) `setNfaTransferStatus` rejects non-NFA firearm with BAD_REQUEST: call against `ids.nonNfaFirearmId` (`isNfa: false`). try/catch: assert TRPCError code `BAD_REQUEST` and message matches `/non-NFA/i`. This locks the NFA-guard decision (RESEARCH Finding 6).

      (c) D-03 regression: confirm `nfaTransferStatus` cannot flow through generic update. Call `firearm.update({ id: ids.nfaFirearmId, entityId, data: { nfaTransferStatus: 'APPROVED' } as unknown })` with a `@ts-expect-error` annotation on the line. Expect the call to reject (Zod will strip or fail validation; either outcome is acceptable — the test asserts the field did NOT persist by following up with `firearm.byId` and checking `nfaTransferStatus !== 'APPROVED'`). At minimum: assert that calling `update` with ONLY `{ data: { nfaTransferStatus: 'APPROVED' } }` (no other fields) rejects on the "Update requires at least one field" refine (since omit removes the field BEFORE the refine sees it, making `Object.values(data)` all-undefined). Use try/catch and assert TRPCError thrown.

    Step 8 — `afterAll` cleanup: `delete from firearm where entityId = ids.entityId`, then `delete from entity where id = ids.entityId`. Wrap with `if (ids.entityId)` guard. Use `TEST_TIMEOUT`.

    Step 9 — Run `bun test tests/trpc/firearm.test.ts`. All tests must pass. Run `bun run typecheck` once more — must exit 0.
  </action>
  <verify>
    <automated>
      grep -c "describe.skipIf(isProductionDb)" tests/trpc/firearm.test.ts | grep -q "^1$" \
      && grep -c "createCallerFactory(appRouter)" tests/trpc/firearm.test.ts | grep -q "^1$" \
      && grep -c "TRPCError" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && grep -c "CONFLICT" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && grep -c "NOT_FOUND" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && grep -c "BAD_REQUEST" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && grep -c "setNfaTransferStatus" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && grep -c "beneficiaryCaller" tests/trpc/firearm.test.ts | grep -qv "^0$" \
      && bun test tests/trpc/firearm.test.ts \
      && bun run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/trpc/firearm.test.ts` exists, wrapped in `describe.skipIf(isProductionDb)`
    - File uses `createCallerFactory(appRouter)` (proves it tests through the real registered router — implicit SC-5 coverage)
    - File contains assertions for `TRPCError` codes `CONFLICT` (SC-2), `NOT_FOUND` (SC-3), and `BAD_REQUEST` (NFA guard)
    - File contains `setNfaTransferStatus` tests (happy path + non-NFA rejection)
    - File contains `beneficiaryCaller` calls covering all 6 procedures (SC-4)
    - `bun test tests/trpc/firearm.test.ts` exits 0 with all tests passing (test count >= 9: list, byId-wrong-entity, byId-nonexistent, create-conflict, 6× beneficiary rejection — minimum; expect more)
    - `bun run typecheck` still exits 0
  </acceptance_criteria>
  <done>
    All 5 ROADMAP success criteria are provably exercised by automated tests; the NFA guard locked in Task 1 is regression-protected; the D-03 omit decision is regression-protected. Phase 29 contract is sealed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → tRPC | Untrusted client input (entityId, ids, serialNumber, NFA status) crosses into the firearm router |
| tRPC → DB | tRPC-validated input crosses into Drizzle; RLS provides defense-in-depth |
| Beneficiary JWT → adminProcedure | Role-bearing context attempts to invoke admin-only procedures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-XEN | Tampering | All 6 procedures (`firearm.ts`) | mitigate | Every WHERE clause includes `eq(firearm.entityId, input.entityId)` (or `and(eq(id), eq(entityId))` for mutations); `db.query.firearm.findFirst` in byId enforces the same; RLS `app.is_admin()` policies on the firearm table are defense-in-depth. Tested by SC-3 (NOT_FOUND on wrong entityId). |
| T-29-SER | Tampering | `create` + `update` mutations | mitigate | `isFirearmSerialConflict` predicate catches Postgres 23505 errors on constraint `firearm_serial_number_key` and remaps to TRPCError CONFLICT. The DB unique index is the authoritative defense; the router maps the error to a typed client-readable code. Tested by SC-2 (CONFLICT on duplicate serial). |
| T-29-AUTH | Elevation of Privilege | All 6 procedures (`firearm.ts`) | mitigate | All 6 procedures declared with `adminProcedure` (init.ts line 221). `adminProcedure` admits only `admin`/`trustee`/`arbiter` roles; beneficiary/user roles throw FORBIDDEN before any DB work. Tested by SC-4 (.rejects.toThrow() on all 6 procedures with beneficiary context). |
| T-29-NFA | Tampering / Data Integrity | `setNfaTransferStatus` mutation | mitigate | Router-layer preflight `findFirst` checks `existing.isNfa === true`; throws TRPCError BAD_REQUEST when false. Prevents misleading ATF Form 5 tracking on non-NFA firearms. Tested by setNfaTransferStatus non-NFA rejection test in Task 3. |
| T-29-RTH | Tampering | `update` mutation try/catch | mitigate | The catch block re-throws `TRPCError` instances FIRST (`if (err instanceof TRPCError) throw err`) before invoking `isFirearmSerialConflict`. Without this guard, a NOT_FOUND thrown by the `.returning()` empty-check would be swallowed and incorrectly re-emitted as CONFLICT. RESEARCH Pitfall 2 explicitly memorializes this. |
| T-29-D03 | Tampering | `update.input.data` schema | mitigate | `nfaTransferStatus` is OMITTED from `update.input.data` by constructing the data schema as `insertFirearmSchemaBase.omit({ nfaTransferStatus: true }).partial().refine(...)` inline. The dedicated `setNfaTransferStatus` mutation is the single path to change the field (CQS / D-02). Tested by Task 3 D-03 regression assertion. |
| T-29-AGG | Information Disclosure | All 6 procedures | accept | Manual activity logging is intentionally NOT added — RESEARCH Finding 1 confirms zero asset routers do this. Trust admin audit needs are met by the existing `activityLog` table population in `userManagement` + DB-trigger absence; adding logging here would be premature and inconsistent. Re-evaluate only if user-facing audit requirements surface for asset CRUD. |
</threat_model>

<verification>
After all 3 tasks:

1. `bun run typecheck` exits 0 — ROADMAP SC-5 (binding)
2. `bun test tests/trpc/firearm.test.ts` exits 0 — covers SC-1..SC-4 + NFA guard + D-03 regression
3. `grep -n "firearm: firearmRouter" src/server/trpc/router.ts` returns exactly one line in the Assets section
4. `grep -n "^export const insertFirearmSchemaBase" db/validation.ts` returns exactly one line
5. `grep -c "createActivityLog\|activityLog\.insert" src/server/trpc/routers/firearm.ts` returns `0` (D-09)
6. Full suite green: `bun test --timeout 30000 tests/api tests/components tests/lib tests/trpc tests/*.test.ts` exits 0 (no regression)
</verification>

<success_criteria>
The phase is complete when ALL of the following are TRUE:

- [ ] `db/validation.ts` exports `insertFirearmSchemaBase` (1-word change)
- [ ] `src/server/trpc/routers/firearm.ts` exists with exactly 6 procedures + 1 local predicate function + 0 activity-log calls
- [ ] `firearm.byId` eager-loads `entity, valuations, documents` AND throws `TRPCError NOT_FOUND` when result is undefined (covers SC-3 and D-05)
- [ ] `firearm.update.input.data` uses `insertFirearmSchemaBase.omit({ nfaTransferStatus: true }).partial().refine(...)` inline — NOT `updateFirearmSchema.omit()` (covers D-03 correctly per RESEARCH Finding 2)
- [ ] `firearm.update` try/catch re-throws `TRPCError instanceof` BEFORE the `isFirearmSerialConflict` check (covers Pitfall 2)
- [ ] `firearm.setNfaTransferStatus` guards `existing.isNfa === true` with TRPCError BAD_REQUEST when false (NFA guard locked IN)
- [ ] `firearm.create` and `firearm.update` both wrap their DB calls in try/catch with the `isFirearmSerialConflict` predicate mapping 23505 → CONFLICT
- [ ] `src/server/trpc/router.ts` imports `firearmRouter` between `entityRouter` and `hemsRequestRouter` (alphabetical)
- [ ] `appRouter` Assets section contains `firearm: firearmRouter,` between `investmentAccount` and `homestead` (D-07 + Finding 4)
- [ ] `bun run typecheck` passes with 0 errors (SC-5)
- [ ] `tests/trpc/firearm.test.ts` exists, gated with `describe.skipIf(isProductionDb)`, and passes via `bun test tests/trpc/firearm.test.ts`
- [ ] Test file exercises: list entity-scoping (SC-1), create CONFLICT on dup serial (SC-2), byId NOT_FOUND on wrong entity (SC-3), beneficiary rejection on all 6 procedures (SC-4), setNfaTransferStatus happy path, setNfaTransferStatus BAD_REQUEST on non-NFA, and the D-03 omit regression
- [ ] Full suite (`bun test --timeout 30000 tests/api tests/components tests/lib tests/trpc tests/*.test.ts`) remains green with no regressions
</success_criteria>

<output>
Create `.planning/phases/29-firearm-trpc-router/29-01-SUMMARY.md` when done. Document any deviations from this plan (e.g. if the NFA guard or D-03 inline expression needed a different shape at the executor's discretion). Reference the 5 ROADMAP success criteria in the SUMMARY's `requirements-completed` field (use `[SC-1, SC-2, SC-3, SC-4, SC-5]` since there are no REQ-IDs for this phase).
</output>
