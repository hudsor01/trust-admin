---
phase: 28-progress-visualization
plan: 01
type: execute
---

<objective>
Add visual progress indicators for liability payoff tracking to the dashboard and liabilities page.

Purpose: Give users immediate visual feedback on loan payoff progress without manual calculations.
Output: LiabilityProgressBar component, dashboard liability summary section, payoff projection displays.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior work (from Phase 25-27):**
- `src/lib/amortization.ts` has calculation functions: `getCurrentLoanPosition`, `estimatePayoffDate`, `calculatePaymentSplit`
- `src/components/charts/` has existing chart patterns (recharts via shadcn)
- Dashboard already fetches `trpc.liability.list.useQuery()`
- Liabilities page has full CRUD + bulk entry mode

**Key files:**
@src/lib/amortization.ts
@src/app/(admin)/dashboard/page.tsx
@src/app/(admin)/liabilities/page.tsx
@src/components/ui/progress.tsx

**Established patterns:**
- shadcn Progress component for progress bars
- sumStrings() from money.ts for totals
- formatCurrency() for display
- tRPC queries with isLoading states
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LiabilityProgressCard component</name>
  <files>src/components/liability-progress-card.tsx</files>
  <action>
Create a reusable card component showing liability payoff progress:

1. Props: `liability` object (from tRPC), optional `showDetails` boolean
2. Display elements:
   - Liability name (creditor) and type badge
   - Progress bar showing % paid (originalAmount vs currentBalance)
   - Text: "X% paid off" or "Paid in full" if balance is 0
   - If loan type with terms: "Payment X of Y" using getCurrentLoanPosition()
   - Estimated payoff date using estimatePayoffDate() if applicable
   - Monthly payment amount
3. Visual states:
   - Green progress for >75% paid
   - Yellow for 25-75%
   - Default for <25%
4. Handle edge cases:
   - No originalAmount (show current balance only)
   - Revolving credit (no term-based progress)
   - Zero balance (show "Paid Off" badge)

Use existing Progress component from shadcn. Import amortization functions for calculations.
  </action>
  <verify>Component renders without errors when imported into a test page</verify>
  <done>LiabilityProgressCard component created with progress visualization and payoff estimates</done>
</task>

<task type="auto">
  <name>Task 2: Add Liability Summary section to Dashboard</name>
  <files>src/app/(admin)/dashboard/page.tsx</files>
  <action>
Add a "Liability Overview" section to the dashboard below the existing charts:

1. Create section with Card wrapper, title "Debt Overview"
2. Summary metrics row:
   - Total debt (sum of all currentBalance)
   - Monthly payments total (sum of monthlyPayment)
   - Liabilities count
   - Average payoff progress %
3. Top 3-5 liabilities list using LiabilityProgressCard (compact mode)
4. "View All" link to /liabilities page
5. Handle loading state with Skeleton components
6. Handle empty state: "No liabilities recorded"

Position: After the asset charts section, before tasks section.
Use existing sumStrings() for totals. Match existing dashboard card styling.
  </action>
  <verify>bun run typecheck passes, dashboard shows liability section with real data</verify>
  <done>Dashboard displays liability overview with totals and top liabilities with progress</done>
</task>

<task type="auto">
  <name>Task 3: Add progress indicators to Liabilities page table</name>
  <files>src/app/(admin)/liabilities/page.tsx</files>
  <action>
Enhance the liabilities DataTable with visual progress:

1. Add "Progress" column to liabilityColumns:
   - Mini progress bar (narrow, inline)
   - Percentage text next to it
   - Color-coded based on progress %
2. Add "Payoff Date" column:
   - Use estimatePayoffDate() for loans with terms
   - Show "-" for revolving credit
   - Format with formatDate()
3. Update column order: Type, Creditor, Balance, Progress, Monthly Payment, Payoff Date, Actions
4. Ensure columns are appropriately sized (Progress ~100px, Payoff ~100px)

Import amortization functions. Handle null/undefined gracefully with fallbacks.
  </action>
  <verify>bun run typecheck passes, liabilities table shows progress bars and payoff dates</verify>
  <done>Liabilities table includes progress visualization and estimated payoff dates</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck` passes
- [ ] `bun run lint` passes
- [ ] `bun test` passes (203+ tests)
- [ ] Dashboard shows liability summary section
- [ ] Liabilities table shows progress bars
- [ ] Progress calculations display correctly for different liability types
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Progress visualization works for mortgages, loans, and credit cards
- Dashboard provides at-a-glance debt overview
</success_criteria>

<output>
After completion, create `.planning/phases/28-progress-visualization/28-01-SUMMARY.md`
</output>
