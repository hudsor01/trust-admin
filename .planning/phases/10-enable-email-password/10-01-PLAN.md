---
phase: 10-enable-email-password
plan: 01
type: execute
---

<objective>
Enable email/password authentication in Neon Auth alongside the existing magic link (email OTP) method.

Purpose: Allow beneficiary accounts to be provisioned with email/password credentials (Phase 51 dependency). Admin can continue using magic link.
Output: Working email/password sign-in, sign-up, forgot-password, and reset-password flows via Neon Auth UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-enable-email-password/CONTEXT.md
@.planning/phases/49-fix-role-mismatch/49-01-SUMMARY.md

# Key source files:
@src/app/layout.tsx
@src/app/auth/[path]/page.tsx

**Constraining decisions:**
- Neon Auth SDK already supports all email/password methods (`signUp.email`, `signIn.email`, `requestPasswordReset`, `resetPassword`, `changePassword`)
- The `AuthView` component auto-renders forms based on enabled props
- `authViewPaths` export from `@neondatabase/auth/react/ui/server` provides all valid auth paths including `forgot-password` and `reset-password`
- Console enablement is BLOCKING — SDK methods will not work without it
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Enable Email/Password authentication in the Neon Console</action>
  <instructions>
  This cannot be automated — requires Neon Console access.

  1. Go to Neon Console → your project → Auth → Configuration
  2. Enable "Email/Password" authentication method
  3. (Optional) Configure custom SMTP with Resend for production emails:
     - SMTP Host: smtp.resend.com
     - SMTP Port: 465
     - SMTP Username: resend
     - SMTP Password: [your RESEND_API_KEY from .env]
     - From Email: noreply@thehudsonfam.com (or your domain)
  </instructions>
  <verification>Email/Password toggle is ON in Neon Console Auth Configuration</verification>
  <resume-signal>Type "done" when Email/Password is enabled in Neon Console</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Enable emailAndPassword prop and add generateStaticParams</name>
  <files>src/app/layout.tsx, src/app/auth/[path]/page.tsx</files>
  <action>
  1. In `src/app/layout.tsx`: Change the `NeonAuthUIProvider` props from `emailOTP` to `emailAndPassword`. Keep magic link available by also including `emailOTP`:
     ```
     <NeonAuthUIProvider authClient={authClient} redirectTo="/dashboard" emailOTP emailAndPassword>
     ```
     This enables both methods — users can choose email/password OR magic link.

  2. In `src/app/auth/[path]/page.tsx`: Add `generateStaticParams` using `authViewPaths` from `@neondatabase/auth/react/ui/server`. This ensures all auth paths (including `forgot-password`, `reset-password`) are statically generated:
     ```
     import { authViewPaths } from '@neondatabase/auth/react/ui/server'

     export function generateStaticParams() {
       return Object.values(authViewPaths).map((path) => ({ path }))
     }
     ```
     This follows the same pattern used by `src/app/account/[path]/page.tsx`.

  DO NOT change any other auth files (client.ts, server.ts, auth events, API routes).
  DO NOT remove `emailOTP` — keep both auth methods available.
  </action>
  <verify>bun run typecheck passes. bun run build succeeds with all auth paths generated.</verify>
  <done>NeonAuthUIProvider has both emailOTP and emailAndPassword props. Auth page has generateStaticParams with authViewPaths. TypeScript compiles cleanly. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run typecheck` passes without errors
- [ ] `bun run build` succeeds — verify auth paths in output (sign-in, sign-up, forgot-password, reset-password)
- [ ] NeonAuthUIProvider has both `emailOTP` and `emailAndPassword` props
- [ ] Auth page has `generateStaticParams` using `authViewPaths`
- [ ] No other auth files modified
</verification>

<success_criteria>

- Email/Password enabled in Neon Console
- NeonAuthUIProvider configured with both emailOTP and emailAndPassword
- Auth pages statically generated for all paths including password-related routes
- TypeScript compiles cleanly
- Build succeeds
- No regressions in existing auth flows
</success_criteria>

<output>
After completion, create `.planning/phases/50-enable-email-password/50-01-SUMMARY.md`:

# Phase 50 Plan 01: Enable Email/Password Auth Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [file] - [description]

## Decisions Made
- [decisions]

## Issues Encountered
- [issues or "None"]

## Next Step
Phase 50 complete, ready for Phase 51 (admin-user-provisioning)
</output>
