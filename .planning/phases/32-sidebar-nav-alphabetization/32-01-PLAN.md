---
phase: 32-sidebar-nav-alphabetization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app-sidebar.tsx
autonomous: false
requirements: [ASSET-03, ASSET-04]
tags: [sidebar, nav, firearms, ui]

must_haves:
  truths:
    - "The Assets dropdown sub-items appear in the order: Accounts, Artwork, Firearms, Insurance, Personal Property, Properties, Vehicles (SC-1, covers ASSET-04)"
    - "Clicking Firearms in the sidebar navigates to /firearms (SC-2, covers ASSET-03)"
    - "Hovering Firearms in the sidebar prefetches utils.firearm.list and utils.entity.list (SC-3)"
  artifacts:
    - path: "src/components/app-sidebar.tsx"
      provides: "Assets nav reordered + firearms prefetch handler + Firearms SidebarMenuSubItem"
      contains: "firearms: () =>"
  key_links:
    - from: "src/components/app-sidebar.tsx (prefetch.firearms)"
      to: "utils.firearm.list (Phase 29) + utils.entity.list"
      via: "onMouseEnter={prefetch.firearms} on the Firearms Link"
      pattern: "utils.firearm.list.prefetch"
    - from: "src/components/app-sidebar.tsx (Firearms SidebarMenuSubItem)"
      to: "/firearms route (Phase 30)"
      via: "<Link href=\"/firearms\">"
      pattern: "href=\"/firearms\""
---

<objective>
Add the Firearms entry to the Assets sidebar navigation and alphabetize all 7 sub-items.

Purpose: Closes the final v5.0 UX gap — /firearms is reachable from the sidebar instead of only by typing the URL or clicking through /assets. Satisfies ASSET-03 (Firearms reachable from Assets nav) and ASSET-04 (Assets sub-items alphabetical).

Output: A modified `src/components/app-sidebar.tsx` containing (a) a new `firearms` prefetch handler in the `prefetch` object and (b) a fully reordered Assets `SidebarMenuSub` block containing 7 items in alphabetical order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-sidebar-nav-alphabetization/32-CONTEXT.md
@.planning/phases/32-sidebar-nav-alphabetization/32-PATTERNS.md
@.planning/phases/30-firearms-admin-page/30-01-SUMMARY.md
@.planning/phases/29-firearm-trpc-router/29-01-SUMMARY.md
@src/components/app-sidebar.tsx

<interfaces>
<!-- Confirmed by codebase scan (2026-05-22). Executor uses these directly — no exploration needed. -->

File: src/components/app-sidebar.tsx
- Total lines: 555
- Imports (lines 1-44, including Link from next/link, lucide-react icons, SidebarMenu* primitives, trpc utils)
- prefetch object: lines 45..~117 (final closing line of the object). Existing keys in declared order:
    properties (line 83), accounts (line 88), vehicles (line 93),
    personalProperty (line 97), artwork (line 104), insurance (line 111)
- Assets SidebarMenuSub block: lines 379..~485 (block contains 6 SidebarMenuSubItem children in declared order:
    Properties, Accounts, Vehicles, Personal Property, Artwork, Insurance)
- No SidebarMenuSubItem in the Assets group uses an icon. The only lucide icon in the file is LogOut in the footer.
- Existing items do NOT pass `prefetch={false}` on the inner <Link>. The CONTEXT.md draft showed `prefetch={false}` but the actual file omits it — match the file.

Existing `vehicles` prefetch handler (lines 93-96, template for the new `firearms` handler):
    vehicles: () => {
        utils.vehicle.list.prefetch({ entityId })
        utils.entity.list.prefetch()
    },

Existing `Vehicles` SidebarMenuSubItem (template — verbatim shape used by all 6 items):
    <SidebarMenuSubItem>
        <SidebarMenuSubButton
            asChild
            isActive={
                pathname === '/vehicles'
            }
        >
            <Link
                href="/vehicles"
                onMouseEnter={
                    prefetch.vehicles
                }
            >
                <span>Vehicles</span>
            </Link>
        </SidebarMenuSubButton>
    </SidebarMenuSubItem>

tRPC client (Phase 29, registered as `firearm` in appRouter):
- utils.firearm.list.prefetch({ entityId })  — exists
- utils.entity.list.prefetch()               — exists

Route target (Phase 30):
- /firearms                                  — exists (src/app/(admin)/firearms/page.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add firearms prefetch handler</name>
  <files>src/components/app-sidebar.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx lines 45-120 (the full `prefetch` object — confirm the exact closing-comma + indentation pattern of `artwork` and `insurance` before inserting between them)
    - .planning/phases/32-sidebar-nav-alphabetization/32-PATTERNS.md "Edit 1: prefetch object insertion" section (verbatim handler shape)
    - .planning/phases/32-sidebar-nav-alphabetization/32-CONTEXT.md D-02 (the locked decision binding the exact handler body)
  </read_first>
  <action>
Insert a new `firearms` handler into the `prefetch` object in `src/components/app-sidebar.tsx`, positioned alphabetically between `artwork` (current line ~104) and `insurance` (current line ~111). Per D-02 (CONTEXT.md), the handler body MUST be:

    firearms: () => {
        utils.firearm.list.prefetch({ entityId })
        utils.entity.list.prefetch()
    },

Use the Edit tool with an unambiguous anchor — match the trailing brace + comma of the `artwork` handler followed by `insurance: () =>` and inject the new key between them. Do NOT reorder or modify any other handler. Match the indentation and trailing-comma style of the surrounding handlers exactly. Do NOT add `prefetch={false}` anywhere; that prop is not used in this file (PATTERNS.md icon-finding paragraph). This implements D-02 from CONTEXT.md (verbatim from 32-PATTERNS.md "New `firearms` handler" block).
  </action>
  <verify>
    <automated>
      bun run typecheck \
        && grep -q "firearms: () =>" src/components/app-sidebar.tsx \
        && grep -q "utils.firearm.list.prefetch" src/components/app-sidebar.tsx \
        && grep -q "utils.entity.list.prefetch" src/components/app-sidebar.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "firearms: () =>" src/components/app-sidebar.tsx` returns exactly 1
    - `grep -c "utils.firearm.list.prefetch" src/components/app-sidebar.tsx` returns exactly 1
    - Total prefetch handler keys remain mechanically correct: `grep -cE "^[[:space:]]+(properties|accounts|vehicles|personalProperty|artwork|firearms|insurance):[[:space:]]\(\)" src/components/app-sidebar.tsx` returns 7
    - `bun run typecheck` exits 0 (tRPC end-to-end types confirm `utils.firearm.list` exists)
    - No other handler was modified (diff inspection shows only an insertion, not edits to existing handlers)
  </acceptance_criteria>
  <done>The `prefetch` object contains a `firearms` key that prefetches both `firearm.list` and `entity.list`, positioned alphabetically between `artwork` and `insurance`. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Assets SidebarMenuSub block — alphabetize + insert Firearms</name>
  <files>src/components/app-sidebar.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx lines 379-485 (current Assets SidebarMenuSub block — note the 6 items in declared order: Properties → Accounts → Vehicles → Personal Property → Artwork → Insurance, and confirm none use an icon and none pass `prefetch={false}`)
    - .planning/phases/32-sidebar-nav-alphabetization/32-PATTERNS.md "Edit 2" section, especially the "Full replacement block" — copy-paste-ready replacement (lines 96-223 of PATTERNS.md)
    - .planning/phases/32-sidebar-nav-alphabetization/32-CONTEXT.md D-03 (locked final order) and D-04 (no icons; match existing pattern)
  </read_first>
  <action>
Replace the entire Assets `SidebarMenuSub` block (starts at line 379 with `<SidebarMenuSub>` and ends with its matching `</SidebarMenuSub>` around line 485 — locate by scanning forward from line 379 until the matching close tag) with the verbatim 7-item replacement block in 32-PATTERNS.md "Full replacement block" (lines 96-223).

The final order, per D-03, is:
1. Accounts          (`href="/accounts"`,          `prefetch.accounts`)
2. Artwork           (`href="/artwork"`,           `prefetch.artwork`)
3. Firearms (NEW)    (`href="/firearms"`,          `prefetch.firearms`)
4. Insurance         (`href="/insurance"`,         `prefetch.insurance`)
5. Personal Property (`href="/personal-property"`, `prefetch.personalProperty`)
6. Properties        (`href="/properties"`,        `prefetch.properties`)
7. Vehicles          (`href="/vehicles"`,          `prefetch.vehicles`)

Each item is a `SidebarMenuSubItem` wrapping a `SidebarMenuSubButton asChild isActive={pathname === '/<route>'}` wrapping a `<Link href="/<route>" onMouseEnter={prefetch.<key>}><span>Label</span></Link>`. No icons (per D-04 + PATTERNS.md icon-finding: no existing item has an icon — Firearms uses none either). No `prefetch={false}` on any `<Link>` (PATTERNS.md confirmed the file omits this — match the file, not the CONTEXT.md example).

Use a single Edit call that replaces the entire block (option `(a)` in CONTEXT.md and the explicit recommendation in PATTERNS.md). Do NOT make 5+ targeted move-edits — one atomic replacement is cleaner and avoids merge-ordering errors. Do NOT touch any other `SidebarMenuSub` in the file (Administration, Financial, Distributions, Liabilities, Activity Log, Settings, Trustees, Beneficiaries — all unchanged per D-07).
  </action>
  <verify>
    <automated>
      bun run typecheck \
        && bun run lint \
        && grep -q 'href="/firearms"' src/components/app-sidebar.tsx \
        && grep -q 'onMouseEnter={[[:space:]]*prefetch.firearms[[:space:]]*}' src/components/app-sidebar.tsx \
        && grep -q "pathname === '/firearms'" src/components/app-sidebar.tsx \
        && grep -q '<span>Firearms</span>' src/components/app-sidebar.tsx \
        && python3 -c "import re,sys; src=open('src/components/app-sidebar.tsx').read(); idx={k:src.index(f\"href=\\\"{v}\\\"\") for k,v in [('Accounts','/accounts'),('Artwork','/artwork'),('Firearms','/firearms'),('Insurance','/insurance'),('Personal Property','/personal-property'),('Properties','/properties'),('Vehicles','/vehicles')]}; order=sorted(idx,key=idx.get); expected=['Accounts','Artwork','Firearms','Insurance','Personal Property','Properties','Vehicles']; sys.exit(0 if order==expected else 1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href="/firearms"' src/components/app-sidebar.tsx` returns exactly 1
    - `grep -c "pathname === '/firearms'" src/components/app-sidebar.tsx` returns exactly 1
    - `grep -c '<span>Firearms</span>' src/components/app-sidebar.tsx` returns exactly 1
    - `grep -c '<SidebarMenuSubItem>' src/components/app-sidebar.tsx` is unchanged-plus-one (Administration, Distributions, Trustees groups untouched; only Assets gained one item)
    - The Python ordering probe in `<automated>` exits 0 — the 7 `href=` strings appear in the source in the locked alphabetical order: Accounts → Artwork → Firearms → Insurance → Personal Property → Properties → Vehicles
    - No `prefetch={false}` token introduced (this prop is not used in the file): `grep -c 'prefetch={false}' src/components/app-sidebar.tsx` returns 0
    - No lucide icon was added to any Assets sub-item: the Firearms `SidebarMenuSubItem` body is `<span>Firearms</span>` only (no `<Icon` element)
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 (no biome warnings — per global memory "lint warnings are never pre-existing")
  </acceptance_criteria>
  <done>The Assets `SidebarMenuSub` block contains exactly 7 items in the locked alphabetical order, with Firearms as the 3rd item. All checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Admin UAT — verify the 3 ROADMAP success criteria visually</name>
  <what-built>
Two surgical edits to `src/components/app-sidebar.tsx`:
1. Added `firearms` prefetch handler that calls `utils.firearm.list.prefetch({ entityId })` and `utils.entity.list.prefetch()`.
2. Reordered the Assets `SidebarMenuSub` block to alphabetical order (Accounts → Artwork → Firearms → Insurance → Personal Property → Properties → Vehicles), inserting a new Firearms entry pointing to `/firearms` with hover prefetch wired up.

Typecheck and lint pass. No tests added (D-06 — sidebar changes ship without dedicated tests, matching Phase 19 precedent).
  </what-built>
  <how-to-verify>
Start the dev server: `bun run dev` (on port 3000). Sign in as admin (`rhudsontspr@gmail.com` or local admin account). Then on any admin page (e.g. `/dashboard`):

**SC-1 — Alphabetical order (ASSET-04):**
1. In the left sidebar, locate the "Assets" group and expand it (if not already expanded).
2. Confirm the sub-items appear in this exact order, top to bottom:
   1. Accounts
   2. Artwork
   3. Firearms
   4. Insurance
   5. Personal Property
   6. Properties
   7. Vehicles
3. There should be exactly 7 items. No extras, no missing.

**SC-2 — Navigation works (ASSET-03):**
4. Click the "Firearms" sub-item.
5. Confirm the URL changes to `http://localhost:3000/firearms` and the firearms admin page renders (DataTable with KPI strip from Phase 30).

**SC-3 — Prefetch fires on hover:**
6. Navigate back to `/dashboard`.
7. Open browser DevTools → Network tab → filter to `trpc`.
8. Clear the network log.
9. Hover the cursor over the "Firearms" sidebar item (do NOT click).
10. Confirm two tRPC POST requests fire in the Network panel:
    - `firearm.list` (with `entityId` in the input)
    - `entity.list`
11. Both should return 200 OK with `trpc-batch-mode` or single-call shape.

**Additional sanity (no regressions):**
12. Hover over each of the other 6 Assets items in turn — each should still fire its existing prefetch (accounts, artwork, insurance, personal-property, properties, vehicles). None of them should error.
13. Confirm the other top-level sidebar groups (Administration, Financial, Distributions, Liabilities, Activity Log, Settings, etc.) are unchanged in structure and ordering — D-07 boundary.
  </how-to-verify>
  <resume-signal>Type "approved" to mark ASSET-03 + ASSET-04 complete and finalize Phase 32, or describe any deviation found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → sidebar Link click | Navigation only; href values are static literals — no user input flows here |
| User → sidebar Link hover | Triggers tRPC prefetch (`firearm.list`, `entity.list`); both procedures already enforce `adminProcedure` + entity-scoped filtering server-side. No new attack surface introduced by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-LNK | Tampering | `<Link href="/firearms">` in app-sidebar.tsx | mitigate | href is a string literal (no interpolation); acceptance grep `grep -q 'href="/firearms"' src/components/app-sidebar.tsx` confirms the exact target |
| T-32-PRE | Information Disclosure | `onMouseEnter={prefetch.firearms}` hover prefetch | mitigate | Prefetch calls go through `utils.firearm.list` which is already `adminProcedure` + entityId-gated (Phase 29 SC-4 sealed beneficiary-JWT rejection); acceptance grep `grep -q 'prefetch.firearms' src/components/app-sidebar.tsx` confirms wiring |
| T-32-ORD | Spoofing | Assets sub-item ordering | accept | Static JSX ordering in source — no runtime input controls the order; Python positional-grep in Task 2 `<automated>` proves the order matches D-03 |
</threat_model>

<verification>
After all 3 tasks pass their acceptance criteria, the phase is verified against the 3 ROADMAP success criteria via Task 3's admin UAT. No automated tests are added (D-06 — sidebar pure-UI changes ship without dedicated tests, matching Phase 19 precedent for artwork / personal-property / insurance sidebar additions).

Static gates that MUST pass before Task 3:
- `bun run typecheck` exits 0
- `bun run lint` exits 0 (no biome warnings)
- The 7 grep-and-ordering acceptance checks in Tasks 1 and 2

Runtime gates (Task 3 admin UAT):
- SC-1: Visual confirmation of 7-item alphabetical order in the Assets dropdown
- SC-2: Click → navigation to `/firearms`
- SC-3: DevTools Network panel shows `firearm.list` + `entity.list` tRPC requests firing on hover
</verification>

<success_criteria>
Phase 32 is complete when all three are TRUE:

1. **ASSET-04 (SC-1):** The Assets sidebar sub-items appear in the order Accounts, Artwork, Firearms, Insurance, Personal Property, Properties, Vehicles — verified visually in Task 3 step 1-3 and via the Python ordering probe in Task 2's automated check.
2. **ASSET-03 (SC-2):** Clicking the Firearms sub-item navigates to `/firearms` — verified in Task 3 step 4-5.
3. **SC-3:** Hovering the Firearms sub-item prefetches `firearm.list` + `entity.list` — verified in Task 3 step 6-11.

Both pending requirements (ASSET-03, ASSET-04) are completed by this single plan.
</success_criteria>

<output>
On Task 3 approval, create `.planning/phases/32-sidebar-nav-alphabetization/32-01-SUMMARY.md` capturing:
- Accomplishments (the two edits + verification)
- ROADMAP SC coverage table (SC-1, SC-2, SC-3 → all verified)
- Files modified: `src/components/app-sidebar.tsx` (single file, two surgical edits)
- Decisions made: D-02..D-08 honored verbatim from CONTEXT.md + PATTERNS.md
- Note for next phase: Phase 33 (beneficiary-ux-cleanup) is fully independent — no shared files
- requirements-completed: [ASSET-03, ASSET-04]
</output>
