---
phase: 30-firearms-admin-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/firearms/page.tsx
  - src/app/(admin)/firearms/loading.tsx
  - src/app/(admin)/firearms/error.tsx
  - src/app/(admin)/firearms/_components/FirearmsClient.tsx
  - src/app/(admin)/firearms/_components/FirearmTable.tsx
  - src/app/(admin)/firearms/_components/FirearmDialog.tsx
  - src/app/(admin)/firearms/_components/FirearmRowDetail.tsx
  - src/app/(admin)/firearms/_components/NfaStatusDialog.tsx
  - src/lib/asset-wizard-steps.ts
  - src/lib/constants.ts
  - src/lib/form-factory.ts
autonomous: false  # Task 8 is checkpoint:human-verify gating SC-1..SC-5; the plan must NOT auto-approve. Plan-checker blocker 2 fix.
requirements: [FIRE-06, FIRE-07]
must_haves:
  truths:
    - "Admin navigates to /firearms and sees a DataTable listing firearm records scoped to the selected entity (SC-1)"
    - "Admin can create a new firearm record via the wizard dialog; the row appears in the table on success (SC-2, FIRE-06)"
    - "Admin can edit any field of an existing firearm record; the table row updates on save (SC-3, FIRE-06)"
    - "Admin can delete a firearm record (single + bulk); the row is removed and a success toast is shown (SC-4, FIRE-06)"
    - "Admin can sort by any column, filter rows by text, and download a CSV of the current view (SC-5, FIRE-07)"
    - "When a firearm is NFA (isNfa=true), the row-detail panel shows the NFA Classification section and a 'Update Form 5 Status' button that opens NfaStatusDialog; non-NFA firearms hide both"
    - "Serial-number conflict from firearm.create surfaces as toast 'A firearm with this serial number already exists.' and keeps the dialog open"
    - "Storage location is excluded from default visible columns (PITFALLS §13); serialNumber is included; action and select columns excluded from CSV via meta.excludeFromExport"
    - "Recordkeeping-not-legal-advice disclaimer is visible in ALL 3 required surfaces: FirearmDialog NFA section, FirearmRowDetail NFA section (Section 2), and NfaStatusDialog body"
  artifacts:
    - path: "src/app/(admin)/firearms/page.tsx"
      provides: "Server Component with HydrationBoundary prefetching firearm.list + entity.list"
      contains: "trpc.firearm.list.prefetch"
    - path: "src/app/(admin)/firearms/loading.tsx"
      provides: "4-card KpiStrip skeleton + table placeholder"
      contains: "md:grid-cols-4"
    - path: "src/app/(admin)/firearms/error.tsx"
      provides: "ErrorBoundary matching /vehicles/error.tsx pattern"
      contains: "Sentry.captureException"
    - path: "src/app/(admin)/firearms/_components/FirearmsClient.tsx"
      provides: "PageHeader + KpiStrip + FirearmTable + FirearmDialog + ConfirmDialog orchestrator with entity-gated list query"
      contains: "trpc.firearm.list.useQuery"
    - path: "src/app/(admin)/firearms/_components/FirearmTable.tsx"
      provides: "DataTable with 9 visible + 21 hidden columns, bulk delete, CSV export, getRowDetail"
      contains: "exportResource=\"firearms\""
    - path: "src/app/(admin)/firearms/_components/FirearmDialog.tsx"
      provides: "3-step ResourceDialog wizard with NFA conditional section via formInstance.Subscribe; OMITS nfaTransferStatus"
      contains: "FIREARM_WIZARD_STEPS"
    - path: "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"
      provides: "3-section row-detail panel rendered via getRowDetail; consumes byId eager-load"
      contains: "trpc.firearm.byId.useQuery"
    - path: "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"
      provides: "Dedicated dialog calling trpc.firearm.setNfaTransferStatus (the ONLY path to change nfaTransferStatus)"
      contains: "setNfaTransferStatus"
    - path: "src/lib/asset-wizard-steps.ts"
      provides: "FIREARM_WIZARD_STEPS export (3 steps: identity / valuation / ownership)"
      contains: "FIREARM_WIZARD_STEPS"
    - path: "src/lib/constants.ts"
      provides: "STATUS_VARIANTS additions for NOT_FILED, FILED, APPROVED, POOR, FAIR, VERY_GOOD, EXCELLENT, NEW"
      contains: "NOT_FILED"
    - path: "src/lib/form-factory.ts"
      provides: "firearmFormDefaults() factory for the wizard form initial values"
      contains: "firearmFormDefaults"
  key_links:
    - from: "src/app/(admin)/firearms/_components/FirearmsClient.tsx"
      to: "trpc.firearm.list"
      via: "useQuery({ entityId: entityId! }, { enabled: !!entityId })"
      pattern: "trpc\\.firearm\\.list\\.useQuery"
    - from: "src/app/(admin)/firearms/_components/FirearmTable.tsx"
      to: "DataTable primitive"
      via: "exportable + exportResource + initialColumnVisibility + getRowDetail"
      pattern: "exportResource=[\"']firearms[\"']"
    - from: "src/app/(admin)/firearms/_components/FirearmDialog.tsx"
      to: "FIREARM_WIZARD_STEPS"
      via: "import from @/lib/asset-wizard-steps"
      pattern: "FIREARM_WIZARD_STEPS"
    - from: "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"
      to: "trpc.firearm.setNfaTransferStatus"
      via: "useMutation onSuccess → utils.firearm.list.invalidate()"
      pattern: "setNfaTransferStatus"
    - from: "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"
      to: "trpc.firearm.byId"
      via: "useQuery({ id, entityId })"
      pattern: "trpc\\.firearm\\.byId"
---

<objective>
Ship the `/firearms` admin page — a Server-Component-prefetched DataTable with 30+ columns (9 visible), KpiStrip (4 cards), 3-step wizard FirearmDialog, NFA-conditional row-detail panel, and a dedicated NfaStatusDialog for the `setNfaTransferStatus` CQS mutation.

Purpose: Closes FIRE-06 (admin can view/edit/delete firearm records from a dedicated `/firearms` admin page) and FIRE-07 (sort + filter + CSV-export). Wires the Phase 29 firearmRouter (6 procedures) into a user-facing surface that follows the vehicle/accounts admin-page template exactly.

Output: 8 new files + 3 modified lib files. Page is reachable at `/firearms`. Sidebar nav linkage is Phase 32 work — out of scope here.

**Why 1 plan with 7 auto tasks (plan-checker scope heuristic override):** Every existing asset admin page in this codebase (vehicles, accounts, properties, personal-property, artwork, insurance — 7 precedents) ships as a single plan in a single phase, regardless of task count. Each Phase-30 task is a template copy from the PATTERNS.md analog with firearm-specific identifier substitutions — no novel architectural decisions remain. Splitting at the 5-task heuristic would create two plans (`lib + shell` / `components + UAT`) with artificial commit boundaries that don't reflect a real shipping unit. The plan-checker's threshold is advisory; the codebase precedent governs. If quality degrades during execution, the right response is a follow-up phase, not a pre-emptive split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-firearms-admin-page/30-UI-SPEC.md
@.planning/phases/30-firearms-admin-page/30-PATTERNS.md
@.planning/phases/28-firearm-schema-and-migration/28-01-SUMMARY.md
@.planning/phases/29-firearm-trpc-router/29-01-SUMMARY.md
@.planning/phases/29-firearm-trpc-router/29-CONTEXT.md
@.planning/research/PITFALLS.md
@src/app/(admin)/vehicles/page.tsx
@src/app/(admin)/vehicles/loading.tsx
@src/app/(admin)/vehicles/error.tsx
@src/app/(admin)/vehicles/_components/VehiclesClient.tsx
@src/app/(admin)/vehicles/_components/VehicleTable.tsx
@src/app/(admin)/vehicles/_components/VehicleDialog.tsx
@src/app/(admin)/accounts/_components/AccountsClient.tsx
@src/app/(admin)/contacts/_components/ContactDialog.tsx
@src/app/(admin)/hems-queue/_components/HemsQueueClient.tsx
@src/app/(admin)/liabilities/_components/PaymentImpactPreview.tsx
@src/app/(admin)/personal-property/_components/PersonalPropertyDialog.tsx
@src/app/(admin)/trustees/_components/TrusteeTable.tsx
@src/lib/asset-wizard-steps.ts
@src/lib/constants.ts
@src/lib/form-factory.ts
@src/server/trpc/routers/firearm.ts
@db/schema.ts
@db/validation.ts

<interfaces>
<!-- Key types and contracts the executor needs — extracted from Phase 28/29 outputs. No codebase exploration needed. -->

From `db/schema.ts` (Phase 28) — the `firearm` table row shape:
- `id: number`, `entityId: number`, `name: string`, `description: string | null`
- Identity: `make: string`, `model: string`, `serialNumber: string`, `firearmType: enum (PISTOL|REVOLVER|RIFLE|SHOTGUN|SUPPRESSOR|SBR|SBS|MACHINE_GUN|AOW|DESTRUCTIVE_DEVICE|OTHER)`, `caliber: string | null`, `barrelLength: string | null`, `action: string | null`
- NFA: `isNfa: boolean`, `nfaClass: enum | null (SUPPRESSOR|SBR|SBS|MACHINE_GUN|AOW|DESTRUCTIVE_DEVICE)`, `atfFormType: enum | null (FORM_1|FORM_4|FORM_5)`, `nfaRegistered: boolean | null`, `atfControlNumber: string | null`, `taxStampDate: string | null`, `nfrtrSerial: string | null`, `nfaTransferStatus: enum (NOT_FILED|FILED|APPROVED)`
- Valuation: `condition: enum (POOR|FAIR|GOOD|VERY_GOOD|EXCELLENT|NEW)`, `acquisitionDate: string | null`, `acquisitionCost: string`, `dodValue: string`, `dodValueDate: string | null`, `dodValueType: string`
- Ownership: `status: enum (ACTIVE|SOLD|TRANSFERRED|DISPOSED)`, `transferStatus: enum (PENDING|IN_PROGRESS|COMPLETE)`, `insured: boolean`, `location: string | null`, `notes: string | null`
- Metadata: `createdAt: Date`, `updatedAt: Date`

From `src/server/trpc/routers/firearm.ts` (Phase 29) — the 6 procedures available:
- `firearm.list({ entityId }) → Firearm[]`
- `firearm.byId({ id, entityId }) → Firearm & { entity, valuations[], documents[] }` (throws NOT_FOUND on cross-entity / missing)
- `firearm.create({ entityId, data }) → Firearm` (CONFLICT on duplicate serialNumber)
- `firearm.update({ id, entityId, data }) → Firearm` (NOT_FOUND on cross-entity; data OMITS `nfaTransferStatus` per D-03)
- `firearm.delete({ id, entityId }) → { success: true }`
- `firearm.setNfaTransferStatus({ id, entityId, status, taxStampDate?, atfControlNumber? }) → Firearm` (BAD_REQUEST when !isNfa)

From `src/lib/asset-wizard-steps.ts` — existing helpers to reuse (do NOT re-declare):
- `nonEmpty` (line 44), `optionalMoney` (line 58), `WizardStep<T>` type
- Reference shape: `VEHICLE_WIZARD_STEPS` at lines 67–115 (3 steps: type-name / valuation / ownership)

From `src/lib/form-factory.ts` — existing helpers to reuse:
- `createFormDefaults<T>(defaults: T): () => T` — see `vehicleFormDefaults` (lines 61–80) and `personalPropertyFormDefaults` (lines 152–166 — provides the `boolean default` precedent for `isNfa` / `insured`)

From `src/lib/constants.ts` — STATUS_VARIANTS shape:
- `Record<string, 'default' | 'secondary' | 'outline' | 'destructive'>`
- Existing keys include `ACTIVE`, `PENDING`, `IN_PROGRESS`, `COMPLETE`, `SOLD`, `TRANSFERRED`, `DISPOSED`
- TRANSFER_STATUS export = SelectOption[] for status transitions

From `db/validation.ts` (Phase 28/29) — Zod schemas the dialog payloads must match:
- `insertFirearmSchema` — for create
- `updateFirearmSchema` — for generic update (omits `nfaTransferStatus`)
- `insertFirearmSchemaBase` — unrefined base (Phase 29 exposed this; used by router only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FIREARM_WIZARD_STEPS, firearmFormDefaults, and STATUS_VARIANTS additions</name>
  <files>src/lib/asset-wizard-steps.ts, src/lib/form-factory.ts, src/lib/constants.ts</files>
  <read_first>
    - src/lib/asset-wizard-steps.ts (study `VEHICLE_WIZARD_STEPS` at lines 67–115 — the 3-step shape (type-name/valuation/ownership), the `fields[]` array, the inline `schema: z.object({ ... })` gate; study `PERSONAL_PROPERTY_WIZARD_STEPS` at lines 284–319 for the boolean-field-in-step-1 precedent; confirm `nonEmpty` (line 44) and `optionalMoney` (line 58) are exported helpers)
    - src/lib/form-factory.ts (study `vehicleFormDefaults` at lines 61–80 and `personalPropertyFormDefaults` at lines 152–166 — the `insured: false` boolean default is the analog for `isNfa: false`; study `createFormDefaults` signature)
    - src/lib/constants.ts (study `STATUS_VARIANTS` record at lines 17–49 — confirm current keys; APPROVED is not yet present)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §Color (badge-variant table for NOT_FILED/FILED/APPROVED/POOR/FAIR/GOOD/VERY_GOOD/EXCELLENT/NEW) and §FirearmDialog (step field lists + gate schemas)
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §asset-wizard-steps.ts and §form-factory.ts and §constants.ts
    - db/schema.ts firearm pgTable definition (enum values + nullability for each field — drives default-value choices in firearmFormDefaults)
  </read_first>
  <action>
    Add `firearmFormDefaults` to src/lib/form-factory.ts (between `vehicleFormDefaults` and the next asset factory, alphabetical) using `createFormDefaults({ ... })`. Default-value choices per 30-PATTERNS.md §form-factory.ts: `firearmType: 'PISTOL'` (first enum value), `condition: 'GOOD'`, `status: 'ACTIVE'`, `transferStatus: 'PENDING'`, `isNfa: false`, `insured: false`, all date/optional fields `null` typed `as string | null`, all required text fields `''`. Include every column from the firearm pgTable EXCEPT `id`, `entityId`, `createdAt`, `updatedAt`, `nfaTransferStatus` (last omitted because D-03 forbids changing it via the dialog — it is set by NfaStatusDialog only).

    Add `FIREARM_WIZARD_STEPS` export to src/lib/asset-wizard-steps.ts (between `VEHICLE_WIZARD_STEPS` and `BANK_ACCOUNT_WIZARD_STEPS`, or at end if alphabetical insertion is awkward). Add the import `type FirearmForm = ReturnType<typeof firearmFormDefaults>` alongside existing form-type aliases (~line 38–42). Step shape per UI-SPEC §FirearmDialog:
    - Step `identity`: label `'Identity'`; fields list per UI-SPEC §Step 1 (name, description, make, model, firearmType, serialNumber, caliber, barrelLength, action, isNfa, nfaClass, atfFormType, nfaRegistered, atfControlNumber, taxStampDate, nfrtrSerial); gate `schema: z.object({ name: nonEmpty, make: nonEmpty, model: nonEmpty, serialNumber: nonEmpty, firearmType: nonEmpty })`
    - Step `valuation`: label `'Valuation'`; fields per UI-SPEC §Step 2 (condition, acquisitionDate, acquisitionCost, dodValue, dodValueDate, dodValueType); gate `schema: z.object({ acquisitionCost: optionalMoney, dodValue: optionalMoney })`
    - Step `ownership`: label `'Ownership'`; fields per UI-SPEC §Step 3 (status, transferStatus, insured, location, notes); gate `schema: z.object({ status: nonEmpty, transferStatus: nonEmpty })`

    Add to src/lib/constants.ts inside the existing `STATUS_VARIANTS` record (per UI-SPEC §Color badge table): `NOT_FILED: 'secondary'`, `FILED: 'outline'`, `APPROVED: 'default'`, `POOR: 'destructive'`, `FAIR: 'outline'`, `GOOD: 'secondary'`, `VERY_GOOD: 'outline'`, `EXCELLENT: 'default'`, `NEW: 'default'`. Do not duplicate keys already present — if any of these collide with existing entries, log the collision and ask before overwriting. NFA transfer status labels constant (`NFA_TRANSFER_STATUS_LABELS: Record<NfaTransferStatus, string>` with NOT_FILED→'Not Filed', FILED→'Filed — Awaiting ATF', APPROVED→'Approved') goes here OR in a new `src/lib/firearm-constants.ts` (per UI-SPEC §New Label Maps line "FirearmTable.tsx or src/lib/firearm-constants.ts" — the executor MAY colocate these in FirearmTable.tsx; if so, skip the constants.ts addition for labels and only add the STATUS_VARIANTS entries).
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | tee /tmp/30-01-typecheck.log; grep -q "error TS" /tmp/30-01-typecheck.log && echo "FAIL: typecheck errors" && exit 1 || echo "OK"</automated>
    <automated>grep -q "FIREARM_WIZARD_STEPS" src/lib/asset-wizard-steps.ts && grep -q "firearmFormDefaults" src/lib/form-factory.ts && grep -E "NOT_FILED.*secondary|'secondary'.*NOT_FILED" src/lib/constants.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run typecheck` passes with 0 errors
    - `src/lib/asset-wizard-steps.ts` exports `FIREARM_WIZARD_STEPS` as a `WizardStep<FirearmForm>[]` with exactly 3 steps whose ids are `'identity'`, `'valuation'`, `'ownership'` (in that order)
    - `src/lib/form-factory.ts` exports `firearmFormDefaults` (a function from `createFormDefaults`), and its return value includes ALL of: `name`, `description`, `make`, `model`, `firearmType`, `serialNumber`, `caliber`, `barrelLength`, `action`, `isNfa`, `nfaClass`, `atfFormType`, `nfaRegistered`, `atfControlNumber`, `taxStampDate`, `nfrtrSerial`, `condition`, `acquisitionDate`, `acquisitionCost`, `dodValue`, `dodValueDate`, `dodValueType`, `status`, `transferStatus`, `insured`, `location`, `notes`
    - `firearmFormDefaults` MUST NOT include `nfaTransferStatus` (D-03 binding — verify with `grep -c "nfaTransferStatus" src/lib/form-factory.ts` returns 0)
    - `STATUS_VARIANTS` in `src/lib/constants.ts` contains keys `NOT_FILED`, `FILED`, `APPROVED`, `POOR`, `FAIR`, `GOOD`, `VERY_GOOD`, `EXCELLENT`, `NEW`
    - `bun run lint` passes (no new biome warnings/errors)
  </acceptance_criteria>
  <done>3 lib files updated with firearm-specific exports; typecheck + lint green; FIREARM_WIZARD_STEPS exists with 3 steps; firearmFormDefaults exists without nfaTransferStatus; STATUS_VARIANTS has 9 new keys.</done>
</task>

<task type="auto">
  <name>Task 2: Build /firearms route entry, loading skeleton, and error boundary</name>
  <files>src/app/(admin)/firearms/page.tsx, src/app/(admin)/firearms/loading.tsx, src/app/(admin)/firearms/error.tsx</files>
  <read_first>
    - src/app/(admin)/vehicles/page.tsx (lines 1–16 — the HydrationBoundary + createTRPCHelpers + Promise.all prefetch pattern; this is the verbatim template)
    - src/app/(admin)/vehicles/loading.tsx (lines 1–18 — Skeleton grid; note 30-PATTERNS.md instructs grid-cols-3 → grid-cols-4 for the 4-card firearm KpiStrip)
    - src/app/(admin)/vehicles/error.tsx (lines 1–49 — AdminError component with Sentry.captureException + Card + AlertTriangle + Try Again button; copy verbatim, no substitutions per 30-PATTERNS.md)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §Page Layout Shell and §Interaction Patterns
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §page.tsx, §loading.tsx, §error.tsx
  </read_first>
  <action>
    Create the `src/app/(admin)/firearms/` directory and add 3 route files:

    `page.tsx` — Server Component. Imports `dehydrate`, `HydrationBoundary` from `@tanstack/react-query`, `createTRPCHelpers` from `@/lib/trpc-server`, and `FirearmsClient` from `./_components/FirearmsClient`. Exports default `async function FirearmsPage()`. Inside: `const helpers = await createTRPCHelpers()`, then `await Promise.all([helpers.firearm.list.prefetch({ entityId: 1 }), helpers.entity.list.prefetch()])`. Return `<HydrationBoundary state={dehydrate(helpers.queryClient)}><FirearmsClient /></HydrationBoundary>`. Mirrors src/app/(admin)/vehicles/page.tsx 1:1 with `vehicle.list` → `firearm.list`, `VehiclesClient` → `FirearmsClient`.

    `loading.tsx` — Default export `function Loading()`. Verbatim copy of vehicles/loading.tsx EXCEPT change `md:grid-cols-3` to `md:grid-cols-4` and add a 4th `<Skeleton className="h-24 rounded-lg" />` inside the grid (4 cards total — matches the 4-item KpiStrip for firearms).

    `error.tsx` — Verbatim copy of vehicles/error.tsx. Component name stays `AdminError` (generic per 30-PATTERNS.md), no substitutions. `'use client'` directive, `Sentry.captureException` in `useEffect`, Card + CardHeader + CardContent + CardTitle, `AlertTriangle` icon, "Try again" Button calling `reset()`.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/page.tsx" && test -f "src/app/(admin)/firearms/loading.tsx" && test -f "src/app/(admin)/firearms/error.tsx"</automated>
    <automated>grep -q "HydrationBoundary" "src/app/(admin)/firearms/page.tsx" && grep -q "firearm.list.prefetch" "src/app/(admin)/firearms/page.tsx"</automated>
    <automated>grep -q "md:grid-cols-4" "src/app/(admin)/firearms/loading.tsx"</automated>
    <automated>grep -q "Sentry.captureException" "src/app/(admin)/firearms/error.tsx" && grep -q "AlertTriangle" "src/app/(admin)/firearms/error.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist under `src/app/(admin)/firearms/`
    - `page.tsx` contains `HydrationBoundary` AND `firearm.list.prefetch` AND `entity.list.prefetch`
    - `page.tsx` references `FirearmsClient` (not VehiclesClient)
    - `loading.tsx` uses `md:grid-cols-4` (4 KPI cards, not 3)
    - `error.tsx` contains `Sentry.captureException(error)` inside `useEffect` and an `AlertTriangle` icon import from lucide-react
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>Route entry (page.tsx), skeleton (loading.tsx), and ErrorBoundary (error.tsx) exist in `/firearms` route group; build typechecks; loading skeleton matches the 4-card KpiStrip layout.</done>
</task>

<task type="auto">
  <name>Task 3: Build FirearmDialog (3-step wizard with NFA conditional section, no nfaTransferStatus field)</name>
  <files>src/app/(admin)/firearms/_components/FirearmDialog.tsx</files>
  <read_first>
    - src/app/(admin)/vehicles/_components/VehicleDialog.tsx (lines 1–631 — full wizard analog: ResourceDialog shell at 49–63, WizardStepGroup wrapping pattern at 66–344, NameDescriptionFields usage, formInstance.Field pattern with Select / Input / Textarea, error display via getFieldError)
    - src/app/(admin)/contacts/_components/ContactDialog.tsx (lines 120–179 — `formInstance.Subscribe` null-return conditional rendering — this is the EXACT pattern for the NFA conditional section in Step 1)
    - src/app/(admin)/personal-property/_components/PersonalPropertyDialog.tsx (lines 418–440 — Switch field pattern inside a bordered row with Label + helper text — the analog for the `isNfa` and `nfaRegistered` toggles)
    - src/app/(admin)/liabilities/_components/LiabilityDialog.tsx (line 153 — `formInstance.Subscribe<string>` typed selector usage — confirms the boolean variant for `isNfa`)
    - src/lib/asset-wizard-steps.ts (the now-existing `FIREARM_WIZARD_STEPS` export from Task 1)
    - src/lib/form-factory.ts (the now-existing `firearmFormDefaults` from Task 1)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §FirearmDialog (binding field/grid layout per step), §Copywriting Contract (verbatim labels — "Add Firearm" / "Edit Firearm" / "Create Firearm" / "Save Changes")
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §FirearmDialog.tsx
    - .planning/research/PITFALLS.md §1 (recordkeeping-not-legal-advice boundary)
  </read_first>
  <action>
    Create `src/app/(admin)/firearms/_components/FirearmDialog.tsx`. Mirror VehicleDialog structure exactly:

    Top: `'use client'`. Imports per 30-PATTERNS.md §FirearmDialog imports block — include `NameDescriptionFields`, `ResourceDialog`, `Input`, `Label`, `Select` family, `Switch`, `Textarea`, `WizardStepGroup`, `ResourceWizardProps`, `UseResourceFormReturn`, `FIREARM_WIZARD_STEPS`, `DOD_VALUE_TYPES`, `TRANSFER_STATUS`, `getFieldError`, plus the firearm label maps `FIREARM_TYPE_LABELS`, `NFA_CLASS_LABELS`, `ATF_FORM_TYPE_LABELS`, `CONDITION_LABELS` (colocate these in this file per UI-SPEC §New Label Maps OR import from a new `src/lib/firearm-constants.ts` if Task 1 placed them there).

    Component signature: `export function FirearmDialog({ isOpen, onOpenChange, isEditing, formInstance, wizard, currentStep, onSubmit, isSubmitting }: ResourceWizardProps<FirearmForm>)`. Use ResourceDialog as the shell — exact prop set per 30-PATTERNS.md §FirearmDialog ResourceDialog block: `title={isEditing ? 'Edit Firearm' : 'Add Firearm'}`, `submitLabel={isEditing ? 'Save Changes' : 'Create Firearm'}`, `steps={FIREARM_WIZARD_STEPS}`, plus the wizard navigation props.

    **Step 1 — Identity** (`<WizardStepGroup step={0} currentStep={currentStep}>`):
    - `<NameDescriptionFields formInstance={formInstance} idPrefix="firearm" />`
    - 3-col grid: `make` Input, `model` Input, `firearmType` Select wired to `FIREARM_TYPE_LABELS`
    - 2-col grid: `serialNumber` Input (required), `caliber` Input
    - 2-col grid: `barrelLength` Input type="number" label "Barrel Length (inches)", `action` Input label "Action Type" placeholder "e.g., bolt-action, semi-auto"
    - isNfa Switch row using PersonalPropertyDialog 418–440 pattern: bordered row with Label "NFA Item" + helper text "Check if this is a Title II / NFA firearm" + Switch
    - NFA conditional section using `formInstance.Subscribe<boolean>` selector on `state.values.isNfa` (contacts/ContactDialog.tsx 120–179 pattern): when `!isNfa` return `null`; when true render `<div className="space-y-4 mt-4">` containing:
      - `<h4 className="text-sm font-medium">NFA Classification</h4>`
      - 3-col grid: `nfaClass` Select wired to NFA_CLASS_LABELS, `atfFormType` Select wired to ATF_FORM_TYPE_LABELS, `nfaRegistered` Switch label "NFRTR Registered"
      - 2-col grid: `atfControlNumber` Input, `taxStampDate` Input type="date"
      - `nfrtrSerial` Input full-width label "NFRTR Serial Number" placeholder "If different from serial number above"
      - Disclaimer: `<p className="text-xs text-muted-foreground mt-2">NFA fields are for recordkeeping only. ATF approval is required before transferring NFA items to a beneficiary.</p>` (verbatim per UI-SPEC §Copywriting Contract)

    **Step 2 — Valuation** (`<WizardStepGroup step={1} currentStep={currentStep}>`):
    - `condition` Select full-width label "NRA Condition Grade" wired to CONDITION_LABELS
    - 2-col grid: `acquisitionDate` Input type="date", `acquisitionCost` Input placeholder "$"
    - 3-col grid: `dodValue` Input placeholder "$ — Blue Book / appraisal", `dodValueDate` Input type="date", `dodValueType` Select wired to `DOD_VALUE_TYPES`

    **Step 3 — Ownership** (`<WizardStepGroup step={2} currentStep={currentStep}>`):
    - 3-col grid: `status` Select (ACTIVE/SOLD/TRANSFERRED/DISPOSED), `transferStatus` Select wired to `TRANSFER_STATUS`, `insured` Switch label "Insured"
    - `location` Input full-width label "Storage Location" placeholder "Safe, bank vault, FFL storage..."
    - `notes` Textarea rows={3}

    **CRITICAL omission:** This dialog MUST NOT render any field bound to `nfaTransferStatus` — that field's only mutation path is NfaStatusDialog (D-03 binding from Phase 29 CONTEXT.md). The wizard's three step fields lists, the FIREARM_WIZARD_STEPS gate schemas, and firearmFormDefaults all already exclude it; this task must not reintroduce it.

    Wrap each field with `<formInstance.Field name="...">{(field) => (...)}</formInstance.Field>` and surface errors via `getFieldError(field)` displayed in a `<p className="text-xs text-destructive">{error}</p>` below the input.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/_components/FirearmDialog.tsx"</automated>
    <automated>grep -q "FIREARM_WIZARD_STEPS" "src/app/(admin)/firearms/_components/FirearmDialog.tsx"</automated>
    <automated>grep -q "Create Firearm" "src/app/(admin)/firearms/_components/FirearmDialog.tsx" && grep -q "Save Changes" "src/app/(admin)/firearms/_components/FirearmDialog.tsx" && grep -q "Add Firearm" "src/app/(admin)/firearms/_components/FirearmDialog.tsx" && grep -q "Edit Firearm" "src/app/(admin)/firearms/_components/FirearmDialog.tsx"</automated>
    <automated>grep -q "formInstance.Subscribe" "src/app/(admin)/firearms/_components/FirearmDialog.tsx"</automated>
    <automated>grep -c "nfaTransferStatus" "src/app/(admin)/firearms/_components/FirearmDialog.tsx" | grep -qx "0"</automated>
    <automated>grep -q "NFA fields are for recordkeeping only" "src/app/(admin)/firearms/_components/FirearmDialog.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(admin)/firearms/_components/FirearmDialog.tsx`
    - Contains the 4 copy strings verbatim: `"Add Firearm"`, `"Edit Firearm"`, `"Create Firearm"`, `"Save Changes"`
    - Contains `formInstance.Subscribe` for the NFA conditional section
    - Contains the verbatim disclaimer `"NFA fields are for recordkeeping only. ATF approval is required before transferring NFA items to a beneficiary."`
    - Contains 0 references to `nfaTransferStatus` (`grep -c nfaTransferStatus` returns 0)
    - Imports `FIREARM_WIZARD_STEPS` from `@/lib/asset-wizard-steps`
    - `bun run typecheck` passes
    - All 3 step labels match UI-SPEC: "Identity", "Valuation", "Ownership"
  </acceptance_criteria>
  <done>FirearmDialog ships a working 3-step ResourceDialog wizard with the NFA conditional section gated by formInstance.Subscribe and the recordkeeping disclaimer present; nfaTransferStatus is absent (D-03 honored).</done>
</task>

<task type="auto">
  <name>Task 4: Build NfaStatusDialog (dedicated dialog for setNfaTransferStatus mutation)</name>
  <files>src/app/(admin)/firearms/_components/NfaStatusDialog.tsx</files>
  <read_first>
    - src/app/(admin)/hems-queue/_components/HemsQueueClient.tsx (lines 102–137 for mutation+open/close state pattern; lines 426–694 for the bare shadcn Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter shell — this is the closest analog because NfaStatusDialog is a small, focused, non-wizard dialog driven by local useState, not a TanStack Form)
    - src/server/trpc/routers/firearm.ts (the `setNfaTransferStatus` procedure shape: input `{ id, entityId, status, taxStampDate?, atfControlNumber? }`; throws BAD_REQUEST when !isNfa)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §"File Form 5" Button — NFA Status Transition and §Copywriting Contract (verbatim copy: "Update ATF Form 5 Status" title, "Save Status" submit, "NFA transfer status updated." success toast, "This app does not file ATF forms. Record the status after filing through your FFL or attorney." disclaimer, status labels "Not Filed" / "Filed — Awaiting ATF" / "Approved")
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §NfaStatusDialog.tsx
    - .planning/research/PITFALLS.md §1 (recordkeeping-not-legal-advice boundary — drives the disclaimer copy)
  </read_first>
  <action>
    Create `src/app/(admin)/firearms/_components/NfaStatusDialog.tsx`. Use bare shadcn Dialog primitives (NOT ResourceDialog — this is a small focused state-transition affordance per 30-PATTERNS.md).

    Top: `'use client'`. Imports: `useState` from react, `toast` from sonner, `Button`, Dialog family (`Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`), `Input`, `Label`, Select family, `trpc` from `@/lib/trpc`, `Loader2` from lucide-react, optionally `NFA_TRANSFER_STATUS_LABELS` from wherever Task 1 placed it.

    Component signature: `export function NfaStatusDialog({ firearm, open, onOpenChange }: { firearm: { id: number; entityId: number; nfaTransferStatus: 'NOT_FILED' | 'FILED' | 'APPROVED' }; open: boolean; onOpenChange: (open: boolean) => void })`.

    Local state (per HemsQueueClient 126–137 pattern): `const [status, setStatus] = useState<'NOT_FILED'|'FILED'|'APPROVED'>(firearm.nfaTransferStatus)`, `const [taxStampDate, setTaxStampDate] = useState('')`, `const [atfControlNumber, setAtfControlNumber] = useState('')`. Use `useEffect(() => { if (open) { setStatus(firearm.nfaTransferStatus); setTaxStampDate(''); setAtfControlNumber(''); } }, [open, firearm.nfaTransferStatus])` to reset on open.

    Mutation wiring (per HemsQueueClient 102–118):
    `const utils = trpc.useUtils()`
    `const setNfaStatusMutation = trpc.firearm.setNfaTransferStatus.useMutation({ onSuccess: () => { utils.firearm.list.invalidate(); utils.firearm.byId.invalidate({ id: firearm.id, entityId: firearm.entityId }); toast.success('NFA transfer status updated.'); onOpenChange(false); }, onError: (err) => toast.error(err.message) })`.

    Submit handler: builds payload `{ id: firearm.id, entityId: firearm.entityId, status, taxStampDate: status === 'APPROVED' && taxStampDate ? taxStampDate : undefined, atfControlNumber: (status === 'FILED' || status === 'APPROVED') && atfControlNumber ? atfControlNumber : undefined }`, then `setNfaStatusMutation.mutate(payload)`.

    Body JSX:
    - `<DialogTitle>Update ATF Form 5 Status</DialogTitle>` (verbatim per UI-SPEC)
    - `<Label htmlFor="nfa-status">Status</Label>` + `<Select value={status} onValueChange={(v) => setStatus(v as ...)}>` with 3 `SelectItem`s using labels "Not Filed" / "Filed — Awaiting ATF" / "Approved"
    - Conditional: when `status === 'FILED' || status === 'APPROVED'` show `atfControlNumber` Input with Label "ATF Control Number"
    - Conditional: when `status === 'APPROVED'` show `taxStampDate` Input type="date" with Label "Tax Stamp Date"
    - Disclaimer paragraph (always visible, below conditional inputs): `<p className="text-xs text-muted-foreground">This app does not file ATF forms. Record the status after filing through your FFL or attorney.</p>` (verbatim)
    - DialogFooter: Cancel `<Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button>` + Submit `<Button onClick={handleSubmit} disabled={setNfaStatusMutation.isPending}>{setNfaStatusMutation.isPending && <Loader2 className="h-4 w-4 animate-spin mr-2" />}Save Status</Button>`
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>grep -q "setNfaTransferStatus" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>grep -q "Update ATF Form 5 Status" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx" && grep -q "Save Status" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>grep -q "NFA transfer status updated" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>grep -q "This app does not file ATF forms" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>grep -q "utils.firearm.list.invalidate" "src/app/(admin)/firearms/_components/NfaStatusDialog.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(admin)/firearms/_components/NfaStatusDialog.tsx`
    - Calls `trpc.firearm.setNfaTransferStatus.useMutation` (it is the ONLY component allowed to do so)
    - Contains all 4 verbatim copy strings: `"Update ATF Form 5 Status"`, `"Save Status"`, `"NFA transfer status updated."`, `"This app does not file ATF forms. Record the status after filing through your FFL or attorney."`
    - On mutation success, invalidates `utils.firearm.list` AND `utils.firearm.byId`, shows success toast, closes the dialog
    - Conditional `atfControlNumber` input shown when status is FILED or APPROVED; conditional `taxStampDate` input shown when status is APPROVED
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>NfaStatusDialog is the single UI path to mutate nfaTransferStatus; conditional inputs match UI-SPEC; recordkeeping disclaimer present; success/error toasts wired; query invalidation runs on success.</done>
</task>

<task type="auto">
  <name>Task 5: Build FirearmRowDetail (3-section row-expand panel + byId eager-load + NFA section + Update Form 5 Status trigger)</name>
  <files>src/app/(admin)/firearms/_components/FirearmRowDetail.tsx</files>
  <read_first>
    - src/app/(admin)/accounts/_components/AccountsClient.tsx (lines 348–387 — inline getRowDetail container with dl/dt/dd metadata-list pattern; this is the closest container analog though FirearmRowDetail is extracted as a standalone file)
    - src/app/(admin)/liabilities/_components/PaymentImpactPreview.tsx (lines 95–100 — `bg-warning/10 border border-warning/30 rounded-lg p-3 text-sm text-warning` warning-alert block — analog for the unregistered-NFA alert)
    - src/app/(admin)/hems/_components/HistoryTable.tsx (lines 88–101 — milestone badge class pattern; the NFA section header pill uses the same `--milestone` token utilities)
    - src/server/trpc/routers/firearm.ts (the `byId` procedure: eager-loads entity/valuations/documents per Phase 29 D-05)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §Row-Detail Panel (3 sections: Physical / NFA conditional / Related Records), §"File Form 5" Button, §Copywriting Contract (verbatim alert + disclaimer strings)
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §FirearmRowDetail.tsx
    - .planning/research/PITFALLS.md §1 + §13 (recordkeeping disclaimer + location-sensitivity context — location IS shown here but NOT in CSV)
    - The NfaStatusDialog file written in Task 4 (this component renders the trigger button + manages its open state)
  </read_first>
  <action>
    Create `src/app/(admin)/firearms/_components/FirearmRowDetail.tsx`. Default `'use client'`.

    Imports: `useState`, `Badge`, `Button`, `trpc`, `STATUS_VARIANTS`, `NFA_TRANSFER_STATUS_LABELS` (and the other firearm label maps from Task 1's placement), `NfaStatusDialog` from `./NfaStatusDialog`, type `Firearm` from `@/db/schema`, `formatCurrency` from `@/lib/money`, `cn` from `@/lib/utils`.

    Component signature: `export function FirearmRowDetail({ firearm }: { firearm: Firearm })`. Inside:

    `const [nfaDialogOpen, setNfaDialogOpen] = useState(false)`

    `const { data: detail, isLoading } = trpc.firearm.byId.useQuery({ id: firearm.id, entityId: firearm.entityId })`. While loading, render a small `<p className="text-sm text-muted-foreground">Loading details…</p>` placeholder.

    Container: `<div className="grid grid-cols-1 md:grid-cols-2 gap-6 p-4 bg-muted/30 rounded-md border border-border">` containing 3 sections:

    **Section 1 — Physical Details** (always visible): heading `<h4 className="text-sm font-medium">Physical Details</h4>`, then a `<dl className="grid grid-cols-2 gap-x-4 gap-y-1 text-sm">` with dt/dd pairs for:
    - "Barrel Length" → `firearm.barrelLength ?? '—'`
    - "Action Type" → `firearm.action ?? '—'`
    - "Caliber" → `firearm.caliber ?? '—'`
    - "Condition" → `firearm.condition`
    - "Storage Location" → `firearm.location ?? '—'` (NOTE: location IS shown in detail per UI-SPEC; only excluded from CSV/default-table)
    - "Insured" → `firearm.insured ? 'Yes' : 'No'`
    - "Acquisition Date" → `firearm.acquisitionDate ?? '—'`
    - "Acquisition Cost" → `formatCurrency(firearm.acquisitionCost)`
    - "Notes" → `firearm.notes ?? '—'` (full-width row if needed)

    **Section 2 — NFA Classification** (conditional on `firearm.isNfa === true`):
    - Section header row: `<div className="flex items-center gap-2"><h4 className="text-sm font-medium">NFA Classification</h4><Badge className="text-milestone-foreground bg-milestone/15 border-milestone/30 text-[10px] px-1 py-0 font-medium">NFA</Badge></div>`
    - dl with: nfaClass, atfFormType, atfControlNumber, taxStampDate, nfrtrSerial, nfaRegistered (Yes/No)
    - Transfer status row: `<Badge variant={STATUS_VARIANTS[firearm.nfaTransferStatus] || 'secondary'}>{NFA_TRANSFER_STATUS_LABELS[firearm.nfaTransferStatus]}</Badge>` next to `<Button variant="outline" size="sm" onClick={() => setNfaDialogOpen(true)}>Update Form 5 Status</Button>`
    - Conditional unregistered alert (when `firearm.nfaRegistered === false`): `<div className="rounded-lg bg-warning/10 border border-warning/30 p-3 text-sm text-warning">This NFA item is not registered in the NFRTR. Unregistered NFA items are contraband. Do not attempt to transfer. Consult an attorney immediately.</div>` (verbatim per UI-SPEC §Copywriting Contract)
    - Persistent disclaimer (always visible in this section): `<p className="text-xs text-muted-foreground mt-3">This section is for recordkeeping only. Consult a licensed firearms attorney or FFL dealer for ATF transfer requirements.</p>`
    - Render the NfaStatusDialog: `<NfaStatusDialog firearm={firearm} open={nfaDialogOpen} onOpenChange={setNfaDialogOpen} />`

    **Section 3 — Related Records** (always visible, full-width spanning both columns via `md:col-span-2`):
    - Valuations sub-block: heading "Valuation History"; iterate `detail?.valuations ?? []` rendering `date / amount / valueType` per row; empty state `<p className="text-sm text-muted-foreground">No valuation history.</p>`
    - Documents sub-block: heading "Documents"; iterate `detail?.documents ?? []` rendering `name / documentType / createdAt` per row; empty state `<p className="text-sm text-muted-foreground">No documents attached.</p>`

    Action and select columns in FirearmTable are NOT placed in this panel — they live on the table row. This panel is the expand-content only.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "trpc.firearm.byId.useQuery" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "bg-warning/10 border border-warning/30" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "bg-milestone/15" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "Update Form 5 Status" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "Unregistered NFA items" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "Consult a licensed firearms attorney or FFL dealer" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>grep -q "NfaStatusDialog" "src/app/(admin)/firearms/_components/FirearmRowDetail.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(admin)/firearms/_components/FirearmRowDetail.tsx`
    - Calls `trpc.firearm.byId.useQuery({ id, entityId })` to consume the Phase 29 D-05 eager-load
    - Renders 3 sections; Section 2 hidden when `!firearm.isNfa`
    - Contains the verbatim NFA unregistered alert string and the persistent recordkeeping disclaimer
    - Renders `NfaStatusDialog` controlled by local `nfaDialogOpen` state; trigger button labeled `"Update Form 5 Status"` is visible only inside Section 2 (i.e., only for NFA items)
    - Uses `bg-warning/10 border border-warning/30` token classes for the alert, `bg-milestone/15` token class for the NFA pill
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>Row-detail panel surfaces firearm.byId eager-load data with 3 conditional sections; the only path to NfaStatusDialog is from inside this panel's NFA section (hidden on non-NFA firearms).</done>
</task>

<task type="auto">
  <name>Task 6: Build FirearmTable (DataTable with 9 visible + 21 hidden columns, bulk delete, CSV export, getRowDetail)</name>
  <files>src/app/(admin)/firearms/_components/FirearmTable.tsx</files>
  <read_first>
    - src/app/(admin)/vehicles/_components/VehicleTable.tsx (lines 1–248 — full template: imports, EditableTextCell / EditableCurrencyCell / EditableSelectCell column cells, selectColumn, actions column shape, DataTable prop set with exportable/exportResource/searchKey/bulkActions/enableRowSelection)
    - src/app/(admin)/trustees/_components/TrusteeTable.tsx (lines 232–275 — Tooltip-wrapped action buttons with `aria-label`, `h-8 w-8` ghost icon buttons)
    - src/app/(admin)/accounting/_components/AccountingTable.tsx (line 217 — `meta: { excludeFromExport: true }` on the actions column)
    - src/app/(admin)/beneficiaries/_components/BeneficiaryTable.tsx (lines 299–304 — `initialColumnVisibility` record shape passed to DataTable)
    - src/app/(admin)/hems/_components/HistoryTable.tsx (lines 88–101 — milestone Badge class pattern for the NFA pill in the typeClassification cell)
    - src/components/ui/data-table.tsx and src/components/ui/data-table-bulk-actions.tsx (to confirm prop signatures for `bulkActions`, `exportable`, `exportResource`, `getRowDetail`, `initialColumnVisibility`, `tableId`)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §DataTable — Default Visible Columns + §DataTable — Behaviors + §CSV Export — Column Scope (binding column list, visibility defaults, meta.excludeFromExport rules)
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §FirearmTable.tsx
    - .planning/research/PITFALLS.md §13 (location sensitivity — hidden from CSV by being hidden from default visible columns)
    - The FirearmRowDetail file from Task 5 (this table renders it via `getRowDetail`)
  </read_first>
  <action>
    Create `src/app/(admin)/firearms/_components/FirearmTable.tsx`. `'use client'`.

    Imports (per 30-PATTERNS.md §FirearmTable.tsx imports block): `ColumnDef` from `@tanstack/react-table`, `Pencil`, `Trash2` from lucide-react, `EditableCurrencyCell`, `EditableSelectCell`, `EditableTextCell` from `@/components/editable-cells`, `Button`, `Badge`, `DataTable`, `BulkAction`, `DataTableColumnHeader`, `selectColumn`, `Tooltip`/`TooltipContent`/`TooltipProvider`/`TooltipTrigger`, type `Firearm` from `@/db/schema`, `STATUS_VARIANTS`, `TRANSFER_STATUS`, `CONDITION_LABELS`, `FIREARM_TYPE_LABELS`, `NFA_TRANSFER_STATUS_LABELS` (and any other firearm label maps from Task 1's placement), `FirearmRowDetail` from `./FirearmRowDetail`.

    Component signature: `export function FirearmTable({ firearms, isLoading, entityId, onEdit, onDelete, onBulkDelete, onInlineUpdate }: { firearms: Firearm[]; isLoading: boolean; entityId: number; onEdit: (f: Firearm) => void; onDelete: (f: Firearm) => void; onBulkDelete: (rows: Firearm[]) => Promise<void>; onInlineUpdate: (id: number, updates: Partial<Firearm>) => Promise<void> })`.

    Define `columns: ColumnDef<Firearm>[]` per UI-SPEC §DataTable — Default Visible Columns. 9 visible:
    1. `selectColumn<Firearm>()` — primitive sets `meta.excludeFromExport` internally per 30-PATTERNS.md
    2. `id: 'name'` — `EditableTextCell` calling `onInlineUpdate(row.original.id, { name: newValue })`; header via `DataTableColumnHeader`
    3. `id: 'firearmIdentity'` — header "Make / Model"; cell: combined `<div><p>{make} {model}</p><p className="text-xs text-muted-foreground">{caliber}</p></div>`; sort accessor on a derived string `\`${make} ${model}\``
    4. `id: 'serialNumber'` — header "Serial #"; cell: `<div className="truncate" title={row.original.serialNumber}><code className="text-xs">{row.original.serialNumber}</code></div>`
    5. `id: 'typeClassification'` — header "Type"; cell: per UI-SPEC §Type + Classification Cell — `<div className="flex items-center gap-2"><span>{FIREARM_TYPE_LABELS[firearmType]}</span>{isNfa && <Badge className="text-milestone-foreground bg-milestone/15 border-milestone/30 text-[10px] px-1 py-0 font-medium">NFA</Badge>}</div>`
    6. `id: 'condition'` — `EditableSelectCell` wired to CONDITION_OPTIONS
    7. `id: 'dodValue'` — `EditableCurrencyCell`
    8. `id: 'transferStatus'` — `EditableSelectCell` wired to TRANSFER_STATUS options
    9. `id: 'actions'` — `meta: { excludeFromExport: true }`; cell: two Tooltip-wrapped ghost icon buttons (Pencil + Trash2) per TrusteeTable lines 232–275; `aria-label="Edit firearm"` and `aria-label="Delete firearm"`; Delete button uses `text-destructive hover:text-destructive`

    Plus 21 hidden columns (still in the `columns[]` array but `initialColumnVisibility` set to false) per UI-SPEC §Hidden by default: `description`, `barrelLength`, `action`, `isNfa`, `nfaClass`, `atfFormType`, `atfControlNumber`, `taxStampDate`, `nfrtrSerial`, `nfaRegistered`, `nfaTransferStatus`, `acquisitionDate`, `acquisitionCost`, `dodValueDate`, `dodValueType`, `location`, `insured`, `status`, `notes`, `createdAt`, `updatedAt`. For each: provide a `ColumnDef` with `accessorKey` matching the field, a `DataTableColumnHeader` header, and a basic cell (`row.original[field] ?? '—'` or `formatCurrency` for money / `format(date)` for dates). For boolean fields (`isNfa`, `nfaRegistered`, `insured`) render `Yes`/`No`. For `nfaTransferStatus` render `<Badge variant={STATUS_VARIANTS[v] || 'secondary'}>{NFA_TRANSFER_STATUS_LABELS[v]}</Badge>`.

    `bulkActions: BulkAction<Firearm>[]` = `[{ label: 'Delete', icon: Trash2, variant: 'destructive', onClick: onBulkDelete }]`.

    `initialColumnVisibility` per UI-SPEC: all 21 hidden columns set to `false` (per BeneficiaryTable.tsx 299–304 shape).

    Render `<DataTable ... />` with props per UI-SPEC §DataTable — Behaviors:
    - `tableId="firearms"`, `columns={columns}`, `data={firearms}`, `searchKey="name"`, `searchPlaceholder="Search firearms..."`, `isLoading={isLoading}`, `emptyMessage="No firearms recorded. Click Add Firearm to create one."` (verbatim per UI-SPEC §Copywriting Contract empty-state body), `enablePagination`, `enableRowSelection`, `bulkActions={bulkActions}`, `exportable`, `exportResource="firearms"`, `initialColumnVisibility={ ... }`, `getRowDetail={(row) => <FirearmRowDetail firearm={row.original} />}`

    `meta.excludeFromExport` MUST be set on the `actions` column (per UI-SPEC §CSV Export). selectColumn handles its own meta internally per 30-PATTERNS.md — but if `selectColumn<Firearm>()` does not auto-set it, set `meta: { excludeFromExport: true }` explicitly on its return (verify via reading src/components/ui/data-table-select-column.tsx).
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "tableId=\"firearms\"" "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q "exportResource=\"firearms\"" "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q "searchKey=\"name\"" "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "Search firearms..." "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q "No firearms recorded. Click Add Firearm to create one." "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "excludeFromExport: true" "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "initialColumnVisibility" "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q "location: false" "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "getRowDetail" "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q "FirearmRowDetail" "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q "bg-milestone/15" "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>grep -q 'aria-label="Edit firearm"' "src/app/(admin)/firearms/_components/FirearmTable.tsx" && grep -q 'aria-label="Delete firearm"' "src/app/(admin)/firearms/_components/FirearmTable.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(admin)/firearms/_components/FirearmTable.tsx`
    - DataTable instantiated with `tableId="firearms"`, `exportResource="firearms"`, `searchKey="name"`, `searchPlaceholder="Search firearms..."`, `emptyMessage="No firearms recorded. Click Add Firearm to create one."`
    - `actions` column has `meta: { excludeFromExport: true }`
    - `initialColumnVisibility` is set with `location: false` (PITFALLS §13 — keeps location out of default CSV) and the other 20 hidden columns set to `false`
    - The typeClassification cell renders the NFA Badge with `bg-milestone/15` class only when `row.original.isNfa === true`
    - `getRowDetail` renders `<FirearmRowDetail firearm={row.original} />`
    - Edit and Delete action buttons are Tooltip-wrapped with `aria-label="Edit firearm"` / `aria-label="Delete firearm"`
    - `bulkActions` contains exactly one entry with `label: 'Delete'`, `variant: 'destructive'`, calling `onBulkDelete`
    - `bun run typecheck` passes
  </acceptance_criteria>
  <done>FirearmTable ships a DataTable with 9 visible default columns + 21 hidden columns, CSV export with location excluded by default, NFA milestone pill in the type cell, row-expand wired to FirearmRowDetail, and bulk delete bulkAction.</done>
</task>

<task type="auto">
  <name>Task 7: Build FirearmsClient (orchestrator with entity-gated list, KPI compute, mutation wiring, CONFLICT handling, bulk delete)</name>
  <files>src/app/(admin)/firearms/_components/FirearmsClient.tsx</files>
  <read_first>
    - src/app/(admin)/vehicles/_components/VehiclesClient.tsx (lines 1–236 — the full orchestrator template: useResourceForm wiring, entity query, list query with `{ enabled: !!entityId }`, mutation onSuccess invalidations, KPI strip derivation, bulk delete sequential pattern with toast.success/error, ConfirmDialog wiring via useConfirmDialog)
    - src/app/(admin)/hems-queue/_components/HemsQueueClient.tsx (lines 305–319 — CONFLICT error handling in a try/catch around mutateAsync: the analog for the serial-number-conflict toast that keeps the dialog open)
    - src/lib/asset-wizard-steps.ts (FIREARM_WIZARD_STEPS from Task 1)
    - src/lib/form-factory.ts (firearmFormDefaults from Task 1)
    - src/components/kpi-strip.tsx (KpiStripItem type signature)
    - src/components/page-header.tsx (PageHeader props)
    - src/lib/money.ts (sumStrings, formatCurrency)
    - .planning/phases/30-firearms-admin-page/30-UI-SPEC.md §Page Layout Shell + §KPI Strip + §Copywriting Contract (verbatim copy: "Firearms" / "Firearms held by the trust." / "Add Firearm" / "Delete Firearm" delete confirm title / "Deleted {n} firearms" / "Failed to delete {n} of {total} firearms" / "A firearm with this serial number already exists.")
    - .planning/phases/30-firearms-admin-page/30-PATTERNS.md §FirearmsClient.tsx
    - The FirearmTable + FirearmDialog files from Tasks 6 and 3
  </read_first>
  <action>
    Create `src/app/(admin)/firearms/_components/FirearmsClient.tsx`. `'use client'`.

    Imports (per 30-PATTERNS.md §FirearmsClient.tsx imports block): `Plus` from lucide-react; `useCallback, useState` from react; `toast` from sonner; `TRPCClientError` from `@trpc/client`; `ConfirmDialog`, `useConfirmDialog` from `@/components/confirm-dialog`; `KpiStrip`, `KpiStripItem` from `@/components/kpi-strip`; `PageHeader` from `@/components/page-header`; `Button`; type `Firearm` from `@/db/schema`; `useResourceForm` from `@/hooks/use-resource-form`; `FIREARM_WIZARD_STEPS` from `@/lib/asset-wizard-steps`; `firearmFormDefaults`, `toDateInput` from `@/lib/form-factory`; `logger`; `sumStrings`, `formatCurrency`, `formatPercent` from `@/lib/money` (or wherever formatPercent lives — check VehiclesClient import); `trpc` from `@/lib/trpc`; `FirearmTable` from `./FirearmTable`; `FirearmDialog` from `./FirearmDialog`.

    `const log = logger.create('Firearms')`

    Component signature: `export function FirearmsClient()`. Inside, follow VehiclesClient line-by-line with `vehicle` → `firearm` substitutions and the CONFLICT addition.

    1. **Entity + list query** (VehiclesClient 29–38 pattern):
       ```
       const utils = trpc.useUtils()
       const { data: entities } = trpc.entity.list.useQuery()
       const entityId = entities?.[0]?.id
       const { data: firearms = [], isLoading: firearmsLoading } = trpc.firearm.list.useQuery({ entityId: entityId! }, { enabled: !!entityId })
       ```

    2. **Mutations** (VehiclesClient 40–48 pattern):
       `createFirearmMutation`, `updateFirearmMutation`, `deleteFirearmMutation` each with `onSuccess: () => utils.firearm.list.invalidate()`. The `update` mutation also calls `utils.firearm.byId.invalidate()` on success — blanket invalidation is ACCEPTABLE here because `useResourceForm`'s `onSuccess` does not expose the row id in scope; blanket form invalidates all byId queries, which is safe and matches the row-detail re-render need. (Distinct from `NfaStatusDialog`, which has direct access to `firearm.id` and uses the scoped form `utils.firearm.byId.invalidate({ id: firearm.id, entityId: firearm.entityId })`.)

    3. **useResourceForm wiring** (mirror VehiclesClient pattern): pass `{ defaults: firearmFormDefaults(), steps: FIREARM_WIZARD_STEPS, onCreate, onUpdate, toFormValues, toApiPayload }` etc. — the toFormValues map turns a Firearm row into form values (date fields → `toDateInput(value)`), the toApiPayload map turns form values into the create/update payload.

    4. **CONFLICT handling** — Inside the `onCreate` (or the wrapped onSubmit that calls `createFirearmMutation.mutateAsync(payload)`): wrap in try/catch. Catch block:
       ```
       if (err instanceof TRPCClientError && err.data?.code === 'CONFLICT') {
         toast.error('A firearm with this serial number already exists.')
         return  // do NOT close the dialog — leaves form open for user to fix serialNumber
       }
       throw err
       ```
       Same pattern in `onUpdate` (in case a future migration permits serialNumber edit — be defensive even though current vehicles flow doesn't update VIN).

    5. **Inline update handler** (per 30-PATTERNS.md §Inline Edit pattern):
       ```
       const handleInlineUpdate = useCallback(async (id: number, updates: Partial<Firearm>) => {
         try { await updateFirearmMutation.mutateAsync({ id, entityId: entityId!, data: updates }) }
         catch (err) { log.error('Failed to update firearm', { error: err }) }
       }, [updateFirearmMutation, entityId])
       ```

    6. **Bulk delete** (VehiclesClient 158–183 sequential pattern):
       ```
       const onBulkDelete = useCallback(async (rows: Firearm[]) => {
         let failed = 0
         for (const row of rows) {
           try { await deleteFirearmMutation.mutateAsync({ id: row.id, entityId: entityId! }) }
           catch (err) { failed++; log.error('Bulk delete failed', { id: row.id, error: err }) }
         }
         if (failed > 0) toast.error(`Failed to delete ${failed} of ${rows.length} firearms`)
         else toast.success(`Deleted ${rows.length} firearms`)
       }, [deleteFirearmMutation, entityId])
       ```

    7. **Single delete confirm** via `useConfirmDialog` (mirror VehiclesClient pattern):
       - Title: "Delete Firearm"
       - Body: "Are you sure you want to delete this firearm? This action cannot be undone."
       - Button: "Delete Firearm"
       - Variant: destructive
       - onConfirm: calls `deleteFirearmMutation.mutateAsync({ id, entityId: entityId! })` then closes
       - No success toast on single delete (per UI-SPEC §Copywriting Contract — matches vehicle precedent)

    8. **KPI derivation** (UI-SPEC §KPI Strip):
       ```
       const totalDodValue = sumStrings(firearms.map((f) => f.dodValue))
       const transferredCount = firearms.filter((f) => f.transferStatus === 'COMPLETE').length
       const transferPct = firearms.length > 0 ? (transferredCount / firearms.length) * 100 : 0
       const nfaCount = firearms.filter((f) => f.isNfa).length
       const kpiData: KpiStripItem[] = [
         { label: 'Firearm count', value: firearms.length },
         { label: 'Total DOD value', value: formatCurrency(totalDodValue) },
         { label: 'Transfer % complete', value: formatPercent(transferPct) },
         { label: 'NFA items', value: nfaCount },
       ]
       ```

    9. **JSX** (UI-SPEC §Page Layout Shell):
       ```
       <div className="space-y-6">
         <PageHeader
           title="Firearms"
           description="Firearms held by the trust."
           breadcrumb={[{ label: 'Assets' }, { label: 'Firearms' }]}
           actions={<Button onClick={firearmForm.handleAdd}><Plus className="h-4 w-4 mr-2" />Add Firearm</Button>}
         />
         <KpiStrip data={kpiData} isLoading={firearmsLoading} />
         <FirearmTable
           firearms={firearms}
           isLoading={firearmsLoading}
           entityId={entityId!}
           onEdit={firearmForm.handleEdit}
           onDelete={(f) => deleteConfirm.confirm(() => handleSingleDelete(f))}
           onBulkDelete={onBulkDelete}
           onInlineUpdate={handleInlineUpdate}
         />
         <FirearmDialog {...firearmForm.dialogProps} />
         <ConfirmDialog {...deleteDialogProps} />
       </div>
       ```

    Copy strings inserted MUST be verbatim per UI-SPEC §Copywriting Contract.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q 'enabled: !!entityId' "src/app/(admin)/firearms/_components/FirearmsClient.tsx" && grep -q "trpc.firearm.list.useQuery" "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "A firearm with this serial number already exists." "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "TRPCClientError" "src/app/(admin)/firearms/_components/FirearmsClient.tsx" && grep -q "err.data?.code === 'CONFLICT'" "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "title=\"Firearms\"" "src/app/(admin)/firearms/_components/FirearmsClient.tsx" && grep -q "Firearms held by the trust." "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "Add Firearm" "src/app/(admin)/firearms/_components/FirearmsClient.tsx" && grep -q "Delete Firearm" "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "Deleted \${" "src/app/(admin)/firearms/_components/FirearmsClient.tsx" && grep -q "Failed to delete \${" "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>grep -q "utils.firearm.list.invalidate" "src/app/(admin)/firearms/_components/FirearmsClient.tsx"</automated>
    <automated>bun run typecheck 2>&1 | grep -v '^#' | grep -c "error TS" | grep -qx "0"</automated>
    <automated>bun run lint 2>&1 | grep -v '^#' | grep -E "error|warning" | grep -c "src/app/\(admin\)/firearms" | grep -qx "0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(admin)/firearms/_components/FirearmsClient.tsx`
    - List query gated with `{ enabled: !!entityId }` (CLAUDE.md non-negotiable entityId pattern)
    - CONFLICT handler present: catches `TRPCClientError` with `err.data?.code === 'CONFLICT'`, toasts the verbatim message `"A firearm with this serial number already exists."`, and returns without closing the dialog
    - PageHeader title `"Firearms"`, description `"Firearms held by the trust."`, primary CTA `"Add Firearm"`
    - KpiStrip data array has exactly 4 entries with labels `"Firearm count"`, `"Total DOD value"`, `"Transfer % complete"`, `"NFA items"`
    - Bulk delete toasts match UI-SPEC verbatim: `"Deleted ${rows.length} firearms"` and `"Failed to delete ${failed} of ${rows.length} firearms"`
    - Single delete confirm uses title `"Delete Firearm"` and button `"Delete Firearm"`
    - All three mutations (create/update/delete) call `utils.firearm.list.invalidate()` on success; update also invalidates byId
    - `bun run typecheck` and `bun run lint` pass with 0 new firearm-page errors/warnings
  </acceptance_criteria>
  <done>FirearmsClient orchestrates the full page: entity-gated list, KpiStrip, FirearmTable, FirearmDialog, ConfirmDialog. Serial conflict surfaces as a toast and keeps the dialog open. All mutations invalidate the list. Verbatim copy from UI-SPEC throughout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Admin UAT — verify all 5 ROADMAP success criteria + NFA workflow</name>
  <what-built>
    The `/firearms` admin page is now reachable. Phase 30 introduces no automated tests (sibling asset pages don't have dedicated tests either; the underlying firearmRouter is already 14/14-test-green from Phase 29). The 5 ROADMAP success criteria are admin-driven UAT items verified manually.
  </what-built>
  <how-to-verify>
    Start dev (`bun run dev`), sign in as admin, navigate to `http://localhost:3000/firearms`. Verify each of the 5 success criteria + the NFA workflow:

    1. **SC-1 (page lists firearms):** Page renders without errors. If no firearms exist yet, you see "No firearms recorded. Click Add Firearm to create one." in the empty state. PageHeader shows "Firearms" + "Firearms held by the trust." breadcrumb "Assets › Firearms". KpiStrip shows 4 cards: Firearm count / Total DOD value / Transfer % complete / NFA items.

    2. **SC-2 (create works):** Click "Add Firearm". 3-step wizard opens with steps "Identity" / "Valuation" / "Ownership". Fill Step 1 minimally (name, make, model, serialNumber, firearmType=PISTOL). Toggle the "NFA Item" switch → the NFA Classification subsection appears with `nfaClass`, `atfFormType`, `nfaRegistered`, `atfControlNumber`, `taxStampDate`, `nfrtrSerial` fields AND the disclaimer "NFA fields are for recordkeeping only..."; untoggle → it disappears. Set isNfa=false for this test, click Next through to Step 3, click "Create Firearm". Toast confirms, row appears in the table.

    3. **SC-2-conflict:** Click "Add Firearm" again, try to create another firearm with the same serialNumber. Expect toast `"A firearm with this serial number already exists."` and the dialog STAYS OPEN so you can change the serial number.

    4. **SC-3 (edit works):** Click the Pencil action on a table row. The dialog opens in edit mode with title "Edit Firearm" and submit button "Save Changes". Change a field (e.g., name or dodValue), save. Row updates in the table.

    5. **SC-3-inline-edit:** Try inline-editing the `name` cell, the `dodValue` cell, the `condition` Select cell, and the `transferStatus` Select cell directly in the table. Each edit persists.

    6. **SC-4 (delete works):** Click the Trash2 action button on a row. ConfirmDialog appears with title "Delete Firearm" and body "Are you sure you want to delete this firearm? This action cannot be undone." Confirm → row disappears. Test bulk delete: select multiple rows via checkboxes, click "Delete" in the bulk-actions bar, confirm — toast `Deleted N firearms` appears, rows removed.

    7. **SC-5 (sort/filter/CSV):** Click any column header — sort toggles. Type in the search box — rows filter. Click "Columns" toggle — verify the 21 hidden columns are togglable. Click "Export" → CSV downloads. Open the CSV and verify: it includes `serialNumber` BUT NOT `location` (default hidden), and the `actions` / `select` columns are absent (meta.excludeFromExport).

    8. **NFA workflow (FIRE-05 surfaced):** Create a firearm with `isNfa=true`, `nfaClass=SUPPRESSOR`, `nfaRegistered=true`. Expand its row via the chevron. The row-detail panel shows 3 sections including "NFA Classification" with the violet "NFA" pill. The recordkeeping disclaimer "This section is for recordkeeping only. Consult a licensed firearms attorney or FFL dealer for ATF transfer requirements." is visible. Click "Update Form 5 Status" → NfaStatusDialog opens titled "Update ATF Form 5 Status". Set status=APPROVED, fill `atfControlNumber` and `taxStampDate`, click "Save Status" → toast "NFA transfer status updated.", dialog closes, the badge in the row-detail updates to "Approved".

    9. **NFA unregistered warning:** Create or edit a firearm with `isNfa=true` and `nfaRegistered=false`. Expand the row → the warning block appears with text starting "This NFA item is not registered in the NFRTR. Unregistered NFA items are contraband. Do not attempt to transfer. Consult an attorney immediately."

    10. **Non-NFA hides NFA UI:** Create a non-NFA firearm (`isNfa=false`). Expand its row → NO "NFA Classification" section, NO "Update Form 5 Status" button.

    11. **Entity gating:** Confirm the URL query never includes data from a different entity. The list query parameter `entityId` must equal `entities[0].id` (Hudson Trust id=1). If you have access to dev tools, inspect the tRPC POST body.

    12. **Final gates:** Run `bun run typecheck` and `bun run lint` from the repo root — both must exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 checks pass. If any fail, describe the failure and the failing step so the executor can produce a follow-up commit. If only cosmetic issues exist (e.g., spacing tweak, label nit), file them as follow-ups but approve the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→tRPC | Untrusted form input (firearm create/update payload, NFA status payload) crosses here; validated by adminProcedure + Zod schemas defined in db/validation.ts (Phase 28) and refined inline in firearm router (Phase 29) |
| browser→CSV download | Sensitive firearm data leaves the trust boundary as a downloaded file |
| admin browser→admin tooling | All routes gated by adminProcedure; beneficiary JWT rejected (Phase 29 SC-4 verified) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-XEN | Information Disclosure | trpc.firearm.list / byId calls from FirearmsClient + FirearmRowDetail | mitigate | Every query passes `entityId: selectedEntity!` from `trpc.entity.list.useQuery()` and uses `{ enabled: !!entityId }`; RLS policy from Phase 28 enforces the same scope server-side; the router's NOT_FOUND on cross-entity byId (Phase 29 SC-3) is defense-in-depth |
| T-30-CSV | Information Disclosure | DataTable CSV export in FirearmTable | mitigate | `location` excluded from default visible columns via `initialColumnVisibility: { location: false, ... }` (PITFALLS §13); DataTable's exporter uses `getVisibleLeafColumns` so hidden columns never reach the CSV; `actions` and `select` columns marked `meta: { excludeFromExport: true }` per UI-SPEC §CSV Export — Column Scope; user must explicitly toggle location visible to include it in CSV (acceptable trade-off — admin can intentionally export full data) |
| T-30-NFA | Tampering | trpc.firearm.setNfaTransferStatus invocation from NfaStatusDialog | mitigate | UI path: NfaStatusDialog trigger button rendered ONLY inside FirearmRowDetail's Section 2, which is itself conditional on `firearm.isNfa === true`. Server defense-in-depth: Phase 29 setNfaTransferStatus throws BAD_REQUEST when `!existing.isNfa`. FirearmDialog OMITS `nfaTransferStatus` entirely (D-03 binding), preventing it from being set via the generic update path |
| T-30-LEG | Repudiation (legal-advice misclassification) | User-facing copy in FirearmDialog NFA section, FirearmRowDetail NFA section, NfaStatusDialog | mitigate | Three persistent disclaimers per UI-SPEC §Copywriting Contract verbatim: (1) inside the FirearmDialog NFA conditional section "NFA fields are for recordkeeping only. ATF approval is required before transferring NFA items to a beneficiary."; (2) inside FirearmRowDetail Section 2 "This section is for recordkeeping only. Consult a licensed firearms attorney or FFL dealer for ATF transfer requirements."; (3) inside NfaStatusDialog body "This app does not file ATF forms. Record the status after filing through your FFL or attorney."; Unregistered-NFA alert block surfaces additional warning when `nfaRegistered === false` |
| T-30-SC | Tampering | npm package installs | accept | No new packages installed in Phase 30 — all dependencies (`@tanstack/react-query`, `@tanstack/react-table`, sonner, shadcn primitives, lucide-react, react-hook-form, zod) already present; legitimacy audit not required for zero-install plan |
</threat_model>

<verification>
**Phase-level success criteria** mapped to task acceptance criteria:

| SC | Description | Verified by |
|----|-------------|-------------|
| SC-1 | Admin sees DataTable listing entity-scoped firearm records | Task 7 (FirearmsClient entity-gated list query) + Task 6 (FirearmTable renders) + Task 8 step 1 |
| SC-2 | Admin can create a firearm; row appears on success | Task 3 (FirearmDialog create flow) + Task 7 (createFirearmMutation + invalidation) + Task 8 steps 2-3 |
| SC-3 | Admin can edit any field; row updates on save | Task 3 (FirearmDialog edit mode) + Task 6 (inline-edit cells) + Task 7 (updateFirearmMutation + invalidation) + Task 8 steps 4-5 |
| SC-4 | Admin can delete a firearm; row removed + success toast | Task 6 (Trash2 + bulkActions) + Task 7 (deleteFirearmMutation + ConfirmDialog + bulk delete toasts) + Task 8 step 6 |
| SC-5 | Admin can sort/filter/CSV-export | Task 6 (DataTable searchKey + exportable + initialColumnVisibility) + Task 8 step 7 |

**Bonus verifications:**
- NFA workflow surface (FIRE-05 UX): Tasks 3 + 4 + 5 + Task 8 step 8
- Unregistered-NFA warning: Task 5 + Task 8 step 9
- Non-NFA hides NFA UI: Tasks 3 + 5 + Task 8 step 10
- Entity gating (T-30-XEN mitigation): Task 7 + Task 8 step 11
- Recordkeeping disclaimers in 3 surfaces (T-30-LEG mitigation): Tasks 3, 4, 5

**Final gates (Task 8 step 12):** `bun run typecheck` exits 0, `bun run lint` exits 0.

**No automated tests added** — Phase 30 is a UI-only phase; the firearmRouter contract (the only data-mutation surface) is already sealed by Phase 29's 14-test integration suite. Sibling asset pages (`/vehicles`, `/accounts`, `/artwork`, etc.) ship without dedicated component tests; following that precedent. UAT in Task 8 is the gate.
</verification>

<success_criteria>
**Plan completes when:**
- [ ] All 8 task `<acceptance_criteria>` blocks satisfied
- [ ] `/firearms` route renders without error after dev start
- [ ] All 5 ROADMAP SCs verified by Task 8 UAT
- [ ] FIRE-06 and FIRE-07 both satisfied (CRUD page + sort/filter/CSV)
- [ ] FirearmDialog contains 0 references to `nfaTransferStatus` (D-03 binding honored)
- [ ] NfaStatusDialog is the only component calling `trpc.firearm.setNfaTransferStatus.useMutation`
- [ ] CSV column-exclusion: `meta: { excludeFromExport: true }` on `actions` column; `location: false` in `initialColumnVisibility`
- [ ] All 3 recordkeeping disclaimers present verbatim (FirearmDialog NFA section, FirearmRowDetail NFA section, NfaStatusDialog body)
- [ ] `bun run typecheck` and `bun run lint` both exit 0
</success_criteria>

<output>
Create `.planning/phases/30-firearms-admin-page/30-01-SUMMARY.md` when done. Frontmatter should mirror Phase 29's SUMMARY shape: `phase: 30-firearms-admin-page`, `plan: 01`, `subsystem: ui-admin-page`, `tags: [firearm, datatable, wizard-dialog, nfa, cqs-affordance, csv-export]`, `requires: [{ phase: 29-firearm-trpc-router, provides: firearmRouter ... }]`, `provides: [/firearms admin page (8 files); FIREARM_WIZARD_STEPS + firearmFormDefaults + STATUS_VARIANTS additions]`, `affects: [31-asset-aggregator-integration, 32-sidebar-nav-alphabetization]`, `requirements-completed: [FIRE-06, FIRE-07]`. Body sections: Performance / Accomplishments / ROADMAP Success Criteria Coverage (5-row table) / Task Commits / Files Created/Modified / Decisions Made / Deviations from Plan / Notes for Next Plan (Phase 31 wires firearm.listAll + dashboard.summary; Phase 32 adds the sidebar Firearms link).
</output>
