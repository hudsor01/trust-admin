# Phase 10 Plan 01: Research and Strategy

**Status**: Not started
**Depends on**: Phase 9 complete (✅)
**Objective**: Research TanStack Table v8 and TanStack Form patterns, audit current implementation, and document migration strategy.

## Execution Context

@.planning/phases/10-tanstack-table-form-integration/OVERVIEW.md - Phase 10 overview with 8 sub-plans
@src/components/data-table.tsx - Current DataTable component (uses manual shadcn/ui Table)
@src/components/resource-dialog.tsx - Current ResourceDialog component (uses manual form state)
@src/hooks/use-resource-form.ts - Current form state management hook
@docs/component-patterns.md - Extracted component patterns documentation

## Context

Phase 10 completes the TanStack ecosystem adoption:
- ✅ **TanStack Query v5** (Phase 8) - Data fetching, caching, mutations
- ⏳ **TanStack Table v8** (Phase 10) - Table UI with advanced features
- ⏳ **TanStack Form** (Phase 10) - Form state management with validation

**Current state**:
- **Tables**: 4 pages use DataTable, 12 use manual shadcn/ui Table
- **Forms**: 4 pages use ResourceDialog, 8+ use manual Dialog forms

**This plan establishes the foundation** for Plans 10-02 through 10-08 by:
1. Researching TanStack Table and Form best practices
2. Auditing current implementations to understand patterns
3. Documenting migration strategy and patterns to follow

## Discovery Assessment

**Level**: Level 1 - Quick Verification (TanStack libraries are well-documented)

**What we know**:
- TanStack Table v8 is headless UI library with built-in sorting, filtering, pagination
- TanStack Form provides type-safe form state with Zod integration
- Both libraries have official docs and TypeScript examples
- We already use TanStack Query v5 successfully (Phase 8)

**What we need to verify**:
- Latest TanStack Table v8 patterns for React 19
- TanStack Form + Zod integration patterns
- Compatibility with shadcn/ui components
- Migration approach that maintains styling and behavior

**Research method**: Quick documentation review + example code analysis

## Tasks

### Task 1: Research TanStack Table v8 patterns

Research TanStack Table v8 for React 19 and document key patterns:

**Research areas**:
1. **Installation and setup**
   - Package: `@tanstack/react-table`
   - Required dependencies
   - TypeScript configuration

2. **Core concepts**
   - Column definitions with type safety
   - Table instance creation with `useReactTable`
   - Rendering patterns (headless UI)
   - Built-in features: sorting, filtering, pagination, column resizing

3. **Integration with shadcn/ui**
   - How to maintain shadcn/ui Table styling
   - Rendering pattern: TanStack logic + shadcn/ui components
   - Example wrapper component structure

4. **Advanced features**
   - Virtual scrolling for large datasets
   - Row selection
   - Inline editing support
   - Custom cell renderers

**Deliverable**: Document findings in `.planning/phases/10-tanstack-table-form-integration/RESEARCH.md`

**Example pattern to document**:
```typescript
// TanStack Table with shadcn/ui styling
import { useReactTable, getCoreRowModel, ColumnDef } from '@tanstack/react-table'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'

function DataTable<T>({ data, columns }: { data: T[], columns: ColumnDef<T>[] }) {
  const table = useReactTable({
    data,
    columns,
    getCoreRowModel: getCoreRowModel(),
  })

  return (
    <Table>
      <TableHeader>
        {table.getHeaderGroups().map(headerGroup => (
          <TableRow key={headerGroup.id}>
            {headerGroup.headers.map(header => (
              <TableHead key={header.id}>
                {/* Render header */}
              </TableHead>
            ))}
          </TableRow>
        ))}
      </TableHeader>
      <TableBody>
        {/* Render rows */}
      </TableBody>
    </Table>
  )
}
```

**Success criteria**:
- [ ] Documented installation steps
- [ ] Documented column definition pattern
- [ ] Documented table instance creation pattern
- [ ] Documented shadcn/ui integration approach
- [ ] Identified inline editing support strategy

**Resources**:
- Official docs: https://tanstack.com/table/latest/docs/introduction
- React 19 examples: https://tanstack.com/table/latest/docs/framework/react/examples

---

### Task 2: Research TanStack Form patterns with Zod

Research TanStack Form for React 19 and document Zod integration:

**Research areas**:
1. **Installation and setup**
   - Package: `@tanstack/react-form`
   - Zod adapter/validator package
   - TypeScript configuration

2. **Core concepts**
   - Form instance creation with `useForm`
   - Field-level type safety
   - Zod schema validation integration
   - Form submission handling
   - Error state management

3. **Integration with shadcn/ui**
   - How to use TanStack Form with shadcn/ui Input, Select, etc.
   - Field wrapper pattern
   - Error message display
   - Form layout preservation

4. **Advanced features**
   - Async validation
   - Field arrays (dynamic forms)
   - Dependent fields
   - Custom validators

**Deliverable**: Add findings to `RESEARCH.md` (same file as Task 1)

**Example pattern to document**:
```typescript
// TanStack Form with Zod validation
import { useForm } from '@tanstack/react-form'
import { zodValidator } from '@tanstack/zod-form-adapter'
import { z } from 'zod'

const schema = z.object({
  name: z.string().min(1, 'Name required'),
  email: z.string().email('Invalid email'),
})

function MyForm() {
  const form = useForm({
    defaultValues: { name: '', email: '' },
    onSubmit: async ({ value }) => {
      // Submit form
    },
    validators: {
      onChange: schema,
    },
  })

  return (
    <form onSubmit={(e) => { e.preventDefault(); form.handleSubmit() }}>
      <form.Field
        name="name"
        validators={{
          onChange: z.string().min(1),
        }}
      >
        {(field) => (
          <Input
            value={field.state.value}
            onChange={(e) => field.handleChange(e.target.value)}
          />
        )}
      </form.Field>
    </form>
  )
}
```

**Success criteria**:
- [ ] Documented installation steps
- [ ] Documented form instance creation pattern
- [ ] Documented Zod integration approach
- [ ] Documented shadcn/ui Input/Select integration
- [ ] Identified error handling pattern

**Resources**:
- Official docs: https://tanstack.com/form/latest/docs/overview
- Zod integration: https://tanstack.com/form/latest/docs/framework/react/guides/validation
- React 19 examples: https://tanstack.com/form/latest/docs/framework/react/examples

---

### Task 3: Audit current table implementations

Analyze existing table patterns across all 16 pages:

**Pages to audit** (from OVERVIEW.md):

**DataTable pages (4)**:
- `src/pages/Accounting.tsx`
- `src/pages/Accounts.tsx`
- `src/pages/Liabilities.tsx`
- `src/pages/Properties.tsx`

**Manual Table pages (12)**:
- `src/pages/ActivityLog.tsx`
- `src/pages/Beneficiaries.tsx`
- `src/pages/Bequests.tsx`
- `src/pages/Contacts.tsx`
- `src/pages/Dashboard.tsx`
- `src/pages/Distributions.tsx`
- `src/pages/DistributionWizard.tsx`
- `src/pages/HemsQueue.tsx`
- `src/pages/Settings.tsx`
- `src/pages/Trustees.tsx`
- `src/pages/Vehicles.tsx`

**Audit criteria per page**:
1. Count Table instances (Grep for `<Table` in file)
2. Identify table features used:
   - Inline editing (EditableCells)
   - Sorting (manual or via state)
   - Filtering (via entity selector or other)
   - Pagination (after Phase 9)
   - Row actions (edit/delete buttons)
   - Custom cell renderers (badges, formatters)
3. Note complexity level: Simple, Complex, or Workflow
4. Identify migration challenges

**Deliverable**: Create `TABLE-AUDIT.md` with findings table:

```markdown
| Page | Table Count | Features | Complexity | Migration Notes |
|------|-------------|----------|------------|-----------------|
| Accounting.tsx | 1 | Inline edit, pagination, entity filter | Simple | Already uses DataTable |
| Contacts.tsx | 19 | Row actions, badges | Complex | Multiple tables, needs consolidation |
```

**Success criteria**:
- [ ] All 16 pages audited
- [ ] Features and complexity documented
- [ ] Migration challenges identified
- [ ] Batching strategy validated (simple → complex → workflow)

**Commands to use**:
```bash
# Count Table instances per file
grep -c "<Table" src/pages/Accounting.tsx

# Show Table usage patterns
grep -A 3 "<Table" src/pages/Contacts.tsx

# Find all EditableCell usage
grep -r "EditableCell" src/pages/
```

---

### Task 4: Audit current form implementations

Analyze existing form patterns across all pages:

**Pages to audit**:

**ResourceDialog pages (4)**:
- `src/pages/Accounting.tsx`
- `src/pages/Accounts.tsx`
- `src/pages/Liabilities.tsx`
- `src/pages/Properties.tsx`

**Manual Dialog pages (8+)**:
- `src/pages/Beneficiaries.tsx`
- `src/pages/Bequests.tsx`
- `src/pages/Contacts.tsx`
- `src/pages/HemsQueue.tsx`
- `src/pages/Settings.tsx`
- `src/pages/Trustees.tsx`
- `src/pages/Vehicles.tsx`

**Audit criteria per page**:
1. Count Dialog instances (Grep for `<Dialog` in file)
2. Identify form patterns:
   - Uses ResourceDialog? (check imports)
   - Manual Dialog + useState?
   - Form fields count (Input, Select, Textarea)
   - Validation approach (manual checks, schema)
   - Field types (text, number, date, select, textarea)
3. Note complexity level: Simple, Complex
4. Identify migration challenges

**Deliverable**: Create `FORM-AUDIT.md` with findings table:

```markdown
| Page | Dialog Count | Pattern | Fields | Complexity | Migration Notes |
|------|--------------|---------|--------|------------|-----------------|
| Accounting.tsx | 1 | ResourceDialog | 8 | Simple | Already abstracted |
| Contacts.tsx | 21 | Manual Dialog | 6 | Simple | Straightforward migration |
```

**Success criteria**:
- [ ] All pages with forms audited
- [ ] Form patterns and field counts documented
- [ ] Migration complexity assessed
- [ ] Batching strategy validated (ResourceDialog → manual)

**Commands to use**:
```bash
# Count Dialog instances per file
grep -c "<Dialog" src/pages/Contacts.tsx

# Check for ResourceDialog usage
grep "ResourceDialog" src/pages/Accounting.tsx

# Find form field types
grep -E "(Input|Select|Textarea)" src/pages/Contacts.tsx | wc -l
```

---

### Task 5: Document migration strategy

Create comprehensive migration strategy document based on research and audits:

**Deliverable**: Create `MIGRATION-STRATEGY.md` with sections:

1. **TanStack Table Migration Strategy**
   - High-level approach (progressive enhancement)
   - Step-by-step migration process
   - Column definition patterns
   - Inline editing strategy (EditableCells compatibility)
   - Styling preservation approach (shadcn/ui)
   - Testing approach per page

2. **TanStack Form Migration Strategy**
   - High-level approach (integrate with existing patterns)
   - Step-by-step migration process
   - Field wrapper patterns for shadcn/ui components
   - Zod schema integration (reuse Drizzle schemas)
   - Error display patterns
   - Testing approach per form

3. **Migration Batches**
   - **Table Batch 1** (Simple): List pages, expected effort
   - **Table Batch 2** (Complex): List pages, expected effort
   - **Table Batch 3** (Workflow): List pages, expected effort
   - **Form Batch 1** (ResourceDialog): List pages, expected effort
   - **Form Batch 2** (Manual Dialogs): List pages, expected effort

4. **Risk Mitigation**
   - Backward compatibility strategy
   - Testing approach (manual testing per page)
   - Rollback plan if issues arise
   - How to handle breaking changes

5. **Code Patterns to Follow**
   - TanStack Table wrapper component structure
   - Column definition examples
   - TanStack Form field wrapper examples
   - Zod schema integration examples

**Success criteria**:
- [ ] Clear migration process documented
- [ ] Code patterns provided for each library
- [ ] Batch groupings finalized with effort estimates
- [ ] Risk mitigation strategies defined
- [ ] Backward compatibility approach documented

---

## Verification

**Documentation completeness**:
```bash
# Verify all deliverables exist
ls -la .planning/phases/10-tanstack-table-form-integration/RESEARCH.md
ls -la .planning/phases/10-tanstack-table-form-integration/TABLE-AUDIT.md
ls -la .planning/phases/10-tanstack-table-form-integration/FORM-AUDIT.md
ls -la .planning/phases/10-tanstack-table-form-integration/MIGRATION-STRATEGY.md
```

**Manual review**:
1. Read RESEARCH.md - verify TanStack patterns documented
2. Read TABLE-AUDIT.md - verify all 16 pages covered
3. Read FORM-AUDIT.md - verify all form pages covered
4. Read MIGRATION-STRATEGY.md - verify clear process and examples

**Validation questions**:
- Can we start Plan 10-02 with confidence based on research?
- Are migration batches properly grouped by complexity?
- Do we have example code patterns to follow?
- Are risks and mitigation strategies clear?

## Success Criteria

- [ ] RESEARCH.md created with TanStack Table and Form patterns
- [ ] TABLE-AUDIT.md created with all 16 pages analyzed
- [ ] FORM-AUDIT.md created with all form pages analyzed
- [ ] MIGRATION-STRATEGY.md created with comprehensive strategy
- [ ] Code examples provided for both TanStack Table and Form
- [ ] Migration batches validated and effort estimated
- [ ] Risk mitigation strategies documented
- [ ] Ready to execute Plan 10-02 (TanStack Table Core Wrapper)

## Notes

**Research focus**: Official TanStack documentation and examples - these libraries are mature and well-documented.

**Audit efficiency**: Use grep commands to quickly count instances and identify patterns rather than reading entire files.

**Migration philosophy**: Progressive enhancement - maintain existing functionality and styling while adding TanStack features.

**Backward compatibility**: Existing non-migrated pages should continue working during gradual migration.

**Time estimate**: 2-3 hours total
- Task 1 (TanStack Table research): 30-45 minutes
- Task 2 (TanStack Form research): 30-45 minutes
- Task 3 (Table audit): 30-45 minutes
- Task 4 (Form audit): 30-45 minutes
- Task 5 (Migration strategy): 30-45 minutes

## Next Steps

After completing this plan:
1. Commit all documentation files
2. Create summary document (10-01-SUMMARY.md)
3. Move to Plan 10-02: TanStack Table Core Wrapper
4. Use MIGRATION-STRATEGY.md as reference for all subsequent plans
