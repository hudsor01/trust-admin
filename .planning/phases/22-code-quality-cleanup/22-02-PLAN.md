---
phase: 22-code-quality-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/dashboard/_components/DashboardClient.tsx
  - src/app/(admin)/accounts/_components/AccountsClient.tsx
  - src/app/(admin)/vehicles/_components/VehiclesClient.tsx
  - src/app/(admin)/properties/_components/PropertiesClient.tsx
  - src/app/(admin)/liabilities/_components/LiabilitiesClient.tsx
  - src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx
  - src/app/(admin)/hems/_components/HemsClient.tsx
  - src/app/(admin)/hems-queue/_components/HemsQueueClient.tsx
  - src/app/(admin)/insurance/_components/InsuranceClient.tsx
  - src/app/(admin)/artwork/_components/ArtworkClient.tsx
  - src/app/(admin)/personal-property/_components/PersonalPropertyClient.tsx
  - src/app/(admin)/bequests/_components/BequestsClient.tsx
  - src/app/(admin)/settings/_components/SettingsClient.tsx
  - src/app/(admin)/trustees/_components/TrusteesClient.tsx
  - src/app/(admin)/accounting/_components/AccountingClient.tsx
  - src/app/(admin)/users/_components/UsersClient.tsx
  - src/app/(admin)/inventory-queue/_components/InventoryQueueClient.tsx
  - src/app/api/auth/custom/reset-password/route.ts
  - src/app/api/auth/custom/forgot-password/route.ts
  - src/app/api/inventory/analyze/route.ts
autonomous: true
requirements: [CLEAN-03, CLEAN-07, CLEAN-08]

must_haves:
  truths:
    - "No admin client component contains the literal 'const entityId = 1'"
    - "All admin client components derive entityId from entity list query cache"
    - "Auth route errors are logged via structured logger, not console.error"
    - "Inventory analyze route 500 response does not contain error.message"
  artifacts:
    - path: "src/app/(admin)/dashboard/_components/DashboardClient.tsx"
      provides: "Example of entity cache pattern"
      contains: "trpc.entity.list.useQuery"
    - path: "src/app/api/auth/custom/reset-password/route.ts"
      provides: "Structured error logging"
      contains: "logger"
    - path: "src/app/api/inventory/analyze/route.ts"
      provides: "Generic 500 error response"
      contains: "Internal server error"
  key_links:
    - from: "src/app/(admin)/*/_components/*Client.tsx"
      to: "trpc.entity.list"
      via: "useQuery for entity cache"
      pattern: "trpc\\.entity\\.list\\.useQuery"
    - from: "src/app/api/auth/custom/*/route.ts"
      to: "src/lib/logger.ts"
      via: "structured logger import"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Replace hardcoded `entityId = 1` in all 17 admin client components with entity derived from the entity list query cache. Also replace console.error in auth routes with structured logger and fix error message leaking in inventory analyze route.

Purpose: entityId=1 is a single-trust assumption baked into component code -- deriving from query cache makes components data-driven. Structured logging and generic error responses follow production best practices.
Output: All admin client components use entity from cache. Auth routes use logger. Analyze route returns generic 500.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing entity cache pattern already used by DashboardClient: -->

```typescript
// src/app/(admin)/dashboard/_components/DashboardClient.tsx (lines 57-58)
const { data: entities } = trpc.entity.list.useQuery()
const entity = entities?.[0] ?? null
// Then uses entity.id where entityId was used
```

<!-- Structured logger from src/lib/logger.ts: -->
```typescript
import { logger } from '@/lib/logger'
// Pre-built namespaced loggers:
logger.auth   // for auth routes
logger.api    // for API routes
// Or create custom:
const log = logger.create('ModuleName')
log.error('message', { key: value })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded entityId=1 in all admin client components</name>
  <files>
    src/app/(admin)/dashboard/_components/DashboardClient.tsx,
    src/app/(admin)/accounts/_components/AccountsClient.tsx,
    src/app/(admin)/vehicles/_components/VehiclesClient.tsx,
    src/app/(admin)/properties/_components/PropertiesClient.tsx,
    src/app/(admin)/liabilities/_components/LiabilitiesClient.tsx,
    src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx,
    src/app/(admin)/hems/_components/HemsClient.tsx,
    src/app/(admin)/hems-queue/_components/HemsQueueClient.tsx,
    src/app/(admin)/insurance/_components/InsuranceClient.tsx,
    src/app/(admin)/artwork/_components/ArtworkClient.tsx,
    src/app/(admin)/personal-property/_components/PersonalPropertyClient.tsx,
    src/app/(admin)/bequests/_components/BequestsClient.tsx,
    src/app/(admin)/settings/_components/SettingsClient.tsx,
    src/app/(admin)/trustees/_components/TrusteesClient.tsx,
    src/app/(admin)/accounting/_components/AccountingClient.tsx,
    src/app/(admin)/users/_components/UsersClient.tsx,
    src/app/(admin)/inventory-queue/_components/InventoryQueueClient.tsx
  </files>
  <action>
In each of the 17 admin client components, apply the same pattern:

1. Replace `const entityId = 1` with:
   ```typescript
   const { data: entities } = trpc.entity.list.useQuery()
   const entityId = entities?.[0]?.id
   ```
   Note: DashboardClient already has this pattern -- skip it or verify it does not also have `const entityId = 1` (it does at line 48, but also has the query at line 57 -- remove the `const entityId = 1` line and use only the query-derived value).

2. For components that have data-fetching queries using `entityId`, add `{ enabled: !!entityId }` if not already present. This prevents queries from firing before the entity list loads. Example:
   ```typescript
   const { data } = trpc.vehicle.list.useQuery(
       { entityId: entityId! },
       { enabled: !!entityId }
   )
   ```
   Most components already use this pattern with `selectedEntity` -- apply the same guard with `entityId`.

3. For mutation calls that pass `entityId`, use `entityId!` (non-null assertion) since mutations are user-triggered and entityId will be loaded by then.

4. If the component already has `trpc.entity.list.useQuery()` (like DashboardClient), just remove the `const entityId = 1` line and ensure the query-derived entityId is used everywhere.

IMPORTANT: The `trpc` import should already exist in every file. If `trpc.entity.list.useQuery()` is not already called, add it. Entity list data is already prefetched by server-side page.tsx files via HydrationBoundary, so the useQuery will hit cache -- no extra network request.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/trust-admin && ! grep -rn "const entityId = 1" src/app/\(admin\)/ && bun run typecheck</automated>
  </verify>
  <done>Zero admin client components contain `const entityId = 1`. All derive entityId from `trpc.entity.list.useQuery()`. TypeScript passes. Queries are guarded with `enabled: !!entityId`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console.error with structured logger in auth routes and fix analyze route error leak</name>
  <files>
    src/app/api/auth/custom/reset-password/route.ts,
    src/app/api/auth/custom/forgot-password/route.ts,
    src/app/api/inventory/analyze/route.ts
  </files>
  <action>
1. In src/app/api/auth/custom/reset-password/route.ts:
   - Add `import { logger } from '@/lib/logger'` at top
   - Replace `console.error('[reset-password]', err)` (line 92) with:
     `logger.auth.error('Password reset failed', { error: err instanceof Error ? err.message : 'Unknown error' })`

2. In src/app/api/auth/custom/forgot-password/route.ts:
   - Add `import { logger } from '@/lib/logger'` at top
   - Replace `console.error('[forgot-password] webhook error:', webhookRes.status, body)` (lines 87-91) with:
     `logger.auth.error('Password reset webhook failed', { status: webhookRes.status, body })`
   - Replace `console.error('[forgot-password]', err)` (line 97) with:
     `logger.auth.error('Forgot password failed', { error: err instanceof Error ? err.message : 'Unknown error' })`

3. In src/app/api/inventory/analyze/route.ts:
   - The final catch block (lines 132-141) currently returns `error.message` in the 500 response. Replace:
     ```typescript
     return NextResponse.json(
         {
             success: false,
             error: error instanceof Error
                 ? error.message
                 : 'Unknown error during analysis',
         },
         { status: 500 },
     )
     ```
     With:
     ```typescript
     return NextResponse.json(
         {
             success: false,
             error: 'Internal server error',
         },
         { status: 500 },
     )
     ```
   - The rate-limit (429) and auth-failure (401) specific error responses above can stay -- those are intentional user-facing messages, not exception detail leaks. The fix is specifically for the generic 500 catch-all that was exposing `error.message`.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/trust-admin && ! grep -n "console\.error" src/app/api/auth/custom/*/route.ts && ! grep -n "error\.message" src/app/api/inventory/analyze/route.ts | grep -v "rate limit\|401\|authentication" && bun run typecheck</automated>
  </verify>
  <done>Auth route catch blocks use logger.auth.error instead of console.error. Inventory analyze route 500 response returns "Internal server error" without leaking exception details. TypeScript passes.</done>
</task>

</tasks>

<verification>
- `grep -rn "const entityId = 1" src/app/\(admin\)/` returns zero results
- `grep -rn "console\.error" src/app/api/auth/custom/` returns zero results
- `grep -n "error\.message" src/app/api/inventory/analyze/route.ts` only matches the rate-limit and auth-failure checks, not the 500 response
- `bun run typecheck` passes
- `bun run lint` passes
</verification>

<success_criteria>
- All 17 admin client components derive entityId from trpc.entity.list.useQuery() cache
- No hardcoded `const entityId = 1` remains in admin client components
- Auth routes use structured logger for error logging
- Inventory analyze route returns generic "Internal server error" on 500
- TypeScript and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-code-quality-cleanup/22-02-SUMMARY.md`
</output>
