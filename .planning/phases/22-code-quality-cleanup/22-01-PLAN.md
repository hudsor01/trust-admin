---
phase: 22-code-quality-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/queries.ts
  - db/index.ts
  - src/server/trpc/routers/contact.ts
  - src/lib/date-utils.ts
  - package.json
  - next.config.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-06]

must_haves:
  truths:
    - "db/queries.ts exports only functions that are imported elsewhere in the codebase"
    - "date-utils.ts does not exist and date-fns is not in dependencies"
    - "TxSql type is defined once in db/index.ts and imported by consumers"
  artifacts:
    - path: "db/queries.ts"
      provides: "Only actively-used query functions"
      contains: "export async function getEntityById"
    - path: "db/index.ts"
      provides: "Shared TxSql type export"
      contains: "export type TxSql"
  key_links:
    - from: "src/server/trpc/routers/contact.ts"
      to: "db/index.ts"
      via: "TxSql import"
      pattern: "import.*TxSql.*from.*@/db"
    - from: "db/queries.ts"
      to: "db/index.ts"
      via: "TxSql import"
      pattern: "import.*TxSql.*from.*./index"
---

<objective>
Remove dead code from db/queries.ts (~80 unused exported functions), delete the unused date-utils.ts module and date-fns dependency, and consolidate the duplicate TxSql type definition into a single shared export.

Purpose: Reduce codebase surface area -- dead code masks what is actually used and adds maintenance burden.
Output: Leaner queries.ts with only active exports, no date-utils.ts, single TxSql definition.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@db/queries.ts
@db/index.ts
@src/server/trpc/routers/contact.ts

<interfaces>
<!-- Functions ACTUALLY imported from db/queries.ts (keep these, delete everything else): -->

From src/server/trpc/routers/entity.ts:
  getEntityById

From src/server/trpc/routers/beneficiary.ts:
  getBeneficiariesWithDistributions, getBeneficiaryById,
  markBeneficiaryDeceased, recalculateBeneficiaryShares

From src/server/trpc/routers/distribution.ts:
  getDistributions, getDistributionsByBeneficiary

From src/server/trpc/routers/hemsRequest.ts:
  approveHemsRequest, getHemsRequestsWithBeneficiary

From src/server/trpc/routers/trustAccounting.ts:
  createTrustAccountingEntry, convertIncomeToPrincipal, getUnconvertedIncomeSummary

From src/server/trpc/routers/liability.ts:
  recordLiabilityPayment, getLiabilityPayments

From src/server/trpc/routers/activityLog.ts:
  getActivityLogWithChanges, SEARCHABLE_ACTIVITY_LOG_FIELDS, searchActivityLogByField

From src/server/trpc/routers/userManagement.ts:
  createActivityLog

From src/server/trpc/routers/valuation.ts:
  getValuationsForAsset, valuationCrud

From src/server/trpc/routers/pendingInventoryItem.ts:
  pendingInventoryItemCrud, personalPropertyCrud
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead exports from db/queries.ts and consolidate TxSql type</name>
  <files>db/queries.ts, db/index.ts, src/server/trpc/routers/contact.ts</files>
  <action>
1. In db/index.ts, add the TxSql type export (move from db/queries.ts):
   ```typescript
   import type postgres from 'postgres'
   /** postgres.js TransactionSql with call signature for tagged template usage. */
   export type TxSql = postgres.TransactionSql &
       (<T extends readonly (object | undefined)[] = postgres.Row[]>(
           template: TemplateStringsArray,
           ...parameters: readonly postgres.ParameterOrFragment<never>[]
       ) => postgres.PendingQuery<T>)
   ```
   The `postgres` import already exists in db/index.ts.

2. In db/queries.ts, replace the local `type TxSql` definition (lines 13-18) with:
   `import type { TxSql } from './index'`
   Remove the existing `import type postgres from 'postgres'` since TxSql was the only consumer.

3. In src/server/trpc/routers/contact.ts, replace the local TxSql definition (lines 3, 8-14) with:
   `import { db, getClient, type TxSql } from '@/db'`
   Remove the separate `import type postgres from 'postgres'` (line 3).

4. In db/queries.ts, DELETE all exported functions that are NOT in the keep-list above. The functions to KEEP (do NOT delete these):
   - getEntityById (used by entity router)
   - getBeneficiariesWithDistributions, getBeneficiaryById, markBeneficiaryDeceased, recalculateBeneficiaryShares (beneficiary router)
   - getDistributions, getDistributionsByBeneficiary (distribution router)
   - approveHemsRequest, getHemsRequestsWithBeneficiary (hemsRequest router)
   - createTrustAccountingEntry, convertIncomeToPrincipal, getUnconvertedIncomeSummary (trustAccounting router)
   - recordLiabilityPayment, getLiabilityPayments (liability router)
   - getActivityLogWithChanges, SEARCHABLE_ACTIVITY_LOG_FIELDS, searchActivityLogByField (activityLog router)
   - createActivityLog (userManagement router)
   - getValuationsForAsset, valuationCrud (valuation router)
   - pendingInventoryItemCrud, personalPropertyCrud (pendingInventoryItem router)
   - RecordPaymentData type (used by recordLiabilityPayment)
   - MarkDeceasedData type (used by markBeneficiaryDeceased)
   - ActivityLogPaginationOptions type (used by getActivityLogs -- check if used)
   - createValuation (check if used by valuationCrud)
   - isSearchableActivityLogField (check if exported/used)
   - Any helper types or internal functions that are dependencies of kept functions

   The functions to DELETE include (but verify each):
   - getEntities, getEntityByIdLite, createEntity, updateEntity, deleteEntity
   - getBeneficiaries, createBeneficiary, updateBeneficiary, deleteBeneficiary
   - createDistribution
   - getVehicles, getVehicleById, createVehicle, updateVehicle, deleteVehicle
   - getHomesteads, getHomesteadById, createHomestead, updateHomestead, deleteHomestead
   - getRentalProperties, getRentalPropertyById, createRentalProperty, updateRentalProperty, deleteRentalProperty
   - getBankAccounts, getBankAccountById, createBankAccount, updateBankAccount, deleteBankAccount
   - getInvestmentAccounts, getInvestmentAccountById, createInvestmentAccount, updateInvestmentAccount, deleteInvestmentAccount
   - getInsurancePolicies, getInsurancePolicyById, createInsurancePolicy, updateInsurancePolicy, deleteInsurancePolicy
   - getPersonalProperties, getPersonalPropertyById, createPersonalProperty, updatePersonalProperty, deletePersonalProperty
   - getArtworks, getArtworkById, createArtwork, updateArtwork, deleteArtwork
   - getContacts, getContactById, createContact, updateContact, deleteContact
   - getTasks, getTaskById, createTask, updateTask, deleteTask
   - getTrustees, getTrusteeById, createTrustee, updateTrustee, deleteTrustee
   - getSpecificBequests, getSpecificBequestById, createSpecificBequest, updateSpecificBequest, deleteSpecificBequest
   - getTrustAccountingEntries, getTrustAccountingEntryById, updateTrustAccountingEntry, deleteTrustAccountingEntry
   - getWithdrawalRecords, getWithdrawalRecordById, createWithdrawalRecord, updateWithdrawalRecord, deleteWithdrawalRecord
   - getHemsRequests, getHemsRequestById, createHemsRequest, updateHemsRequest, deleteHemsRequest
   - getPendingHemsRequests
   - getTrusteeFeeSchedules, getTrusteeFeeScheduleById, createTrusteeFeeSchedule, updateTrusteeFeeSchedule, deleteTrusteeFeeSchedule
   - getTrusteeFeeEntries, getTrusteeFeeEntryById, createTrusteeFeeEntry, updateTrusteeFeeEntry, deleteTrusteeFeeEntry
   - getTrusteeFeeEntriesWithSchedule
   - getLiabilities, getLiabilityById, createLiability, updateLiability, deleteLiability
   - getActivityLogs (if only used internally -- verify)

   Also remove any schema table imports that are no longer referenced after deletions (e.g., if `vehicle` import is only used by deleted functions, remove it from the import block).

   IMPORTANT: Before deleting, verify each function is truly unused by searching for its name across the codebase. Some may be referenced in test files.

5. Also remove the `export type * from './schema'` line if it duplicates what `db/index.ts` already re-exports via `export * from './schema'`.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/trust-admin && bun run typecheck && bun run lint</automated>
  </verify>
  <done>db/queries.ts contains only functions that are actively imported by router files. TxSql is defined once in db/index.ts. No TypeScript errors. No lint errors.</done>
</task>

<task type="auto">
  <name>Task 2: Delete date-utils.ts and remove date-fns dependency</name>
  <files>src/lib/date-utils.ts, package.json, next.config.ts</files>
  <action>
1. Verify date-utils.ts has zero imports across the codebase (already confirmed: no file imports from `date-utils`).

2. Delete src/lib/date-utils.ts.

3. Verify date-fns has no other imports across the codebase besides date-utils.ts (already confirmed: only date-utils.ts imports from 'date-fns').

4. Remove `"date-fns": "4.1.0"` from package.json dependencies.

5. In next.config.ts, remove `'date-fns'` from the `transpilePackages` array (line 77). If the array becomes empty, remove the `transpilePackages` key entirely.

6. Run `bun install` to update the lockfile.

7. Verify the build still passes.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/trust-admin && test ! -f src/lib/date-utils.ts && ! grep -q "date-fns" package.json && bun run typecheck</automated>
  </verify>
  <done>src/lib/date-utils.ts is deleted. date-fns is not in package.json. next.config.ts no longer references date-fns. Build passes.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with zero errors
- `bun run lint` passes
- `grep -rn "date-utils\|date-fns" src/ package.json next.config.ts` returns no matches
- `grep -c "^export" db/queries.ts` shows significantly fewer exports than before (~25 vs ~100)
- TxSql is defined only in db/index.ts: `grep -rn "type TxSql" db/ src/` shows exactly one definition in db/index.ts and imports elsewhere
</verification>

<success_criteria>
- db/queries.ts exports only functions actively imported by tRPC routers
- src/lib/date-utils.ts does not exist
- date-fns is not in package.json
- TxSql type is defined once in db/index.ts, imported by db/queries.ts and contact.ts
- `bun run typecheck` and `bun run lint` pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-code-quality-cleanup/22-01-SUMMARY.md`
</output>
