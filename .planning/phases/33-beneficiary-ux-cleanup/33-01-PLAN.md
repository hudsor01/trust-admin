---
phase: 33-beneficiary-ux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kpi-strip.tsx
  - src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx
  - src/app/(admin)/beneficiaries/_components/BeneficiaryAvatarStack.tsx
  - src/app/(admin)/beneficiaries/_components/BeneficiarySortableList.tsx
  - src/app/(admin)/beneficiaries/_components/WithdrawalMilestoneGantt.tsx
  - src/server/trpc/routers/beneficiary.ts
  - tests/trpc/beneficiary-reorder.test.ts
autonomous: false
requirements: [BENE-01, BENE-02, BENE-03, BENE-04]
tags: [ui, prune, beneficiaries, kpi-strip, trpc-cleanup]

must_haves:
  truths:
    - "BENE-01: Admin /beneficiaries page renders with no avatar-stack card anywhere on the page"
    - "BENE-02: Admin /beneficiaries page renders with no 'Display Order' drag-to-reorder card"
    - "BENE-03: Admin /beneficiaries page renders with no withdrawal-milestone gantt chart"
    - "BENE-04: Beneficiary list order in the admin table, /portal, /hems-queue, and distribution views matches the pre-phase order byte-for-byte (driven by unchanged beneficiary.sortIndex + asc(sortIndex) ORDER BY)"
    - "KpiStrip shows exactly 5 tiles on /beneficiaries in the order: Beneficiary count, Total share %, Lifetime distributions, Distributions YTD, Pending HEMS"
    - "PageHeader description reads exactly: 'Trust beneficiaries with share allocations and distribution history.'"
    - "Cold-load skeleton and warm/loaded render of /beneficiaries KpiStrip both use lg:grid-cols-5 (no 4→5 tile jump on first paint)"
    - "All other KpiStrip consumers (/dashboard, /accounts, /accounting, /artwork, /firearms, /insurance, /personal-property, /properties, /vehicles, /liabilities, /hems, /hems-queue, /bequests, /trustees, /contacts) render unchanged (lg:grid-cols-4 for ≤4 tiles)"
    - "bun run typecheck exits 0; bun run lint exits 0 with zero warnings"
  artifacts:
    - path: "src/components/kpi-strip.tsx"
      provides: "KpiStrip with skeleton+loaded-state grid class derived from data.length (4 or 5)"
      contains: "import { cn } from '@/lib/utils'"
    - path: "src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx"
      provides: "Pruned admin Beneficiaries page: PageHeader + 5-tile KpiStrip + ShareDonuts + Table + dialogs"
      contains: "Lifetime distributions"
    - path: ".planning/phases/33-beneficiary-ux-cleanup/33-01-SUMMARY.md"
      provides: "Execution summary"
  key_links:
    - from: "src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx"
      to: "src/components/kpi-strip.tsx"
      via: "KpiStrip data prop with 5 KpiStripItem entries"
      pattern: "<KpiStrip data=\\{kpiData\\}"
    - from: "src/components/kpi-strip.tsx"
      to: "@/lib/utils cn helper"
      via: "named import"
      pattern: "import \\{ cn \\} from '@/lib/utils'"
    - from: "src/server/trpc/routers/beneficiary.ts"
      to: "asc(beneficiary.sortIndex) ORDER BY"
      via: "PRESERVED — beneficiary.list query untouched"
      pattern: "asc\\(beneficiary\\.sortIndex\\)"
---

<objective>
Prune the admin Beneficiaries page so only PageHeader + KpiStrip + ShareDonuts + Table + dialogs remain. Delete the avatar-stack card, the Display Order drag-reorder card, and the withdrawal-milestone gantt. Drop the now-orphan `beneficiary.reorder` tRPC procedure and its test. Add a 5th KPI ("Lifetime distributions") and fix the shared `KpiStrip` skeleton so the cold-load grid matches the loaded-state grid for 5-tile consumers. Preserve the `beneficiary.sortIndex` column, the `idx_beneficiary_entity_sort` index, and the `asc(sortIndex)` ORDER BY in `beneficiary.list` — list ordering across `/portal`, `/hems-queue`, and distribution views must be byte-for-byte unchanged.

Purpose: Closes BENE-01..04 — removes redundant UI surfaces from the Beneficiaries page while preserving the persistent sort order other pages rely on.

Output: Pruned `BeneficiariesClient.tsx`, fixed `kpi-strip.tsx`, three deleted component files, deleted `reorder` procedure + test, clean typecheck + lint, admin UAT confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-beneficiary-ux-cleanup/33-CONTEXT.md
@.planning/phases/33-beneficiary-ux-cleanup/33-PATTERNS.md
@.planning/phases/33-beneficiary-ux-cleanup/33-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/kpi-strip.tsx (existing public surface — unchanged by this phase):
```typescript
export type KpiStripItem = {
    label: string
    value: string | number
    delta?: number
    invertDelta?: boolean
    sparkline?: number[]
    icon?: LucideIcon
}

export type KpiStripProps = {
    data: KpiStripItem[]
    isLoading?: boolean
}

export function KpiStrip({ data, isLoading }: KpiStripProps): JSX.Element
```

From @/lib/utils:
```typescript
export function cn(...inputs: ClassValue[]): string  // tailwind-merge wrapper, canonical
```

From src/server/trpc/routers/beneficiary.ts (preserved — DO NOT touch):
```typescript
// beneficiary.list — ORDER BY beneficiary.sortIndex ASC stays as-is.
// asc(beneficiary.sortIndex) at line ~28 is the BENE-04 invariant.
list: adminProcedure
    .input(z.object({ entityId: z.coerce.number() }))
    .query(({ input }) => db.select().from(beneficiary)
        .where(eq(beneficiary.entityId, input.entityId))
        .orderBy(asc(beneficiary.sortIndex))),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix KpiStrip skeleton + loaded-state grid to derive from data.length (D-17)</name>
  <files>src/components/kpi-strip.tsx</files>

  <read_first>
    - .planning/phases/33-beneficiary-ux-cleanup/33-CONTEXT.md §"D-17 [MANDATORY TASK]" (lock + diff shape)
    - .planning/phases/33-beneficiary-ux-cleanup/33-PATTERNS.md §"src/components/kpi-strip.tsx (D-17 — skeleton + grid fix)" (lines 19-40 current → required shape)
    - .planning/phases/33-beneficiary-ux-cleanup/33-UI-SPEC.md §"Decisions for the Planner — KpiStrip skeleton count" (Option A locked)
    - src/components/kpi-strip.tsx in full (the file is small — ~60 lines)
    - src/components/confirm-dialog.tsx line 5 (canonical `cn` import form: `import { cn } from '@/lib/utils'`)
  </read_first>

  <action>
    Make `src/components/kpi-strip.tsx` derive the loading-skeleton tile count and the responsive column class from `data.length` so 5-tile consumers do not flash a 4-tile skeleton.

    Exact changes:

    1. Add `import { cn } from '@/lib/utils'` to the imports block. Place it grouped with the other module-internal imports (alongside `SummaryCard`); ordering follows what biome's `useSortedClasses` / import rules produce — run `bun run lint:fix` if biome reshuffles.

    2. Replace the skeleton wrapper (currently lines 22-23):
       - From: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">` followed by `{Array.from({ length: 4 }).map(...)}`
       - To: `<div className={cn("grid grid-cols-1 md:grid-cols-2 gap-4", data.length === 5 ? "lg:grid-cols-5" : "lg:grid-cols-4")}>` followed by `{Array.from({ length: data.length }).map(...)}`

    3. Replace the loaded-state wrapper (currently line 40):
       - From: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">`
       - To: `<div className={cn("grid grid-cols-1 md:grid-cols-2 gap-4", data.length === 5 ? "lg:grid-cols-5" : "lg:grid-cols-4")}>`

    DO NOT touch the `KpiStripItem` type, the `KpiStripProps` interface, the empty-state guard, the inner `SummaryCard` children, or any other prop surface. This is an internal-only refactor — public contract is unchanged.

    Scope fence (do not extend): `lg:grid-cols-5` is the only new responsive col utility introduced. Do not add `lg:grid-cols-6` or any branch for other counts — every existing consumer (DashboardClient 3, ContactsClient 4, ActivityLogClient 4, PropertiesClient 2, AccountsClient 2, PersonalPropertyClient 3, FirearmsClient 4, VehiclesClient 4, BequestsClient 4, AssetsClient 2, InsuranceClient 4, TrusteesClient 3, LiabilityKpiStrip 4) has ≤4 tiles and routes to the `lg:grid-cols-4` branch unchanged.

    Per D-13 this task MUST land before Task 2 — otherwise the first cold dev reload of `/beneficiaries` flashes a 4-tile skeleton → 5-tile loaded state.
  </action>

  <verify>
    <automated>bun run typecheck && bun run lint && grep -c "import { cn } from '@/lib/utils'" src/components/kpi-strip.tsx | grep -q '^1$' && grep -c 'lg:grid-cols-5' src/components/kpi-strip.tsx | grep -q '^2$' && grep -c 'Array.from({ length: data.length })' src/components/kpi-strip.tsx | grep -q '^1$'</automated>
  </verify>

  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 with zero warnings (CLAUDE.md: lint warnings are never pre-existing)
    - `grep -c "import { cn } from '@/lib/utils'" src/components/kpi-strip.tsx` returns exactly 1
    - `grep -c 'lg:grid-cols-5' src/components/kpi-strip.tsx` returns exactly 2 (skeleton wrapper + loaded-state wrapper)
    - `grep -c 'Array.from({ length: data.length })' src/components/kpi-strip.tsx` returns exactly 1
    - `grep -c 'Array.from({ length: 4 })' src/components/kpi-strip.tsx` returns exactly 0 (hardcoded 4 is gone)
    - `grep -F 'data.length === 5 ? "lg:grid-cols-5" : "lg:grid-cols-4"' src/components/kpi-strip.tsx` matches on both wrappers
    - `KpiStripItem` and `KpiStripProps` type definitions are unchanged from pre-edit (`git diff src/components/kpi-strip.tsx` shows no changes to those exported types)
  </acceptance_criteria>

  <done>KpiStrip skeleton renders `data.length` tiles using the same `lg:grid-cols-N` class as the loaded state for N ∈ {≤4, 5}. Typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Prune BeneficiariesClient.tsx — remove 3 imports + entityDetail query + 2 useMemos + 3 JSX blocks, rewrite PageHeader description, insert 5th KPI (D-06..D-11)</name>
  <files>src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx</files>

  <read_first>
    - .planning/phases/33-beneficiary-ux-cleanup/33-CONTEXT.md §"BeneficiariesClient.tsx edits" (D-06..D-11) and §"KPI strip change" (D-11)
    - .planning/phases/33-beneficiary-ux-cleanup/33-PATTERNS.md §"BeneficiariesClient.tsx (D-06..D-11)" — exact line refs and JSX block diffs
    - src/app/(admin)/beneficiaries/_components/BeneficiariesClient.tsx in full (300 lines)
    - src/app/(admin)/contacts/_components/ContactsClient.tsx lines 128-133 (kpiData array analog)
  </read_first>

  <action>
    Single-pass edit of `BeneficiariesClient.tsx` applying all six decisions (D-06..D-11). Atomic — do not commit a partial state.

    **D-06 — remove 3 imports** (lines 15, 18, 21 per PATTERNS.md):
    - Delete: `import { BeneficiaryAvatarStack } from './BeneficiaryAvatarStack'`
    - Delete: `import { BeneficiarySortableList } from './BeneficiarySortableList'`
    - Delete: `import { WithdrawalMilestoneGantt } from './WithdrawalMilestoneGantt'`
    - Keep `UserPlus` from `lucide-react` (line 3) — still used by the Add Beneficiary button.

    **D-07 — remove entityDetail query** (lines 42-46 per PATTERNS.md, including the jsdoc/comment immediately above the query):
    - Delete the `// Entity dod for WithdrawalMilestoneGantt reference point ...` comment.
    - Delete `const { data: entityDetail } = trpc.entity.byId.useQuery(entityId!, { enabled: !!entityId })`.
    - After removal, check whether `Card`, `CardContent`, `CardHeader`, `CardTitle` (the `@/components/ui/card` import, line 9) are still referenced anywhere in the surviving JSX. If grep on the file shows zero `<Card`, `<CardContent`, `<CardHeader`, `<CardTitle` matches after Block 2 deletion below, remove that entire import line. If any other JSX still uses Card (unlikely — BeneficiaryShareDonuts owns its own Card internally), keep it.

    **D-08 — remove avatarItems and milestoneItems useMemo blocks** (lines 167-188 per PATTERNS.md):
    - Delete the `const avatarItems = useMemo(...)` block.
    - Delete the `const milestoneItems = useMemo(...)` block.
    - PRESERVE: `totalDistributed` (lines 118-126), `totalShares` (lines 128-131), `totalDistributedYtd` (lines 135-148), `pendingHemsCount` (lines 150-153), `donutItems` (lines 155-165). All five remain in use after edits.

    **D-09 — remove 3 JSX blocks** from the surviving page render:
    - Block 1: the avatar/sidekick grid `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 items-start">…</div>` wrapping `<BeneficiaryAvatarStack beneficiaries={avatarItems} />` and the inline `{beneficiaries.length} beneficiaries · ... lifetime · ... YTD` text (PATTERNS.md lines 218-227).
    - Block 2: the `{!loading && beneficiaries.length > 1 && entityId && (<Card>…<BeneficiarySortableList .../>…</Card>)}` Display Order card (PATTERNS.md lines 234-256).
    - Block 3: the `<WithdrawalMilestoneGantt beneficiaries={milestoneItems} entityDod={entityDetail?.dod ?? null} isLoading={loading} />` element (PATTERNS.md lines 258-262).

    **D-10 — rewrite PageHeader description** (PATTERNS.md line 204):
    - Replace `description="Trust beneficiaries with share allocations and withdrawal milestones."` with exactly: `description="Trust beneficiaries with share allocations and distribution history."`
    - The trailing period is required; the rest of the PageHeader props (title, action) are untouched.

    **D-11 — insert 5th KPI at position 3** (PATTERNS.md lines 190-198):
    Replace the existing 4-item `kpiData` array with the 5-item array verbatim:
    ```
    const kpiData: KpiStripItem[] = [
        { label: 'Beneficiary count', value: beneficiaries.length },
        { label: 'Total share %', value: `${totalShares}%` },
        { label: 'Lifetime distributions', value: formatCurrency(totalDistributed) },
        { label: 'Distributions YTD', value: formatCurrency(totalDistributedYtd) },
        { label: 'Pending HEMS', value: pendingHemsCount },
    ]
    ```
    No new query, no new useMemo — `totalDistributed` is already computed at lines 118-126 of the pre-edit file. `formatCurrency` is already imported.

    After all edits, run `bun run lint:fix` to normalize whitespace. Do not introduce other style changes.
  </action>

  <verify>
    <automated>bun run typecheck && bun run lint && ! grep -E "BeneficiaryAvatarStack|BeneficiarySortableList|WithdrawalMilestoneGantt|avatarItems|milestoneItems|entityDetail" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx && grep -c "Lifetime distributions" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx | grep -q '^1$' && grep -F "Trust beneficiaries with share allocations and distribution history." src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx && ! grep -F "withdrawal milestones" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 with zero warnings
    - `grep -E "BeneficiaryAvatarStack|BeneficiarySortableList|WithdrawalMilestoneGantt" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx` returns nothing (all 3 component references removed)
    - `grep -E "avatarItems|milestoneItems|entityDetail" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx` returns nothing (all 3 derived/state references removed)
    - `grep -c "Lifetime distributions" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx` returns exactly 1
    - `grep -F "Trust beneficiaries with share allocations and distribution history." src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx` matches (D-10 verbatim)
    - `grep -F "withdrawal milestones" src/app/\(admin\)/beneficiaries/_components/BeneficiariesClient.tsx` returns nothing (old description gone)
    - The 5 KPI labels (`Beneficiary count`, `Total share %`, `Lifetime distributions`, `Distributions YTD`, `Pending HEMS`) appear in that exact order in the kpiData array — verify by reading the file and confirming index order
    - `totalDistributed`, `totalShares`, `totalDistributedYtd`, `pendingHemsCount`, `donutItems` useMemo blocks are still present in the file (`grep -E "const (totalDistributed|totalShares|totalDistributedYtd|pendingHemsCount|donutItems) = useMemo"` returns 5 lines)
  </acceptance_criteria>

  <done>BeneficiariesClient.tsx renders only PageHeader + 5-tile KpiStrip + BeneficiaryShareDonuts + BeneficiaryTable + dialogs. Description copy matches D-10 verbatim. Typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Delete 3 orphaned component files (D-01, D-02, D-03)</name>
  <files>
    src/app/(admin)/beneficiaries/_components/BeneficiaryAvatarStack.tsx
    src/app/(admin)/beneficiaries/_components/BeneficiarySortableList.tsx
    src/app/(admin)/beneficiaries/_components/WithdrawalMilestoneGantt.tsx
  </files>

  <read_first>
    - .planning/phases/33-beneficiary-ux-cleanup/33-CONTEXT.md §"Files to delete" (D-01, D-02, D-03)
    - .planning/phases/33-beneficiary-ux-cleanup/33-PATTERNS.md §"DELETE targets"
    - (No need to re-read the deletion targets — they are dead code after Task 2; the pattern map already inventoried them.)
  </read_first>

  <action>
    After Task 2 lands, the three components have zero importers. Delete them with `git rm` so the deletion shows up cleanly in the diff:

    ```
    git rm src/app/\(admin\)/beneficiaries/_components/BeneficiaryAvatarStack.tsx
    git rm src/app/\(admin\)/beneficiaries/_components/BeneficiarySortableList.tsx
    git rm src/app/\(admin\)/beneficiaries/_components/WithdrawalMilestoneGantt.tsx
    ```

    Do not delete any other file from `_components/`. The four surviving members of the directory — `BeneficiaryTable.tsx`, `BeneficiaryDialog.tsx`, `BeneficiaryDialogContent.tsx`, `AddBeneficiaryDialog.tsx`, `BeneficiaryShareDonuts.tsx`, `BeneficiarySummaryCards.tsx`, `types.ts` (and whatever else exists) — stay (D-15 scope fence).

    If a shell-escape-sensitive glob is preferred, an equivalent invocation is:
    `for f in BeneficiaryAvatarStack.tsx BeneficiarySortableList.tsx WithdrawalMilestoneGantt.tsx; do git rm "src/app/(admin)/beneficiaries/_components/$f"; done`
  </action>

  <verify>
    <automated>test ! -f 'src/app/(admin)/beneficiaries/_components/BeneficiaryAvatarStack.tsx' && test ! -f 'src/app/(admin)/beneficiaries/_components/BeneficiarySortableList.tsx' && test ! -f 'src/app/(admin)/beneficiaries/_components/WithdrawalMilestoneGantt.tsx' && bun run typecheck && bun run lint</automated>
  </verify>

  <acceptance_criteria>
    - All 3 files do not exist on disk (`test ! -f` exits 0 for each)
    - `git status` shows all 3 files as deleted (or in staged-deletion state if `git rm` was used)
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 with zero warnings
    - The directory `src/app/(admin)/beneficiaries/_components/` still contains the surviving components per D-15: BeneficiaryTable.tsx, BeneficiaryDialog.tsx, BeneficiaryDialogContent.tsx, AddBeneficiaryDialog.tsx, BeneficiaryShareDonuts.tsx, BeneficiarySummaryCards.tsx, types.ts (whichever subset already existed pre-phase remains)
  </acceptance_criteria>

  <done>Three orphan component files are removed from disk + git index; typecheck + lint clean; no other `_components/` files affected.</done>
</task>

<task type="auto">
  <name>Task 4: Delete beneficiary.reorder procedure + its test, prune orphan imports (D-04, D-05)</name>
  <files>
    src/server/trpc/routers/beneficiary.ts
    tests/trpc/beneficiary-reorder.test.ts
  </files>

  <read_first>
    - .planning/phases/33-beneficiary-ux-cleanup/33-CONTEXT.md §"Server-side cleanup" (D-04, D-05) and the §"Scope fence" within D-04
    - .planning/phases/33-beneficiary-ux-cleanup/33-PATTERNS.md §"src/server/trpc/routers/beneficiary.ts (D-04)" — exact line refs (PATTERNS.md says lines 111-156; CONTEXT.md says 113-167 — read the file and locate by the jsdoc sentinel + the `reorder: adminProcedure` block, not by line number alone)
    - src/server/trpc/routers/beneficiary.ts in full (read once, identify the `reorder` block and the import line at the top)
    - tests/trpc/beneficiary-reorder.test.ts (read briefly to confirm it only tests the reorder procedure — no shared fixtures consumed by other test files)
  </read_first>

  <action>
    Two edits, atomic commit:

    1. **Delete the `reorder` procedure** from `src/server/trpc/routers/beneficiary.ts`. The block spans from the jsdoc comment opening (`/**` describing the new display order) through the closing `}),` of the `.mutation(...)` call, immediately before the `me: beneficiaryProcedure.query(...)` procedure. Remove the jsdoc + the entire `reorder: adminProcedure.input(...).mutation(async ({ input }) => { ... })` block + the trailing comma.

    2. **Check whether `getClient` and `TxSql` imports become orphaned.** Per PATTERNS.md scope fence: after deleting `reorder`, grep the remainder of `beneficiary.ts` for `getClient(` and `TxSql` references. If neither identifier appears anywhere else in the file, remove them from the import line (e.g. if the import reads `import { db, getClient, type TxSql } from '@/db'`, replace with `import { db } from '@/db'`). If either identifier is still referenced, leave the import line intact. Do NOT touch any other import.

    3. **Delete the test file:** `git rm tests/trpc/beneficiary-reorder.test.ts`.

    Scope fences (do not violate):
    - DO NOT touch `beneficiary.list` (the `asc(beneficiary.sortIndex)` ORDER BY is the BENE-04 invariant)
    - DO NOT touch `beneficiary.create`, `beneficiary.update`, `beneficiary.delete`, `beneficiary.byId`, `beneficiary.me`, `beneficiary.listWithDistributions`, or any other procedure
    - DO NOT touch `db/schema.ts` (the `beneficiary.sortIndex` column stays)
    - DO NOT touch migrations
    - DO NOT touch the `idx_beneficiary_entity_sort` index

    Per D-04 the procedure performs no `activity_log` writes — no audit trail cleanup is needed.
  </action>

  <verify>
    <automated>bun run typecheck && bun run lint && ! grep -rn "beneficiary\\.reorder\\|utils\\.beneficiary\\.reorder\\|trpc\\.beneficiary\\.reorder" src/ tests/ && test ! -f tests/trpc/beneficiary-reorder.test.ts && grep -c "asc(beneficiary.sortIndex)" src/server/trpc/routers/beneficiary.ts | grep -q '^1$'</automated>
  </verify>

  <acceptance_criteria>
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0 with zero warnings
    - `grep -rn "beneficiary\\.reorder\\|utils\\.beneficiary\\.reorder\\|trpc\\.beneficiary\\.reorder" src/ tests/` returns nothing (zero consumers)
    - `tests/trpc/beneficiary-reorder.test.ts` does not exist on disk
    - `grep -c "asc(beneficiary.sortIndex)" src/server/trpc/routers/beneficiary.ts` returns exactly 1 (the `list` procedure's ORDER BY is preserved — BENE-04 invariant)
    - `grep "beneficiary.list" src/server/trpc/routers/beneficiary.ts` shows the `list:` procedure unchanged
    - If `getClient` and `TxSql` imports were removed from the top of `beneficiary.ts`, `grep -E "getClient|TxSql" src/server/trpc/routers/beneficiary.ts` returns nothing; if they were kept (still in use elsewhere), the matches that remain correspond to non-`reorder` call sites
    - `bun test tests/trpc` runs to completion without referencing `beneficiary-reorder.test.ts` (file is gone; no other test imports its fixtures)
  </acceptance_criteria>

  <done>`beneficiary.reorder` procedure is gone, `tests/trpc/beneficiary-reorder.test.ts` is gone, orphan imports pruned, `beneficiary.list` ORDER BY preserved. Typecheck + lint + tRPC test suite clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Admin UAT — verify BENE-01..04 + KpiStrip non-regression sweep</name>
  <files>(no files — visual verification only; Tasks 1-4 already shipped all code changes)</files>

  <action>Pause for admin UAT. Do not proceed past this task without explicit "approved" response from the user. See `<how-to-verify>` for the 26-step verification walkthrough.</action>

  <what-built>
    All four BENE requirements are implemented in code:
    - BENE-01: avatar-stack card removed from /beneficiaries (Task 2 + Task 3)
    - BENE-02: Display Order drag card removed; `beneficiary.reorder` tRPC procedure deleted (Tasks 2, 3, 4)
    - BENE-03: WithdrawalMilestoneGantt removed (Tasks 2, 3)
    - BENE-04: `beneficiary.sortIndex` column + `asc(sortIndex)` ORDER BY preserved (no schema or query change)
    - KpiStrip shows 5 tiles on /beneficiaries; skeleton matches loaded state for 5-tile consumers
    - D-17 cross-page non-regression: all other KpiStrip consumers (2/3/4-tile strips) keep `lg:grid-cols-4` behavior

    Static gates already passed in Tasks 1-4: `bun run typecheck` exit 0, `bun run lint` exit 0.

    This checkpoint is the visual confirmation step.
  </what-built>

  <how-to-verify>
    Bring the app up locally (`bun run dev`) and sign in as admin (the `ADMIN_EMAIL` account). Walk through these screens in order:

    **A. /beneficiaries — primary acceptance (BENE-01..04 + new KPI):**
    1. Navigate to `/beneficiaries`.
    2. PageHeader description reads exactly: `Trust beneficiaries with share allocations and distribution history.`
    3. KPI strip shows exactly 5 tiles in this left-to-right order: `Beneficiary count`, `Total share %`, `Lifetime distributions`, `Distributions YTD`, `Pending HEMS`. The "Lifetime distributions" value is a currency string ($X,XXX.XX).
    4. Cold reload (hard refresh, Cmd+Shift+R): the skeleton tiles appear in a 5-tile grid on `lg` screens (matches the loaded state — no 4-to-5 layout jump).
    5. Page contains: PageHeader → KPI strip → BeneficiaryShareDonuts (allocation donut chart(s)) → BeneficiaryTable. Nothing else.
    6. **NOT visible anywhere on the page:**
       - Any avatar grid / avatar-stack ("circle of beneficiary initials") — BENE-01
       - A "Display Order" Card with drag handles or grip icons — BENE-02
       - A withdrawal-milestone Gantt chart — BENE-03
    7. The "Add Beneficiary" button still works (opens AddBeneficiaryDialog).
    8. Click a row in BeneficiaryTable — BeneficiaryDialog opens; closing it leaves the page intact.

    **B. /portal, /hems-queue, distributions — BENE-04 (ordering invariant):**
    9. Sign out, sign in as a beneficiary (or open `/portal` as the beneficiary test user). The beneficiary's own record displays in the same order they appeared in pre-phase (if multiple beneficiaries are visible there). Sign back in as admin.
    10. Navigate to `/hems-queue`. If there's a beneficiary filter or column showing beneficiary names, the order matches the order seen in step 5's BeneficiaryTable.
    11. Navigate to any view that lists distributions grouped or filtered by beneficiary. Beneficiary order matches step 5.

    **C. KpiStrip cross-page non-regression (D-17 scope fence):**
    Eyeball each page below — none of them should show a regression. KpiStrip tiles fit on one row on `lg+` screens (their existing `lg:grid-cols-4` layout, unchanged because `data.length` is 2/3/4, not 5):
    12. `/dashboard`
    13. `/accounts`
    14. `/accounting`
    15. `/artwork`
    16. `/firearms`
    17. `/insurance`
    18. `/personal-property`
    19. `/properties`
    20. `/vehicles`
    21. `/liabilities`
    22. `/hems`
    23. `/hems-queue`
    24. `/bequests`
    25. `/trustees`
    26. `/contacts`

    Expected: each page's KpiStrip renders the same number of tiles as it did before this phase, in the same grid columns, with no layout shift.

    **Pass = all 26 checks pass.** If any check fails, capture the URL + the failing condition and stop.
  </how-to-verify>

  <verify>
    <automated>echo "checkpoint:human-verify — visual UAT; no automated assertion at this gate. Tasks 1-4 already enforce typecheck + lint exit 0 + grep gates."</automated>
  </verify>

  <acceptance_criteria>
    - All 26 visual checks (A.1-A.8, B.9-B.11, C.12-C.26) pass when reviewed by admin user
    - User explicitly types "approved" in the resume signal
  </acceptance_criteria>

  <done>User has typed "approved"; BENE-01..04 are visually confirmed; no other KpiStrip consumer regressed.</done>

  <resume-signal>Type "approved" if all 26 checks pass. Otherwise describe which check failed (which page, which symptom, screenshot if possible).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC | Admin clients call `trpc.beneficiary.*` procedures over the tRPC link; all procedures touched are `adminProcedure` (admin/trustee/arbiter only) |
| tRPC → Postgres | Drizzle queries against `beneficiary` table; RLS policies enforce `app.is_admin()` for writes, scoped reads for admins |

No new trust boundaries introduced. This phase is a strict-subtractive pure-UI prune plus a delete-only tRPC change; it removes a mutation procedure (`beneficiary.reorder`) but adds none.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | beneficiary.sortIndex column / asc(sortIndex) ORDER BY | mitigate | DO NOT touch `db/schema.ts` `sortIndex` column, `idx_beneficiary_entity_sort` composite index, or the `asc(beneficiary.sortIndex)` clause in `beneficiary.list` (Task 4 scope fence). BENE-04 acceptance covers regression; UAT Task 5 step B verifies cross-page ordering invariant |
| T-33-02 | Denial of Service | client breakage from dangling import | mitigate | Execution order D-13 (Task 1 → Task 2 → Task 3): edit `kpi-strip.tsx`, then edit `BeneficiariesClient.tsx` to drop imports, THEN delete component files. No transient broken-state commit. Typecheck gate after each task |
| T-33-03 | Elevation of Privilege | beneficiary.reorder procedure | accept | Procedure is being DELETED in Task 4. After deletion the attack surface shrinks (one fewer admin-gated mutation). No new procedure added |
| T-33-04 | Information Disclosure | PageHeader description copy change | accept | Copy is non-sensitive marketing/descriptive text; no PII or system internals exposed. D-10 string is reviewed verbatim |
| T-33-05 | Repudiation | activity_log impact | accept | The `beneficiary.reorder` procedure (per D-04) wrote no `activity_log` entries — deleting it removes no audit history. Audit trail for `beneficiary.create/update/delete` is untouched |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO new packages — pure deletion plus one existing `cn` helper import. No package-legitimacy gate required. RESEARCH.md package audit not applicable (no install tasks exist in this plan) |

**Severity assessment:** No `high` severity threats. All threats are `low` (either mitigated by the execution-order discipline already locked in D-13, or "accept" because the disposition is a strict reduction of attack surface). Phase proceeds.
</threat_model>

<verification>
After Task 5 approval:
- `bun run typecheck` exits 0 (confirmed in Tasks 1-4)
- `bun run lint` exits 0 with zero warnings (confirmed in Tasks 1-4)
- All 26 admin UAT checks pass (Task 5)
- Git diff shows: `kpi-strip.tsx` modified, `BeneficiariesClient.tsx` modified, `beneficiary.ts` modified, 3 component files deleted, 1 test file deleted. No other files changed.
- `db/schema.ts` unchanged (BENE-04 column-preservation invariant)
- No new dependencies in `package.json` (pure deletion phase)
</verification>

<success_criteria>
- BENE-01: /beneficiaries renders with no avatar-stack card (UAT step A.6, BeneficiaryAvatarStack.tsx deleted)
- BENE-02: /beneficiaries renders with no Display Order drag-to-reorder card (UAT step A.6, BeneficiarySortableList.tsx deleted, `beneficiary.reorder` procedure deleted)
- BENE-03: /beneficiaries renders with no withdrawal-milestone gantt (UAT step A.6, WithdrawalMilestoneGantt.tsx deleted)
- BENE-04: beneficiary ordering in admin table, /portal, /hems-queue, and distribution views matches pre-phase order (UAT steps B.9-11; `beneficiary.list` ORDER BY untouched; `beneficiary.sortIndex` column + composite index preserved)
- 5-tile KpiStrip on /beneficiaries with correct labels in correct order (UAT step A.3)
- PageHeader description matches D-10 verbatim (UAT step A.2)
- KpiStrip cold-load skeleton matches loaded state for 5-tile case (UAT step A.4)
- D-17 non-regression: all other KpiStrip consumers render unchanged (UAT step C.12-26)
- Static gates clean: typecheck + lint exit 0 (Tasks 1-4)
</success_criteria>

<output>
Create `.planning/phases/33-beneficiary-ux-cleanup/33-01-SUMMARY.md` when done. The summary records:
- Files modified (kpi-strip.tsx, BeneficiariesClient.tsx, beneficiary.ts)
- Files deleted (3 components + 1 test)
- Whether `getClient`/`TxSql` imports were pruned from beneficiary.ts (yes/no)
- UAT approval timestamp
- Any deviations from the plan (expected: none — this phase is fully locked)
</output>
