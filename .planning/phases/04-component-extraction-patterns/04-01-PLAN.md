---
phase: 04-component-extraction-patterns
plan: 01
type: execute
---

<objective>
Extract generic ResourceDialog component with form state management to eliminate duplication across pages.

Purpose: Reduce 76 Dialog instances across 13 pages by creating reusable form dialog pattern with consistent behavior.
Output: Generic ResourceDialog component and FormDialog helper that standardizes create/edit workflows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-notification-system/03-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/pages/Liabilities.tsx
@src/pages/Properties.tsx
@src/components/ui/dialog.tsx

**Tech stack available:**
- Radix UI Dialog (already in use)
- shadcn/ui components
- React 19.2.3
- TailwindCSS 4.1.18

**Established patterns:**
- Component files use kebab-case
- Dialog max height: `max-h-[90vh] overflow-y-auto`
- Form state pattern: separate `editing` state and `form` data
- Event handlers: `handleEdit`, `handleAdd`, `handleSave`
- Import organization: React → External → Internal

**Constraining decisions:**
- Keep current tech stack (no new libraries)
- Maintain existing shadcn/ui Dialog API compatibility
- Use TypeScript generics for type safety

**Issues being addressed:**
- Large component files (>800 lines) - CONCERNS.md line 14-19
- Form dialog duplication across 13 pages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResourceDialog generic wrapper component</name>
  <files>src/components/resource-dialog.tsx</files>
  <action>Create ResourceDialog component accepting generic type parameter <T>. Props: open (boolean), onOpenChange (callback), title (string), children (React.ReactNode), onSubmit (callback), submitLabel (string, default "Save"), isLoading (boolean, default false). Use shadcn Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter. Content max height: `max-h-[90vh] overflow-y-auto`. Footer has Cancel and Submit buttons. Submit button shows Loader2 spinner when isLoading. Export as named export. Follow established component structure: imports, types, component function, export.</action>
  <verify>TypeScript compiles without errors. Component can be imported: `import { ResourceDialog } from "@/components/resource-dialog"`</verify>
  <done>ResourceDialog component created with proper TypeScript generics, loading states, and consistent Dialog styling</done>
</task>

<task type="auto">
  <name>Task 2: Create useResourceForm hook for form state management</name>
  <files>src/hooks/use-resource-form.ts</files>
  <action>Create useResourceForm<T> hook that encapsulates form state pattern. Accepts: initialData (T | null), onSubmit (async (data: T) => void). Returns: { isOpen, open, close, form, setForm, isEditing, handleEdit(item: T), handleAdd(), handleSave(), isSubmitting }. Internal state: `isOpen`, `editing`, `form`, `isSubmitting`. `handleEdit` populates form from item and sets isEditing=true. `handleAdd` resets to initialData. `handleSave` calls onSubmit, handles loading state, closes on success. Export as named export. Add to src/hooks/index.ts barrel export.</action>
  <verify>TypeScript compiles. Hook can be imported: `import { useResourceForm } from "@/hooks"`. No runtime errors when mounting.</verify>
  <done>useResourceForm hook created with proper TypeScript generics, encapsulates form state pattern used across pages</done>
</task>

<task type="auto">
  <name>Task 3: Document usage pattern with example</name>
  <files>src/components/resource-dialog.tsx</files>
  <action>Add JSDoc comment to ResourceDialog component with usage example showing: useResourceForm integration, ResourceDialog wrapper, form fields in children, type safety. Example: type Liability = {...}, const { isOpen, open, close, form, setForm, handleEdit, handleAdd, handleSave } = useResourceForm<Liability>({ creditor: "" }), ResourceDialog with onSubmit=handleSave. Format: @example tag with code block. Keep example concise (10-15 lines).</action>
  <verify>JSDoc renders correctly in IDE hover tooltips. Example code is syntactically valid TypeScript.</verify>
  <done>ResourceDialog documented with clear usage example showing integration with useResourceForm hook</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compilation passes without errors
- [ ] ResourceDialog component can be imported and type-checked
- [ ] useResourceForm hook can be imported from @/hooks
- [ ] JSDoc example is valid and helpful
- [ ] No existing functionality broken (dev server runs)
</verification>

<success_criteria>

- ResourceDialog component created with TypeScript generics
- useResourceForm hook created with form state management pattern
- Both components follow established conventions (kebab-case, import organization)
- JSDoc documentation with usage example
- Ready for adoption in Plan 04-02 (refactoring pages to use new components)
</success_criteria>

<output>
After completion, create `.planning/phases/04-component-extraction-patterns/04-01-SUMMARY.md`:

# Phase 4 Plan 01: Extract Resource Dialog Component Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Created ResourceDialog generic wrapper component
- Created useResourceForm hook for form state management
- Documented usage pattern with example
- Foundation for eliminating 76 Dialog instances across pages

## Files Created/Modified

- `src/components/resource-dialog.tsx` - Generic dialog wrapper
- `src/hooks/use-resource-form.ts` - Form state hook
- `src/hooks/index.ts` - Added barrel export

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 04-02: Extract SummaryCard component for metric displays
</output>
