---
phase: 04-component-extraction-patterns
plan: 03
type: execute
---

<objective>
Extract DataTable component with inline editing support to standardize table patterns across pages.

Purpose: Create reusable table component that supports inline editing, sorting, and consistent styling used across all resource pages.
Output: Generic DataTable component with column configuration and inline editing cell support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-extraction-patterns/04-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/pages/Liabilities.tsx
@src/components/editable-cells.tsx
@src/components/ui/table.tsx

**Tech stack available:**
- shadcn/ui Table components
- Existing EditableCell components (text, currency, select, date, number, percent)
- lucide-react icons
- React 19.2.3
- TailwindCSS 4.1.18

**Established patterns:**
- Component files use kebab-case
- Inline editing using EditableCell components
- Action buttons (Edit, Delete) in rightmost column
- Badge components for status displays
- Empty state messaging when no data

**Constraining decisions:**
- Leverage existing EditableCell components (don't rebuild)
- Keep shadcn/ui Table components as foundation
- Support column configuration with render functions
- Maintain existing inline editing behavior

**Issues being addressed:**
- Repetitive table rendering across pages
- Inconsistent table styling and behavior
- Duplication of action column patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataTable component with column configuration</name>
  <files>src/components/data-table.tsx</files>
  <action>Create DataTable<T> generic component. Props: data (T[]), columns (ColumnDef<T>[]), onEdit (optional (item: T) => void), onDelete (optional (item: T) => void), emptyMessage (optional string, default "No data available"), isLoading (optional boolean). ColumnDef<T> interface: { key: string, header: string, render?: (item: T) => React.ReactNode, sortable?: boolean }. Use shadcn Table, TableHeader, TableBody, TableRow, TableCell. If no render function, display item[key]. If onEdit or onDelete provided, add actions column with Edit/Delete buttons (Pencil and Trash2 icons from lucide-react). Empty state shows emptyMessage centered. Loading shows Skeleton rows. Export as named export with ColumnDef type export.</action>
  <verify>TypeScript compiles. Component can be imported: `import { DataTable, type ColumnDef } from "@/components/data-table"`. Renders table with sample data and columns.</verify>
  <done>DataTable component created with generic typing, column configuration, action buttons, empty/loading states</done>
</task>

<task type="auto">
  <name>Task 2: Add sorting support to DataTable</name>
  <files>src/components/data-table.tsx</files>
  <action>Add sorting state to DataTable: useState for sortKey and sortDirection (asc/desc). For columns with sortable=true, TableHead shows clickable header with sort icon (ArrowUpDown from lucide-react). Click toggles direction or activates sort. Sort data before rendering: use array.sort() with comparison based on sortKey and sortDirection. Handle string, number, date types. Preserve original data order when no sort active. Add visual indicator (ChevronUp/ChevronDown) for active sort column.</action>
  <verify>Clicking sortable column headers reorders table rows. Toggle between asc/desc/none. TypeScript compiles without errors.</verify>
  <done>DataTable has working column sorting with visual indicators and type-aware comparison</done>
</task>

<task type="auto">
  <name>Task 3: Add JSDoc documentation with usage example</name>
  <files>src/components/data-table.tsx</files>
  <action>Add JSDoc comment to DataTable with: component description, @param for each prop, @example showing: type definition, column configuration with render functions, EditableCell integration, onEdit/onDelete handlers. Example demonstrates: basic string column, formatted number column with render function, status badge column, inline editable column using EditableCurrencyCell. Keep example realistic but concise (20-25 lines). Format: JSDoc comment block above component function.</action>
  <verify>JSDoc renders correctly in IDE hover tooltips. Example code is valid TypeScript and demonstrates common table patterns.</verify>
  <done>DataTable documented with comprehensive usage example showing column configuration and inline editing integration</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compilation passes without errors
- [ ] DataTable renders with sample data and columns
- [ ] Sorting works for string, number, and date columns
- [ ] Action buttons (Edit/Delete) trigger callbacks
- [ ] Empty and loading states display correctly
- [ ] JSDoc examples are valid and helpful
- [ ] No existing functionality broken (dev server runs)
</verification>

<success_criteria>

- DataTable component created with TypeScript generics
- Column configuration with render functions and sortable support
- Action buttons integrated (Edit/Delete)
- Empty and loading states implemented
- Sorting functionality working
- JSDoc documentation with usage example
- Ready for adoption in page refactors (Phase 5-7)
</success_criteria>

<output>
After completion, create `.planning/phases/04-component-extraction-patterns/04-03-SUMMARY.md`:

# Phase 4 Plan 03: Extract DataTable Component Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Created DataTable generic component with column configuration
- Implemented sorting support with visual indicators
- Integrated action buttons (Edit/Delete) pattern
- Added empty and loading states
- Documented usage patterns with example

## Files Created/Modified

- `src/components/data-table.tsx` - Generic table component with sorting

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 04-04: Document component patterns and usage examples
</output>
