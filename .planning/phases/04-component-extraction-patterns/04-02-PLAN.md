---
phase: 04-component-extraction-patterns
plan: 02
type: execute
---

<objective>
Extract SummaryCard component for metric displays to standardize dashboard-style summary cards across pages.

Purpose: Eliminate duplication of Card-based metric displays appearing on most pages (Properties, Accounting, Liabilities, etc.).
Output: Reusable SummaryCard component with optional trend indicators, loading states, and consistent styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-extraction-patterns/04-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/pages/Properties.tsx
@src/pages/Accounting.tsx
@src/pages/Liabilities.tsx
@src/components/ui/card.tsx

**Tech stack available:**
- shadcn/ui Card components
- lucide-react icons
- formatCurrency, formatPercentage utilities
- React 19.2.3
- TailwindCSS 4.1.18

**Established patterns:**
- Component files use kebab-case
- Card with CardContent for metric displays
- Grid layouts for multiple cards (2-4 column grids)
- Import organization: React → External → Internal

**Constraining decisions:**
- Use existing Card components from shadcn/ui
- Maintain current visual design (no styling changes)
- Support both numeric and text values
- Optional trend/change indicators

**Issues being addressed:**
- Repetitive Card/CardContent structures across pages
- Inconsistent metric display formatting
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryCard component</name>
  <files>src/components/summary-card.tsx</files>
  <action>Create SummaryCard component with props: title (string), value (string | number), icon (optional LucideIcon), trend (optional object: { value: number, isPositive: boolean }), isLoading (optional boolean), formatter (optional function: (value: number) => string). Use shadcn Card and CardContent. Layout: flex with icon (if provided), title, value, trend (if provided). Value formatted via formatter if provided and value is number. Loading shows Skeleton from shadcn/ui. Trend displays with up/down arrow (TrendingUp/TrendingDown from lucide-react) and green/red color based on isPositive. Export as named export. Follow conventions: kebab-case file, double quotes, 2-space indent.</action>
  <verify>TypeScript compiles. Component can be imported: `import { SummaryCard } from "@/components/summary-card"`. Renders without errors when passed required props.</verify>
  <done>SummaryCard component created with optional icon, trend indicator, loading states, and value formatting</done>
</task>

<task type="auto">
  <name>Task 2: Create SummaryCardGrid layout component</name>
  <files>src/components/summary-card-grid.tsx</files>
  <action>Create SummaryCardGrid component as simple wrapper for grid layout. Props: children (React.ReactNode), columns (optional number, default 3). Uses Tailwind grid classes: `grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-{columns}`. Responsive: 1 column on mobile, 2 on tablet, specified columns on desktop. Export as named export. Simple, focused component for consistent grid layouts.</action>
  <verify>TypeScript compiles. Component can be imported: `import { SummaryCardGrid } from "@/components/summary-card-grid"`. Renders grid correctly with responsive classes.</verify>
  <done>SummaryCardGrid component created for consistent responsive grid layouts across pages</done>
</task>

<task type="auto">
  <name>Task 3: Add JSDoc documentation with usage examples</name>
  <files>src/components/summary-card.tsx</files>
  <action>Add JSDoc comments to SummaryCard with: component description, @param for each prop explaining purpose, @example showing basic usage (title + value), @example showing advanced usage (icon + trend + formatter). Keep examples concise (5-7 lines each). Format: JSDoc comment block above component function. Show formatCurrency usage in advanced example.</action>
  <verify>JSDoc renders correctly in IDE hover tooltips. Example code is valid TypeScript and demonstrates common use cases.</verify>
  <done>SummaryCard documented with clear usage examples for basic and advanced scenarios</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compilation passes without errors
- [ ] SummaryCard renders correctly with all prop combinations
- [ ] SummaryCardGrid applies responsive grid classes correctly
- [ ] JSDoc examples are valid and helpful
- [ ] No existing functionality broken (dev server runs)
</verification>

<success_criteria>

- SummaryCard component created with icon, trend, loading, formatter support
- SummaryCardGrid component created for consistent layouts
- Both components follow established conventions
- JSDoc documentation with usage examples
- Ready for adoption in page refactors (Phase 5-7)
</success_criteria>

<output>
After completion, create `.planning/phases/04-component-extraction-patterns/04-02-SUMMARY.md`:

# Phase 4 Plan 02: Extract SummaryCard Component Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Created SummaryCard component with trend indicators and formatting
- Created SummaryCardGrid for responsive layouts
- Documented usage patterns with examples
- Foundation for standardizing metric displays across pages

## Files Created/Modified

- `src/components/summary-card.tsx` - Metric display component
- `src/components/summary-card-grid.tsx` - Grid layout wrapper

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 04-03: Extract DataTable component with inline editing support
</output>
