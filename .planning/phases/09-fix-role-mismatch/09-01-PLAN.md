---
phase: 09-fix-role-mismatch
plan: 01
type: execute
---

<objective>
Fix the broken beneficiaryProcedure by using userProfile.role as the source of truth instead of Neon Auth's native role. Neon Auth only supports "admin" and "user" but the app needs "beneficiary" for fine-grained authorization.

Purpose: Unblock ALL beneficiary portal features — currently 4 procedures are completely broken (beneficiary.me, hemsRequest.submit, hemsRequest.myRequests, distribution.myDistributions).
Output: Working role system where userProfile.role drives tRPC authorization and Neon Auth role drives layout guards.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-fix-role-mismatch/CONTEXT.md

# Key source files:
@src/server/trpc/index.ts
@src/lib/auth.ts
@db/schema.ts

**Constraining decisions:**
- Neon Auth native role only supports "admin" and "user" — cannot set "beneficiary"
- userProfile table already has role column with enum ('admin', 'beneficiary')
- Layout guards (admin layout, portal layout) use Neon Auth session.user.role and work correctly — DO NOT change them
- beneficiaryProcedure and isBeneficiary() are the only broken checks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tRPC context to use userProfile.role as source of truth</name>
  <files>src/server/trpc/index.ts</files>
  <action>
  In createContext(), the code already queries userProfile to get beneficiaryId. Modify that query to ALSO select userProfile.role.

  Then when building the appUser object, use userProfile.role instead of session.user.role:
  - If userProfile exists: use profile.role (will be "admin" or "beneficiary")
  - If no userProfile exists but session.user.role is "admin": use "admin" (backwards compat for admins without profile)
  - If no userProfile and not admin: use "user" as fallback (safe default — no beneficiary access)

  DO NOT change adminProcedure — it checks ctx.user.role === 'admin' which will still work since admin userProfiles have role='admin'.
  DO NOT change beneficiaryProcedure's check logic — it checks role !== 'beneficiary' which will now work because userProfile.role returns 'beneficiary'.
  DO NOT change layout guards — they read session.user.role from Neon Auth directly, not ctx.user.role.
  </action>
  <verify>bun run typecheck passes with no errors</verify>
  <done>createContext reads role from userProfile table. appUser.role is "admin" or "beneficiary" (from userProfile) or "user" (fallback). beneficiaryProcedure will now accept users with userProfile.role === "beneficiary".</done>
</task>

<task type="auto">
  <name>Task 2: Fix isBeneficiary type guard and AppUser type</name>
  <files>src/lib/auth.ts</files>
  <action>
  1. Verify AppUser type definition allows 'admin' | 'beneficiary' (it should already — the UserRole enum is ('admin', 'beneficiary')). If the type also allows 'user' as a fallback value, ensure it does: role should be 'admin' | 'beneficiary' | 'user' to handle the no-profile fallback.

  2. Verify isBeneficiary() function checks role === 'beneficiary' AND beneficiaryId is truthy. This should already be correct — the fix is in Task 1 (context now provides the right role value). But confirm the function works end-to-end.

  3. Run typecheck to ensure all type assertions are valid with the updated role values.
  </action>
  <verify>bun run typecheck passes. Grep for "role === 'beneficiary'" shows only expected locations (beneficiaryProcedure in trpc/index.ts, isBeneficiary in auth.ts).</verify>
  <done>AppUser type accepts 'admin' | 'beneficiary' | 'user'. isBeneficiary() returns true for users with userProfile.role === 'beneficiary' and a valid beneficiaryId. All TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run typecheck` passes without errors
- [ ] `bun run build` succeeds
- [ ] Grep confirms: no code checks for role === "user" in authorization contexts (only in fallback assignment)
- [ ] Grep confirms: beneficiaryProcedure checks role !== 'beneficiary' (unchanged)
- [ ] Grep confirms: adminProcedure checks role === 'admin' (unchanged)
- [ ] Layout guards remain unchanged (use session.user.role from Neon Auth)
</verification>

<success_criteria>

- createContext() reads role from userProfile.role
- beneficiaryProcedure accepts users with userProfile.role === 'beneficiary'
- adminProcedure still works for admins
- Layout guards unchanged and working
- TypeScript compiles cleanly
- No regressions in build
</success_criteria>

<output>
After completion, create `.planning/phases/49-fix-role-mismatch/49-01-SUMMARY.md`:

# Phase 49 Plan 01: Fix Role Mismatch Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [file] - [description]

## Decisions Made
- [decisions]

## Issues Encountered
- [issues or "None"]

## Next Step
Phase 49 complete, ready for Phase 50 (enable-email-password)
</output>
