---
phase: 03-error-notification-system
plan: 02
type: execute
---

<objective>
Create error notification hook and integrate with query factory to automatically display API errors as toast notifications.

Purpose: Show users clear error messages when API requests fail, replacing silent console.error() logging.
Output: Error notifications automatically displayed for all API failures throughout the application.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-notification-system/03-01-SUMMARY.md
@src/hooks/use-query.ts
@src/lib/api-error.ts

**Tech stack available:**
- sonner (toast library - installed in Plan 03-01)
- ApiError class with typed error codes

**Established patterns:**
- Hooks in src/hooks/ with `use` prefix
- Error handling: try/catch, console.error for logging
- Import organization: React → External → Internal

**Constraining decisions:**
- Keep hooks simple and focused (single responsibility)
- Errors currently logged via console.error (line 78 in use-query.ts)
- ApiError class exists with structured error information

**Issues being addressed:**
- Error state stored but not displayed to users (CONCERNS.md)
- Users don't see failed requests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useToastError hook for error formatting</name>
  <files>src/hooks/use-toast-error.ts</files>
  <action>Create useToastError hook that wraps toast.error() with intelligent error formatting. Function signature: `const showError = useToastError()`. Inside, return a function that accepts `(error: unknown)` and displays formatted toast. Check if error is ApiError instance (has .code, .message, .details), and format accordingly: title=message, description=details or error code. For generic Error objects, show error.message. For unknown types, show "An unexpected error occurred". Use toast.error() from sonner with 5-second duration. Export as named export.</action>
  <verify>TypeScript compiles, no errors. Hook can be imported: `import { useToastError } from '@/hooks/use-toast-error'`</verify>
  <done>useToastError hook created, handles ApiError and generic Error types, formats for toast display</done>
</task>

<task type="auto">
  <name>Task 2: Integrate error notifications with query factory</name>
  <files>src/hooks/use-query.ts</files>
  <action>Replace console.error(err) at line 78 with toast notification. Import { toast } from 'sonner' at top. In catch block, check error type: if ApiError with details, call toast.error(err.message, { description: err.details }). If Error with message, call toast.error(err.message). Otherwise, call toast.error('An unexpected error occurred'). Keep console.error(err) for debugging but add toast for user visibility. This ensures all API errors from useLiabilities, useEntities, etc. automatically show toasts.</action>
  <verify>No TypeScript errors. Test by triggering API error (stop server, trigger request). Toast should appear top-right with error message.</verify>
  <done>All query hook API errors automatically display toast notifications, error messages visible to users</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run dev` starts without errors
- [ ] TypeScript compilation passes
- [ ] Manual test: Stop API server, trigger action in UI, verify toast appears with error message
- [ ] Console still logs errors for debugging
</verification>

<success_criteria>

- useToastError hook created with error formatting logic
- Query factory integrated with toast notifications
- API errors display to users automatically
- Both ApiError and generic Error types handled
- Console.error preserved for debugging
- Ready for error boundary implementation (Plan 03-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-notification-system/03-02-SUMMARY.md`:

# Phase 3 Plan 02: Create Error Notification Hook Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Created useToastError hook with intelligent error formatting
- Integrated toast notifications into query factory
- All API errors now visible to users

## Files Created/Modified

- `src/hooks/use-toast-error.ts` - Error formatting hook
- `src/hooks/use-query.ts` - Integrated toast notifications

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 03-03: Add error boundary for React component crashes
</output>
