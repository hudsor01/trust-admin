---
phase: 03-error-notification-system
plan: 03
type: execute
---

<objective>
Add React Error Boundary to catch component crashes and display user-friendly fallback UI instead of blank white screen.

Purpose: Gracefully handle component rendering errors and provide recovery mechanism for users.
Output: Error boundary wrapping application with styled fallback UI and error reset functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-notification-system/03-02-SUMMARY.md
@src/main.tsx
@src/components/app-sidebar.tsx

**Tech stack available:**
- sonner (toast library with Toaster configured)
- React 19.2.3
- TailwindCSS 4.1.18

**Established patterns:**
- Component files use kebab-case
- Tailwind utility classes for styling
- Components in src/components/
- Import organization: React → External → Internal

**Constraining decisions:**
- Keep UI simple and consistent with existing design
- Error boundary different from toast notifications: catches component crashes, not async errors
- Phase 3 complete after this plan
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-error-boundary package</name>
  <files>package.json, bun.lock</files>
  <action>Run `bun add react-error-boundary` to install the error boundary library. This provides a robust, battle-tested ErrorBoundary component compatible with React 19. Use react-error-boundary (not custom implementation) as it handles edge cases, supports error reset, and provides TypeScript types.</action>
  <verify>Check package.json contains "react-error-boundary" dependency and bun.lock updated</verify>
  <done>react-error-boundary package installed and available for import</done>
</task>

<task type="auto">
  <name>Task 2: Create ErrorFallback component</name>
  <files>src/components/error-fallback.tsx</files>
  <action>Create ErrorFallback component accepting { error, resetErrorBoundary } props. Display centered layout with: app title "Trust Admin", error message heading "Something went wrong", error.message in gray text, and blue "Try again" button calling resetErrorBoundary. Use Tailwind classes: `flex items-center justify-center min-h-screen` for centering, `space-y-4` for spacing, `text-2xl font-bold` for heading, `text-gray-600` for message, `px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600` for button. Keep styling consistent with existing UI (check app-sidebar.tsx for color palette). Export as named export.</action>
  <verify>TypeScript compiles, component can be imported</verify>
  <done>ErrorFallback component created with styled fallback UI and reset button</done>
</task>

<task type="auto">
  <name>Task 3: Wrap App with ErrorBoundary in main.tsx</name>
  <files>src/main.tsx</files>
  <action>Import ErrorBoundary from 'react-error-boundary' and ErrorFallback from '@/components/error-fallback'. Wrap entire <App /> component with <ErrorBoundary FallbackComponent={ErrorFallback}>. Place ErrorBoundary inside StrictMode but wrapping App and Toaster. Order: StrictMode → ErrorBoundary → (App + Toaster). This catches errors from all components including routing. Add onError prop logging to console: `onError={(error, info) => console.error('ErrorBoundary caught:', error, info)}`.</action>
  <verify>Dev server shows no errors. Test by adding throw new Error('test') in a component, verify fallback UI displays. Click "Try again" to reset.</verify>
  <done>ErrorBoundary wraps application, catches component crashes, displays fallback UI with reset functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run dev` starts without errors
- [ ] TypeScript compilation passes
- [ ] Manual test: Trigger component error, verify fallback UI appears
- [ ] "Try again" button resets error boundary and restores app
- [ ] Phase 3 complete: Toast notifications + Error Boundary working
</verification>

<success_criteria>

- react-error-boundary package installed
- ErrorFallback component created with styled UI
- ErrorBoundary wraps entire application
- Component crashes display user-friendly fallback
- Reset functionality works
- Phase 3 complete: All 3 plans executed
- Users see clear error messages for both API failures (toasts) and component crashes (boundary)
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-notification-system/03-03-SUMMARY.md`:

# Phase 3 Plan 03: Add Error Boundary Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Installed react-error-boundary package
- Created ErrorFallback component with reset functionality
- Wrapped application with ErrorBoundary
- Phase 3 complete: Complete error notification system

## Files Created/Modified

- `package.json` - Added react-error-boundary dependency
- `src/components/error-fallback.tsx` - Fallback UI component
- `src/main.tsx` - Added ErrorBoundary wrapper

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 3 complete. Ready for Phase 4: Component Extraction Patterns
</output>
