---
phase: 03-error-notification-system
plan: 01
type: execute
---

<objective>
Install and configure Sonner toast notification library to provide visual feedback for user actions and errors.

Purpose: Enable user-facing error messages instead of silent failures, improving UX and debugging.
Output: Working toast notification system integrated into React application with proper positioning and styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phase-3-test-completion/02-03-SUMMARY.md
@src/main.tsx
@src/hooks/use-query.ts

**Tech stack available:**
- React 19.2.3
- Vite 7.3.0
- TailwindCSS 4.1.18
- Bun package manager

**Established patterns:**
- Component files use kebab-case: `use-toast-error.ts`
- Hooks use `use` prefix: `useToastError()`
- Imports organized: React → External → Internal
- Double quotes for strings

**Constraining decisions:**
- Phase 1: Working API POST endpoints (validation bug resolved)
- Phase 2: 100% integration test pass rate (48/48 tests)
- Keep current tech stack simple (no heavy frameworks)

**Issues being addressed:**
- Missing error handling in UI (from CONCERNS.md)
- Network errors fail silently with no user feedback
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sonner toast library</name>
  <files>package.json, bun.lock</files>
  <action>Run `bun add sonner` to install the toast notification library. Sonner is React 19 compatible, lightweight (~5KB), and provides accessible toast notifications with swipe-to-dismiss. Use Sonner (not react-hot-toast) as it has better TypeScript support and Tailwind integration.</action>
  <verify>Check package.json contains "sonner" dependency and bun.lock updated</verify>
  <done>sonner package installed and available for import</done>
</task>

<task type="auto">
  <name>Task 2: Add Toaster component to application root</name>
  <files>src/main.tsx</files>
  <action>Import and render Toaster component from sonner in main.tsx. Place after <App /> component (before closing StrictMode). Configure with position="top-right" for desktop visibility, duration={5000} for 5-second auto-dismiss, and richColors={true} for better visual feedback. Follow existing import organization: React imports first, then external libraries.</action>
  <verify>Dev server shows no errors, toast system ready (test with browser console: import { toast } from 'sonner'; toast('test'))</verify>
  <done>Toaster component rendered, positioned top-right, 5-second duration, ready for use throughout app</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun install` completes without errors
- [ ] Dev server starts without errors (`bun run dev:ui`)
- [ ] No TypeScript errors in main.tsx
- [ ] Toaster component visible in React DevTools
</verification>

<success_criteria>

- sonner package installed
- Toaster component added to main.tsx
- Position configured (top-right)
- Duration configured (5 seconds)
- No build errors
- Ready for error notification hook integration (Plan 03-02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-notification-system/03-01-SUMMARY.md`:

# Phase 3 Plan 01: Install and Configure Toast Library Summary

**[Substantive one-liner - what was accomplished]**

## Accomplishments

- Installed sonner package
- Configured Toaster component with positioning and duration
- Toast system ready for error notifications

## Files Created/Modified

- `package.json` - Added sonner dependency
- `src/main.tsx` - Added Toaster component

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 03-02: Create error notification hook and integrate with query factory
</output>
