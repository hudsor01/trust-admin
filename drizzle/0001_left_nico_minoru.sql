-- Historical migration. The baseline 0000_high_ares.sql was regenerated
-- from a pg_dump of the live production schema (which already reflects the
-- results of this migration's original DROP CONSTRAINT / DROP INDEX
-- operations), so applying this file in sequence after 0000 would fail on
-- a fresh DB ("constraint does not exist"). Contents replaced with a
-- no-op so ordering is preserved for drizzle's journal without re-doing
-- work that is already baked into the baseline.
--
-- Existing DBs that have this migration marked applied in
-- __drizzle_migrations will see a new hash on next `bun run db:migrate`
-- and attempt to apply this (no-op) file — safe.
SELECT 1;
